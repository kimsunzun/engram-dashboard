node_modules/
dist/
target/
.DS_Store

# 로컬 런타임 데이터(데몬 토큰·프로필 등) — 절대 커밋 금지. data_dir 기본 위치(S13 TRD).
.engram-data/

# 핸드오프 히스토리(/handoff용) — 기록 보존 위해 git 추적(2026-07-09, 사용자 결정). history/ append-only.
.ccb/

# 작업용 스크린샷 보관함(cdp shot 등) — 로컬 전용, 커밋 안 함.
_wip/shots/
