# Step 타임라인 — Engram Dashboard

언제·무엇을·어떻게 했는지 시간순 기록. 산출 문서와 커밋을 매핑한다.
상세는 각 폴더 참조: `design/`(설계) · `reviews/`(검증) · `briefings/`(구현 지시) · `spec/`(요구사항) · `history/`(초안).

**검증 3-게이트:** 코더(dco23 Opus / dcs24 Sonnet) → LLD 리뷰(dr26 Fable) → QA(dq25, build/lint/test).

---

## S0 — View phase (이전 세션)
- **무엇:** 더미 데이터로 UI 골격 — 테마/폰트/레이아웃/슬롯/에이전트 트리/diff/팝업 (Step 1–10 + 슬롯 동적 분할)
- **결과:** Tauri 창에 더미 대시보드 동작 (백엔드 없이 UI 검증 완료)
- **문서:** `spec/view-spec.md` (+ `view-spec-gpt-review.md`), `spec/requirements.md`, `spec/research.md`

## S1 — 백엔드 설계 (이전 세션)
- **무엇:** 백엔드 아키텍처 → 상세설계(LLD) → 프론트 통합 설계
- **어떻게:** architecture 초안 → Gemini/GPT 리뷰 → final. LLD Stage 1 작성 → **fable/Gemini/GPT 3자 adversarial 검증** → GO. frontend-integration LLD도 동일 3자 검증.
- **결과:** 구현 계약서 확정 — `pty/` Tauri 격리(OutputSink/StatusSink), kill 6단계, C4 replay→live, AppState 단일 manager
- **문서:** `design/backend-architecture.md`, `design/backend-lld-stage1.md`, `design/frontend-integration-lld.md`, `reviews/*`

## S2 — Phase 0: Spike (2026-06-11)
- **무엇:** 본 구현 전 PTY kill 시퀀스를 Windows 실기기에서 실측
- **어떻게:** dco23 — `examples/spike.rs`로 portable-pty spawn → kill 6단계 → reader join 타이밍 측정
- **결과:** ✅ PASS. `master.take()`(ConPTY 종료)로 reader가 **17ms** 내 EOF. LLD 가정 그대로.
- **문서:** `briefings/phase0-spike.md`

## S3 — Phase 1: 백엔드 PTY 코어  · 커밋 `575e36d`
- **무엇:** `pty/` 6모듈 + `logging/` + headless 테스트
- **어떻게:** m1 types → m2 session(C4 subscribe) → m3 drain(lock-밖-send) → m4 platform/windows(JobObject) → m5 manager(kill 6단계) → m6 logging → headless. 각 모듈 3-게이트 통과.
- **결과:** ✅ headless PASS — spawn→write→resize→kill, 상태 Running→Exiting→Killed, kill **23ms**. `pty/` tauri import 0. dr26이 win_err 버그·transition race·poison 정책 등 실결함 포착.
- **문서:** `briefings/m1-types` ~ `m6b-headless`, `design/backend-lld-stage1.md`

## S4 — Channel spike: tauri 핀 결정 (Phase 2 직전)
- **무엇:** tauri 버전 확정을 위한 Channel 무손실 실측 (LLD는 "2.5 금지"였으나 caret이라 2.11.2 resolve)
- **어떻게:** dco23 — 임시 `channel_spike` command로 1000회 연속 send, 프론트 수신 카운트
- **결과:** ✅ 1000/1000 무손실 (이슈 #11421은 Linux 특정, Windows WebView2 미재현). **최신 2.x(2.11.2) 유지 확정.**
- **문서:** `briefings/channel-spike.md`

## S5 — Phase 2: Tauri 연결 계층  · 커밋 `f959304`
- **무엇:** commands 8개 + `lib.rs`(AppState / ChannelOutputSink / TauriStatusSink / setup)
- **어떻게:** dcs24 — thin wrapper + OutputSink/StatusSink의 Tauri 구현. 3-게이트.
- **결과:** ✅ dead_code 전멸(코어가 command 통해 사용됨), 앱 기동. `RunEvent::ExitRequested → shutdown_all` graceful 종료.
- **문서:** `briefings/m7-commands-lib.md`

## S6 — 백엔드 마감  · 커밋 `26dc649`
- **무엇:** ① 로그 API키 마스킹(T-1) + ④ shutdown 병렬 kill(T-8). ② cwd 검증은 **claude 자체 권한과 중복이라 스킵**.
- **어떻게:** dcs24 마스킹(regex, 키 패턴 6종) / dco23 병렬 kill(`std::thread::scope`). 3-게이트.
- **결과:** ✅ debug 로깅 시 키 노출 차단, 앱 종료 worst case N×5s→5s.
- **문서:** `tracking.md` T-1 / T-6 / T-8

## S7 — Phase 3: 프론트 통합 3a–3c  · 커밋 `ca61cbd`
- **무엇:** API 레이어(types/ptyApi) → eventBus+store → TerminalSlot 실제 PTY 연결
- **어떻게:** dcs24 — 3a→3b→3c. C2 reset / T-2 seq dedup / G-1 cleanup / 입력가드 / resize debounce.
- **결과:** ✅ **첫 E2E** — 실제 창에서 claude 기동, **PTY ↔ Tauri ↔ React 전체 파이프라인 실증**. 3d(popup+monaco)는 보류.
- **문서:** `briefings/m8a-api-layer` ~ `m8d-popup`, `design/frontend-integration-lld.md`

## S8 — 문서 정리  · 커밋 `fdf6d06` + 진행 중
- **무엇:** core/dashboard 통합 → docs 트리 재편 → 과정/정설 분리 + 타임라인
- **결과:** ✅ 모든 자료 `apps/engram-dashboard` 일원화. `process/`(과정 기록) + `reference/`(정설, 추후) 분리, 이 `step-log.md` 작성.

## S9 — 세션 저장/복원 (코어 GO)  · 커밋 `8981cb9`~`7052bc2`
- **무엇:** claude 세션 무손실 복원 + 에이전트 프로필 영속화 + 약간의 추상화(claude.rs/profile.rs 분리). 자동재시작(restart_agent)은 **게이트**(설계만).
- **어떻게(H-4 순서, 매니저 직접 구현 + fable 리뷰 게이트):**
  1. `profile.rs` + ProfileRegistry(단일 소유자, sid 생성·갱신) + dunce — `8981cb9`
  2. `persistence/` atomic agents.json(tmp+sync_all+rename+parent fsync, schema_version, .corrupt 보존) — `d7f42f4`
  3. `session_tracker.rs` sid drift 폴링(best-effort, PID shim 우회 스캔, 단일 스레드+정지핸들, degraded 강등) — `0a59fa3`
  4. LLD 개정 a~g(backend §18 / frontend §11, Stage-1 보존+addendum) — `1b9499c`
  5. `claude.rs` 격리 + manager `spawn_agent(profile,mode)`/`restore_all`(백그라운드)/fallback(조기종료 윈도→fresh, 종점 Failed) — `f67476b`
  6. profile CRUD 커맨드 + 프론트 TS 미러(epoch 재구독) + **fable 리뷰 수정** — `7052bc2`
- **검증:** unit test 19, headless PASS, `cargo fmt`·`tsc` 클린, `pty/` tauri import 0.
- **fable 리뷰(조건부 GO→수정 완료):** C-1 remove_session drain 대기(stale Killed 경합 제거), M-1 resume 조기종료 code 무관 fallback, Mn-1 status_changed epoch 동봉, Mn-2 Started variant, Mn-5 단일 persist.
- **핵심 메커니즘:** spawn 시 `--session-id <uuid>`로 우리가 sid 통제 → 재시작 `--resume`로 무손실 복원. `/clear`로 sid 바뀌면 `sessions/<pid>.json` 폴링으로 따라잡아 즉시 persist. 복원 정확성은 우리 통제 sid에만 의존(추적 파일은 best-effort).
- **문서:** `S9-session-restore/session-restore-lld.md`, `-code-plan.md`(§H), `spike-results.md`, `s9-*-review-*.md`

## S10 — 백엔드 추상화 (AgentTransport/OutputEvent)  · 커밋 `60fe859`~`fb50917`
- **무엇:** 검증된 S9 PTY 코드를 `AgentManager → AgentSession(OutputCore) → dyn AgentTransport(PtyTransport/ApiTransport)` 구조로 재편. 멀티 백엔드(claude/codex/gemini 콘솔 + API) 통합 인터페이스. **회귀 0**이 목표(기능 추가 아닌 seam 추상화).
- **어떻게(9단계, 단계별 build/test/commit + fable 리뷰 게이트, 오케스트레이터=서브에이전트 디스패치):**
  1. 중립 타입/enum: `OutputEvent`/`InputEvent`(확장 enum) · `TerminalReason` · `Capabilities`(영역별) · `CommandSpec` · PtyChunk→`OutputChunk` — `60fe859`
  2. `output_core.rs` OutputCore: seq/replay/subscribers/status/finalize, emit(variant-agnostic)/finish(finalize 1회)/join_pump/enter_exiting/subscribe — `dbcde55`
  3. `transport/{mod,pty}.rs` AgentTransport trait + PtyTransport: spawn/kill 1~5단계/drain_loop+transition 흡수, pump 스레드, shutdown 멱등 — `cd3b048`
  4. `backend/{mod,claude,shell}.rs` AgentBackend + CommandSpec 산출 dispatch — `38d2fe7`
  5. `session.rs` AgentSession 합성: kill=shutdown+join_pump 2동사 — `7c68e31`
  6. `manager.rs` AgentManager(PtyManager 개명) 신경로 전환 + 옛 구조(PtySession/drain.rs/claude.rs) 제거 — `c954305`
  7/8. ApiTransport 껍데기 + codex/gemini stub(dispatch 미연결, best-guess) + interrupt_agent 커맨드 + AgentInfo.capabilities + TS 미러 — `fb50917`
- **검증:** unit test 38(S9 19→backend 이관·stub 추가), headless·transport_smoke·session_smoke PASS, full build(bin), `cargo fmt`·`tsc` 클린, `pty/` tauri import 0.
- **신경로 실측:** `examples/transport_smoke.rs`·`session_smoke.rs` 신설 — manager 없이 PtyTransport/AgentSession 직접 검증(shutdown→pump EOF→finish(Killed) 인과, hang 없음).
- **fable 리뷰(2/3/6 + 최종 게이트, 전부 GO):** 회귀 0 확인. B-1(enter_exiting/finish 알림 역전 창)은 S9 기존 동작=natural-exit race, agent-list-updated 완화로 회귀 아님. attach_pump race는 mpsc 무한버퍼로 무손실. 소유권 분할(transport/core/session) 깨끗, "교체 가능" 추상화 성립(ApiTransport가 같은 trait로 끼워짐).
- **단계화(후일):** OutputEvent API variant(TextDelta/Usage 등)·ApiTransport 내부 HTTP 스트림·codex/gemini CLI 플래그(spike 후 AgentCommand variant 추가)·semantic event log/replay 고도화.
- **문서:** `S10-backend-abstraction/agent-transport-design.md`, `impl-spec.md`(코더 공통 참조 — 구체 시그니처)

## S11 — 에이전트 트리 슬롯화 + capability 잠복 버그 수정 (2026-06-14)
- **무엇:** ① `AgentInfo.name` 백엔드 채움(ProfileRegistry 조회, AgentSession에 중복 필드 안 둠). ② 슬롯 콘텐츠 모델 객체 union(`{kind:'terminal',agentId}|{kind:'tree'}`) + `LayoutCommand`/`dispatch` 단일 제어 표면(§5, `window.__engramLayout` 노출=LLM 제어 경로). ③ 트리를 슬롯 콘텐츠로(`LayoutRenderer` kind 분기). ④ `AgentTree` 재작성: 클릭=selectOnly(자기파괴 루프 방지), 우클릭 메뉴(배치/종료/중단), interrupt는 `ControlCaps.interrupt` capability 분기, 이름 표시. ⑤ 슬롯 우클릭 트리/터미널 토글.
- **어떻게:** `/consult`(GPT+Gemini+opus judge 블라인드 교차검증) → 설계 보정(string enum→객체 union 확정, store 직접호출→dispatch, name=ProfileRegistry 조회가 옳음 판정). 코딩 → opus 리뷰([높음] AgentTree 우클릭 메뉴 바깥닫기 가드 누락 수정) → QA(test/tsc/GUI 실측).
- **★capability 잠복 버그★:** `src-tauri/capabilities/` 파일이 없어 core event listen이 막혀, `agent-list-updated`/`status-changed` 상태 브로드캐스트가 **프론트에 원래부터 안 닿던** 버그(PTY 출력은 Channel이라 권한 무관 → 여태 미노출). 증상: spawn해도 트리 미갱신 → 거기서 kill 불가. `capabilities/default.json`(`core:default`+`core:event:default`)로 해결. spawn→트리 추가 / kill→트리 제거 실시간 GUI 실측 PASS.
- **검증:** unit test 38/38, `tsc` 0, `cargo fmt`, `pty/` tauri import 0, GUI 실측(9223 cdp eval — 트리 토글·이름·spawn/kill 실시간 갱신·`__engramLayout` 제어표면) 전부 PASS.
- **보류:** tabs 구조(`content`→`tabs[]` 확장)·레이아웃 영속화·UI 동작의 완전 backend 이관(데몬화 때). `assignAgent`/`setSlotContent` 명령 중복은 편의로 유지.

## S12 — 데몬화 (설계 단계, 2026-06-14)  · 별도 repo 분리
- **repo 분리:** engram-dashboard를 Engram 모노레포에서 분리(54 커밋 history 보존, filter-repo) → `github.com/kimsunzun/engram-dashboard`(private) push. 모노레포는 추적 0(.gitignore), 폴더는 당분간 apps/engram-dashboard 잔류(나중에 이동). engram=고수준 LLM 워크스페이스 / engram-dashboard=LLM 운용 툴로 완전 분리.
- **데몬화 결정·설계(consult 2회 교차검증):** 라이브러리 실조사(턴키 없음, 커스텀 불가피, replay=데몬 in-process, 경로 B-direct, raw tokio-tungstenite) + 세부 설계 비판. 상세: `S12-daemonization/{ipc-library-consult,daemon-design}.md`, tracking D-8.
- **확정 핵심:** 단일 WS 연결(lane 분리 금지)·output hot path 커스텀 binary frame/control JSON·ring 잘림 truncated 분기·토큰 env/ACL portfile(arg 금지)·emit try_send=코어변경0·Job breakaway가 단일 장애점(spike #1). 두-모드 토글(Embedded/Daemon). Cargo workspace(core/protocol/daemon/tauri).
- **상태:** 설계 완료, 사용자 결정 7개(+transport=8) 전부 완료(daemon-design §8-d). 구현 진입.
- **★spike #1 완료(2026-06-14) — Job breakaway 실측, 판정 GO.** 측정 환경 부모 Job=`0x2000`(KILL_ON_JOB_CLOSE+breakaway 불허, worst-case). `CREATE_BREAKAWAY_FROM_JOB` 직접 실패(os err 5)·`start /b` fallback 동반사망(설계 가정 폐기)·**WMI `Win32_Process.Create` 분리 성공(in_job=false).** → spawn=WMI 채택, 파생제약: WMI는 env 주입 불가 → 토큰=ACL port.json 강제. 코드 `examples/spike_breakaway.rs`(보존), 결과 `S12-daemonization/spike1-breakaway-result.md`.
- **✅ phase 0 완료(2026-06-14):** `engram-dashboard-protocol` 독립 crate — AgentCommand/AgentEvent/OutputChunk/SubscribeAction + domain 미러 + binary frame codec + ts-rs 바인딩. 테스트 21 PASS. 커밋 `61f2d0f`.
- **✅ phase 1 완료(2026-06-14):** Cargo workspace 추출 + 프론트 AgentClient.
  - 1a: pty/persistence/logging→`crates/engram-dashboard-core`(git mv, 내부 crate:: 무수정). examples 동반. 격리 게이트 0. 회귀 0(unit 38/headless·smoke/빌드/tauri dev target 재배치 정상). 커밋 `576c5e1`.
  - 1b: AgentClient 인터페이스+EmbeddedClient(Channel·base64·#13133 캡슐화)+clientFactory(§5 window.__ENGRAM_AGENT__). 컴포넌트 ptyApi→agentClient. GUI E2E PASS(spawn→subscribe→writeStdin 277B/3청크, kill 0, UI 정상). 커밋 `5346240`.
- **phase 2 consult 완료(2026-06-14):** GPT·Gemini·Claude-opus 블라인드+judge. 병합: `S12-daemonization/phase2-design-consult.md`. 핵심: "코어 변경 0"은 거짓(raw-first 필요)·try_send 안전·core protocol무의존(데몬 번역)·연결단위 cleanup·SubscribeAck 순서 race 실재·ReplayBuffer event-cap 필요(GPT)·WsStatusSink 필요(GPT). Gemini 오류 2(split Control Lane 데드락 오인·WMI를 PTY child에 오적용) 폐기.
- **✅ phase 2 Step1(raw 경계화) 커밋 `122078f`:** OutputSink::send(OutputFrame raw), base64 책임을 ChannelOutputSink로 이동. 회귀 0(unit 38+headless+GUI E2E 285B). 
- **✅ phase 2 Step3(ReplayBuffer event-cap) — unit 39.**
- **✅ 구현 실행 규약 신설(CLAUDE.md):** 비자명 코드는 메인=오케스트레이터, 코더/리뷰어/QA 서브에이전트 분리 스폰. 리뷰어=fable, 불가 시 opus 고노력 대체(스킵 금지). 단위테스트 누적→cargo test 한 방.
- **✅ phase 2 Step4a(데몬 수명주기 토대) 커밋 `2db5bf0`:** engram-dashboard-daemon bin — 단일 인스턴스(Global\ mutex)+랜덤 포트+256bit 토큰+atomic daemon.json+AgentManager 소유. WS는 4b. **코더(opus)→리뷰어(opus)→수정 사이클로 진행**: 리뷰어가 Blocker 2 잡음 — B1(data_dir Embedded와 불일치 %LOCALAPPDATA% vs %APPDATA% → dirs::data_dir().join("com.engram.dashboard")로 일치) / B2(mutex Local\ 세션한정 → Global\+USERNAME 사용자단위). QA: build/clippy0/test8.
- **결정 정리:** 1(SubscribeAck=단일 conn_tx 순서)·2(송신=agent별 큐+select!)·3(seq=number 유지)는 매니저가 확정. 5(Origin)는 측정해 채움. 4(port.json ACL)는 로컬 v1이라 보류(원격 열 때 보안 담당).
- **✅ phase 2 Step4b(WS 서버) 커밋 `11d0f76`:** ws.rs — 단일 WS·단일 writer(conn_tx). protocol Auth variant 추가. accept_hdr_async(Origin allowlist)→1s Auth(상수시간)→split→Hello+목록스냅샷→read/write task→cleanup. WsOutputSink(try_send only)/DaemonStatusSink(broadcast). Subscribe v1=full replay+Reset(afterSeq resume=4c). 코더(opus)→리뷰어(opus) Major 4→수정: M1+M2(conn_tx full 좀비/replay gap→out-of-band Notify close_signal), M3(AgentInfo roundtrip 전 variant 테스트+agent_id 로그), M4(StopDaemon force 거부+kill_agents always-kill 명시). QA: build/clippy0/daemon16/protocol21.
- **✅ phase 2 Step4c(afterSeq resume) — core 45 / daemon 17:** 코어 중립 `subscribe_from(sink, after_seq, epoch_matches, on_ready)` 신설(types.rs `ReplayKind{FromOldest/Truncated/Resumed}`/`SubscribeOutcome`). 분기: epoch 불일치||after_seq=None→FromOldest(전체), s<oldest→Truncated, s>=oldest→Resumed(partition_point tail만). 데몬 `handle_subscribe`가 ReplayKind→protocol SubscribeAction 매핑(`kind_to_action`). 코더(opus)→리뷰어(opus) **Major M-A(TOCTOU)**: 데몬 get_snapshot(A)로 Ack 채우고 코어 subscribe_from(B)로 실 전송→두 스냅샷 사이 evict 시 replay_from 어긋나 손실 미인지. **근본 수정**: get_snapshot/predict_ack 제거, subscribe_from이 단일 스냅샷 outcome 확정 후 **replay 전송 직전 lock 보유 중 `on_ready(&outcome)` 콜백**으로 데몬이 Ack를 conn_tx에 먼저 try_send→불변식2(Ack→replay FIFO)+C4 동시 유지하며 TOCTOU 제거. m-1: 경량 `agent_epoch()` 추가(list_agents 순회 제거). QA: fmt/build/clippy0/core45/daemon17, 코어 protocol·tauri import 0.
- **✅ phase 2 Step5(WMI discovery) — protocol 22 / daemon 19 / core 49 / src-tauri 17:** Tauri→데몬 발견·spawn 경로. `DaemonInfo`를 protocol crate로 이동(공유 IPC 계약, ts-rs 미export). src-tauri `discovery.rs`: `ensure_daemon`(daemon.json 읽기→stale 검사→없/죽으면 WMI `Win32_Process.Create` 절대경로 spawn→50ms 폴링 회수). OS/WMI/시계를 trait(PidLiveness/DaemonReader/Spawner/Clock)로 주입해 순수 `ensure_with`만 단위테스트. §5 `discover_daemon` command 노출(자동부팅 배선은 phase4). 코더(opus)→리뷰어(opus) **NO-GO**: C1(`CoUninitialize` 누락+`RPC_E_CHANGED_MODE` 무시=COM 누수)·M1(살아있는 데몬 daemon.json stale 오판 삭제→부팅때만 쓰므로 영구교착)·M2(PID 재사용 false-live 교착)·커버리지 갭(real 진입점 4개 무테스트). **수정**: C1=`ComGuard` RAII(needs_uninit 분류 후 Drop에서 1회 uninit)·M2=`DaemonInfo.start_time`(GetProcessTimes creation, `#[serde(default)]` wire호환)로 "PID 생존 AND creation time 일치" 판정→PID 재사용 직접 구분·liveness 공유함수를 core `platform/process.rs`로 DRY(daemon/tauri 사본 제거)·M1=timeout 직전 옛 info live 재검사 복구 안전망·무테스트 함수 전부 커버(ComGuard매핑/DTO/locate/ensure_daemon/폴링 corrupt·version·복구 분기). QA: fmt/build/clippy0, 코어 tauri import 0.
- **✅ phase 2 Step6(격리 하네스 WS E2E) — daemon lib 19 + ws_e2e 23:** 프론트 없이 데몬 WS 서버를 in-process 기동(127.0.0.1:0, 알려진 token, MemProfileStore)하고 tokio-tungstenite 클라로 전 경로 검증. **운영 리팩터**: main 본체를 `lib.rs`(`run`/`run_accept_loop`/`build_manager_with_store`/`start_test_server`)로 이동, main은 thin entry, 운영·테스트 동일 코드 공유(회귀 0). **출력평면 12종**(auth 성공/실패/타임아웃·출력순서·replay→live FIFO·afterSeq resume·truncated·epoch mismatch·slow-consumer close·reconnect 무손실·high-throughput·멀티구독 역다중화). 코더(opus)→리뷰어(opus, mutation으로 위양성 아님 실증) **Major 2**: M1(WS command dispatch가 E2E 미경유 — Auth/Subscribe만 검증)·M2(빈 `#[ignore]` 3종이 `--ignored`서 PASS 위장). **수정**: M1=**dispatch E2E 11종 추가**(Spawn→Ack+AgentListUpdated·WriteStdin→Raw·Kill·Interrupt·Resize·Unsubscribe·ListAgents·StopDaemon force거부+force종료·2차Auth거부·binary거부·파싱실패 Error, request_id echo)·M2=`unimplemented!()`로 교체해 `--ignored` 시 RED(위장 제거)·m1=slow-consumer 출력량 상향(28MB)으로 flaky 완화. 프로필 등록은 `manager.profiles().upsert()` 공개 API(운영 무영향). QA: fmt/build/clippy0, ws_e2e 23 PASS, `--ignored` 3 RED(의도), 회귀 0.
- **★ phase 2(데몬화 코어) 완료 ★** — protocol/core/daemon/src-tauri 4-crate workspace, WS 서버(auth/replay/resume/backpressure)·WMI discovery·격리 하네스 E2E. **남은 것**: ①실프로세스 3종 하네스(데몬 .exe kill→PTY Job 정리/single-instance mutex/stale discovery — Step6 `unimplemented!` RED 표식, **다음 step7 = 실 .exe harness bin**) ②프론트 DaemonClient(WS) + 두-모드 토글 배선(phase4) ③타입 드리프트 게이트 ④Origin 실측 ⑤Interrupt 출력정지·Resize PTY 실반영 검증수단 ⑥request_id idempotency dedup table(미구현).
- **✅ phase 2 전체 코드 검사(3축 리뷰 opus 고노력) + 정리 — daemon lib 24 / e2e 26 / core 49:** 축A(구조)·축B(불변식)·축C(테스트) 병렬 적대 점검. **Critical 0** — crate 의존 단방향·코어 격리 무위반·8개 핵심 불변식(kill 인과/finalize 1회/락순서/C4/backpressure/epoch/소유권/tokio↔std) 데몬 경로 보존 확인. **자율 정리 적용**: (1)`core_*_to_wire` reflection 왕복(serde to_value→from_value, silent drop 위험)→**명시 변환 함수**로 교체(필드 누락=컴파일 에러, RestoreOutcome Uuid→String 명시, core protocol 무의존 유지) (2)`dirs` 5→6 통일(두-모드 data_dir 일치 근거 못박음) (3)StopDaemon force 판정 terminal/Exiting 제외(실활성만) (4)테스트 갭: OriginCheck 3종(mutation 무방비 실증분)·dispatch 실패 arm e2e(case24-26 unknown agent/profile→Error req echo)·resolve_data_dir 단언·case02 deadline 단축. **보류(타 단계 묶음)**: pty/→agent/ rename(step7 후), ts-rs 바인딩 프론트 배선·DaemonInfoDto(phase4), AgentCommand→SpawnSpec 개명, restore abort 협조취소(step7 Job 하네스). QA: clippy0, 코어 격리 0.
- **✅ phase 2 step7(실프로세스 격리 하네스) — daemon lib 25 + e2e 26 + ignored 3 PASS:** Step6 `unimplemented!` RED 표식이던 실프로세스 3종을 실제 데몬 .exe 기동 검증으로 구현. ①데몬 kill→PTY child Job 동반사망(실 daemon_pid + child cmd.exe 식별 후 TerminateProcess→child 사망 관측, core `platform/process.rs::child_pids` 추가) ②single-instance(같은 instance_key+data_dir로 2번 데몬 spawn→3s내 exit+1번 daemon.json 불변) ③stale daemon.json→데몬 자가 덮어쓰기(start_time 갱신). `CARGO_BIN_EXE_engram-dashboard-daemon` 로 실 .exe 경로. 코더(opus)→QA **flaky 적출**: `--ignored` 실행마다 다른 케이스 FAIL — 원인=전역 mutex(`Global\...-USERNAME`) 경합(병렬 테스트가 동시 데몬 기동→첫째만 mutex 획득·daemon.json 발행, 나머지 거부 종료). **수정(fixer)**: `instance.rs` mutex_name에 `ENGRAM_INSTANCE_KEY` env override(미설정=기존 USERNAME, 회귀 0) + 테스트별 유니크 data_dir/instance_key 격리(single-instance만 동일 key로 충돌 유발) + 데몬 stdout/stderr 캡처 진단. `--ignored` 3회 연속 3 PASS(flaky 제거). QA: clippy0, daemon 기본 25+26 회귀 0.
- **★ phase 2 (데몬화) 전체 완료 ★** — 4-crate workspace + WS 서버 + WMI discovery + 격리 하네스(in-process 26 + 실프로세스 3). 남은 것은 **phase4(프론트 DaemonClient WS + 두-모드 토글 배선)** 및 별건(pty/→agent/ rename, ts-rs 바인딩 프론트 배선, AgentCommand→SpawnSpec 개명, Origin 실측, request_id dedup).
- **✅ 데몬 3대장(tmux/Zellij/Mosh) 갭 분석(3축 opus 병렬) + v1 갭 2건 구현 — core 55 / daemon lib 25 + e2e 29:** 참조 소스(`I:/Engram_Workspace/references/`) 직접 대조. **충족 확인**: 세션 영속/이름, Windows I/O(portable-pty+Job — Zellij보다 자식정리 강함), 재연결 무손실(afterSeq replay=안정망 짧은 끊김선 Mosh 동급). **v1 필수 2건 구현**(coder→reviewer opus GO, mutation 위양성 아님 실증): ①**WS keepalive**(tmux·Mosh 만장일치) — 데몬 능동 ping 부재로 half-open 연결(sleep/wake·NAT·모바일터널, FIN 없음) 미감지→좀비 구독·broadcast 누수. write_task에 interval ping arm + read_task last-recv(Arc<AtomicU64>) + idle_timeout(기본20s/50s, 테스트 주입 가능) 초과 시 close→cleanup. ②**데몬 panic 격리**(Zellij) — pump 스레드 panic 시 silent 출력정지. main `install_panic_hook`(Once+체이닝) + pump를 `catch_unwind`로 감싸 panic→`finish(Error)` Failed 전이(done_tx는 밖이라 join_pump hang 0, finalize 1회 보존). 리뷰어 M1(emit 내 panic 시 core lock poison→재구독 재-panic, 단 emit에 panic원 없음+연결task 격리)→**주석 정정**(fail-fast 의도 명확화), poison-tolerant 통일은 fail-fast가 안전이라 보류. 경량: alt-screen evict 케이스 daemon-design 문서 정정. **phase4 로드맵 박음**: 다중연결 입력 lease(Zellij, viewport_id seam 존재)·멀티 resize 협상(tmux)·VT-framebuffer 화면상태동기(Mosh 갭2, 대역폭+alt-screen)·roaming TCP→UDP(Mosh 갭3)·predictive echo(Mosh 갭4). 이들은 소비처가 프론트 DaemonClient라 phase4와 묶음(지금 만들면 죽은 코드=§0).
- **✅ 데몬측 완결 갭 2건 추가 구현(lease + resize 협상) — daemon lib 25 + e2e 34:** "데몬에서 완결·ws_e2e로 검증되는 것은 미리 깐다(§0 seam)" 판단으로 구현(코어 변경 0, 데몬 정책). ①**멀티 resize 협상**(tmux smallest): per-agent viewport별 (cols,rows) 맵→smallest(독립 min) 협상→master.resize. viewport_id=None은 단일뷰어 직접(하위호환), 연결 종료 시 그 viewport 제거+재협상(detach 동치). ②**다중입력 lease**(명시 모델): per-agent `Option<conn_id>` lease. protocol `AcquireInput`/`ReleaseInput` + `InputLeaseChanged{held}` 추가. lease 비면 자유, 보유자만 통과, 타 conn WriteStdin→Error. 연결 종료 시 그 conn lease 자동해제(좀비 lock 방지). MultiViewState lock은 짧게 잡고 해제 후 manager 호출(§10). case30~34. **사건/주의**: 첫 리뷰어 에이전트가 mutation 검증(lease gate 무력화) 중 폴링 버그로 19분 후 비정상 종료하며 ws.rs의 mutation을 **원복 못 한 채 작업트리 오염** → 사용자 "보수적으로 가자" 지시로 재검수 중 build warning(`LeasePass::Denied` never constructed)으로 적발·원복. **데몬 실동작 실측**: 데몬 .exe 직접 기동→daemon.json 발행(port/token/start_time)·생존·종료 확인 + `--ignored` 실프로세스 3종 PASS. **Interrupt-lease 정책 미결(phase4)**: 현재 Interrupt(Ctrl+C)도 lease 게이트 대상 — 다른 뷰어 lease 보유 중 긴급 중단이 막힐 수 있음. lease 자체가 v1엔 멀티클라(phase4) 전 미사용이라 실영향 0, phase4 멀티클라 실사용 때 "긴급 중단은 누구나 허용" 여부 결정.
- **✅ phase4 1단계(프로필 CRUD + spawn WS wire) — protocol 26 / daemon lib 25 + e2e 44 / core 55:** DaemonClient 의 전제 — `AgentClient` 인터페이스 전 메서드가 WS command 로 표현 가능하도록 누락 wire 경로 추가(데몬 backend, 프론트 DaemonClient 는 다음). protocol `AgentCommand` 6 variant(SpawnByCwd/ListProfiles/CreateProfile/DeleteProfile/SpawnProfile/SetProfileAutoRestore/GetSnapshot) + `AgentEvent` 2(ProfileListUpdated/Snapshot) append(하위호환, codec golden 무회귀). domain.rs 에 wire 프로필 미러(AgentSpawnCommand/RestartPolicy/AgentProfile/SnapshotChunk) + 데몬 명시 변환(reflection 아님, core protocol 무의존 유지). dispatch 6 arm(manager.profiles/spawn/snapshot + request_id Ack/Error + CRUD 는 broadcast_profile_list). coder(opus)→리뷰어(opus 고노력, **포그라운드 강제로 폴링버그 없이 정상**) GO: EmbeddedClient↔dispatch 의미 전수 일치 대조·변환 완전성·mutation 3종 위양성 아님 실증. case35~44. **#6 반환타입 매핑(다음 단계 과제)**: wire 는 Ack+broadcast(ProfileListUpdated/AgentListUpdated)인데 인터페이스는 직접 반환(Promise<AgentProfile>) — broadcast event 에 request_id 없어 동시 생성 시 "내 것" 식별 race. DaemonClient 구현 시 Ack 페이로드에 생성결과 동봉 또는 `Created{request_id,..}` event append(하위호환)로 해소 — 현 단계 블로킹 아님. QA: clippy0, 격리 0.
- **✅ phase4-2 #6(반환 event) — protocol 32 / daemon e2e 44:** DaemonClient 가 인터페이스(`Promise<AgentProfile>`/`Promise<AgentInfo>`)를 동시생성 race 없이 채우도록 request_id 동봉 반환 event 추가. `AgentEvent` 에 `Created{request_id, profile}`·`Spawned{request_id, agent}` append(externally-tagged 하위호환). dispatch: CreateProfile→Created(+broadcast_profile_list 유지), SpawnByCwd/SpawnProfile→Spawned(성공)/Error(실패), Ack 중복 제거. bare `Spawn` arm 은 프론트 미사용(SpawnByCwd/SpawnProfile 만 호출)이라 Ack 유지(리뷰 확인). coder(opus)→reviewer-deep GO-with-nits(Blocker0, mutation 3건 위양성 아님 실증)→case35 cwd assert 보강. 커밋 `7ed756b`.
- **✅ phase4-2 step2(프론트 DaemonClient WS + 두-모드 토글) — 커밋 `45af572`:** `daemonClient.ts`(AgentClient WS 구현) + `clientFactory.ts` mode 토글(window.__ENGRAM_MODE__/localStorage, 기본 embedded·daemon opt-in, 회귀0). discover_daemon→ws→Auth→Hello, JSON text=AgentEvent / ArrayBuffer=binary frame(decodeOutputFrame 순수함수, codec.rs 와 바이트 단위 일치·UUID network order). request_id pending Map. 지수 백오프 재연결. coder(opus)→reviewer-deep **NO-GO 4건**(SubscribeAck replay_from dedup 재동기화 / resubscribe epoch 리셋 / 조회 끊김 reject / close in-flight)→수정→재검수 GO. tsc0.
- **✅ phase4-2 step4·5(Origin 실측 + 데몬모드 GUI E2E, cdp 9223):** 앱 띄워 실측. **Origin**: WebView2 dev `location.origin = http://localhost:1420` → 데몬 `ALLOWED_ORIGINS` 에 이미 포함(설계값 적중), WS 핸드셰이크 수용(연결 성공이 곧 Origin 통과). prod `tauri.localhost`/`https://tauri.localhost` 도 allowlist 존재. **E2E**: daemon 모드 전환→connected→spawn(Spawned)→subscribeOutput(replay+live binary frame)→writeStdin echo 왕복(`MARKER`/`ENGRAM_WS_OK` 정확 디코드, ANSI 무결)→kill(remaining0) 전 경로 PASS.
  - **★재연결 resume Major 버그 — GUI 실측이 정적 리뷰가 두 번 놓친 걸 적출★**: 소켓 강제 드롭(`ws.close()`, 데몬 생존)→재연결 시 seq `[2,1,2,3]`(중복+역전) 관측. 원인 2개 — (A) resubscribe 가 wire `epoch:null` 전송→데몬 epoch 불일치(ws.rs:1524 None=불일치)로 FromOldest 전체 replay→재배달, (B) dedup 기준을 `replay_from`(=보내는 첫 seq, ws_e2e:758 replay_from>last_seq)에 묶어 Resume 시 첫 프레임 off-by-one 유실. **수정**: dedup 을 클라 실배달 high-water(`lastDeliveredSeq`, 초기 -1) 기준 일원화 + resubscribe 는 알려진 epoch 전송(tail-only Resume) + epoch 변경 시에만 리셋. replay_from/truncated 정보용. reviewer-deep GO(재연결+epoch변경 동시도 데몬 FIFO+on_ready 순서로 안전). **GUI 재검증: 초기 [0,1,2] + 드롭→재연결 [3], dupes=0/inOrder=true/무손실.** 커밋 `6998404`.
  - **부수 발견(코드 버그 아님)**: start_time 필드 없는 옛(기능 이전) daemon.json + 죽은 PID 면, `pid_alive`(process.rs:66-68)가 OpenProcess 실패를 "보수적 live=true" 처리해 stale 을 live 로 오판→discover 가 죽은 포트 반환. 정상 daemon.json(start_time!=0)은 영향 없음. 옛 파일 삭제로 즉시 해소. **후속 nit**: `pid_alive` 가 ERROR_INVALID_PARAMETER(부재=dead)와 ERROR_ACCESS_DENIED(권한=live) 미구분 — start_time=0 fallback 경로 한정 저영향.
- **✅ 테스트 인프라 정비(2026-06-15) — 커밋 `e469e67`·`68d8295`·`6ed9bc3`:** ①**테스트 전략 문서**(`docs/testing-strategy.md`) 신설 — 3층(①단위 ②통합 ③실/시각) + 배치 일원화(검증 하네스는 전부 `tests/`, examples 는 데모·스파이크 전용). ②**core examples→tests 이관** — 로그 eyeball 하던 headless/transport_smoke/session_smoke 를 단언 기반 통합테스트(`crates/engram-dashboard-core/tests/`)로 전환, RecordingSink 로 단언(spawn→list1, echo, kill 후 list0, Exiting→Killed, hang 5s 미만). default 실행 → `cargo test -p core` 58(단위55+통합3). ③**프론트 vitest 도입** — `npm test`(vitest+jsdom), `daemonClient.test.ts`/`clientFactory.test.ts` 코로케이션. mock WS+invoke 로 decode/dedup/request_id/#13133/**재연결 resume 회귀**. reviewer-deep 가 mutation 3종(epoch=null/replay_from dedup/off-by-one)으로 회귀 테스트 실효 입증. **배경**: 재연결 버그가 정적 리뷰 2회 통과·GUI 실측에서야 잡힌 사건 → 로직은 ①(vitest)에서 잡고 CDP 는 ③ 시각 전용으로 강등(EDR 마찰도 감소). **EDR**: CDP(`--remote-debugging-port`)가 "브라우저 메모리 접근"으로 보안 탐지됨(2026-06-15) — 보안관제 문의·예외 진행 중.
- **✅ phase4-2 #1(이벤트 배선) — 커밋 `0ccd3fa`:** 데몬 모드에서 출력은 오나 spawn/kill 시 트리·상태바 정지하던 갭 해소. eventBus 가 Tauri 이벤트를 직접 listen 하던 걸(데몬 WS 이벤트 미수신) **AgentClient 인터페이스 경유로 통일** — `onAgentListUpdated`/`onStatusChanged`/`onRestoreResult`(sync disposer). EmbeddedClient=Tauri listen 래핑(syncListen, 등록 책임 eventBus→여기 이동, 회귀0), DaemonClient=콜백 레지스트리+handleEvent 라우팅(decode/dedup/재연결 무변경), eventBus=agentClient.on* 소비(모드 무관). reviewer-deep GO(embedded 회귀0·getAgents waiter 독립·순환 import 없음·mutation 3종 실효). vitest 35 green / tsc 0. **GUI 실측(데몬 모드 트리 갱신)은 보안 CDP 통과 후 잔여**.
- **✅ 별건: core `pty/`→`agent/` rename — 커밋 `b79a7a4`:** 폴더가 PTY 전용이 아니라 에이전트 코어 전반(Manager/Session/OutputCore/transport/backend)이라 이름 일치화. git mv 18파일(history 보존), `crate::pty::`→`crate::agent::`·`engram_dashboard_core::pty::`→`::agent::` 전수 치환. **실 PTY 항목(transport/pty.rs·PtyTransport/PtyEvent/PtyError)·src-tauri commands/pty.rs 는 이름 유지**(첫 마디 모듈 경로만). build/test(core58·protocol·daemon44+ign3)/clippy/fmt/격리 green. CLAUDE.md 모듈맵 참조 갱신.
- **✅ phase4-2 #1 GUI 실측 — 데몬 모드 트리 실시간 갱신 PASS(2026-06-15, 보안 CDP 예외 통과 후):** daemon 모드 자동연결(stale daemon.json 삭제 후 discover_daemon→WMI 자동 spawn, 새 PID/port 발행, conn=connected) 확인. cdp eval 로 트리 DOM 직접 관측 — 초기 "에이전트 없음"(count 0) → `spawnAgent` → "● Running"(count 1, 실시간 추가) → `killAgent` → "에이전트 없음"(count 0, 실시간 제거). `AgentListUpdated` WS 이벤트 → agentListCbs → eventBus → store → 트리 경로 E2E 동작 입증. 로직(vitest 35)에 더해 GUI 갱신까지 실측 완료 → **#1 이벤트 배선의 마지막 미확인 항목 종료**. (코드 변경 없는 검증 게이트, 인라인.)
- **✅ nit: `pid_alive` 부재/권한 구분 — 코더→reviewer-deep(GO)→QA(2026-06-15):** `core/agent/platform/process.rs` 의 `pid_alive`(Windows)가 `OpenProcess` 실패를 무조건 live(`Err(_)=>true`)로 봐 "프로세스 부재"도 살아있다고 오판하던 false-live 버그 수정. 순수 함수 2개 추출 — `win32_from_hresult(hr)=(hr as u32)&0xFFFF`(HRESULT 하위워드=win32 코드), `alive_from_open_error(code)`(87 INVALID_PARAMETER=dead / 5 ACCESS_DENIED=live / 그 외=보수적 live). 비대칭 보수 설계(확실한 부재 87만 dead, 나머지 false-live 유지 → 새 false-dead 0). 영향 경로: 옛 daemon.json(start_time 미상=0) + 죽은 PID fallback 한정(정상 daemon.json 영향 0). reviewer-deep 가 windows-rs 0.58→windows-result 0.2.0 소스 체인으로 HRESULT_FROM_WIN32(facility=7) 하위워드 추출 정확성 검증 + mutation 2종(`&0xFFFF` 제거·87↔5 스왑) 테스트 실효 확인, GO. 쟁점2(부재 코드를 87로만 좁힘) 주석 1줄 보강. cargo test 57 / fmt / clippy -D warnings green.
- **✅ nit: 데몬 재연결 후 status 재동기 — 조사+실측 결과 "갭 없음"(2026-06-15, 코드 변경 0):** 백엔드 계약 확인 — 데몬은 **모든 연결 직후** Hello + 현재 전체 목록(`AgentListUpdated`)을 push(`daemon/ws.rs:769`). 재연결도 같은 Auth→Hello 핸드셰이크라 자동 재수신. 클라(`daemonClient.ts:342`)는 이 push 를 영속 콜백(agentListCbs)으로 트리에 라우팅(재연결 무관). CDP 실측: spawn(count1) → `ws.close()` 강제드롭 → 상태추이 connected→reconnecting→connected → 재연결 후 count1·트리 "● Running" 무손실·무중복. → **추가 구현 불필요**(핸드셰이크 push + 영속 콜백으로 이미 충족).
- **✅ nit: 목록/스냅샷 조회 request_id echo — 코더→reviewer-deep(GO)→QA(2026-06-15):** 조회 응답의 "broadcast 편승 매칭"(다음 도착 메시지로 짝짓기) 제거. protocol: `ListAgents`/`ListProfiles` unit→`{request_id}`, 신규 전용 응답 variant `AgentList`/`ProfileList`, `Snapshot`+request_id, **PROTOCOL_VERSION 1→2**. daemon: 3개 조회 arm 을 전용 reply 로(broadcast emitter 무변경), GetSnapshot 중복 Ack 제거. client: waiter 배열 제거→sendCommand/pending(request_id) 통합, **broadcast(AgentListUpdated/ProfileListUpdated) 라우팅 유지(트리 실시간 갱신 보존)**. spawn/Created 패턴 확장(타사례 JSON-RPC/LSP/DAP 전부 echo 토큰+broadcast 구조분리). 게이트: protocol(26+golden5+ts_export1)·daemon44·core회귀0·vitest40·tsc0·clippy0·fmt. reviewer-deep 가 편승 차단·broadcast 보존·바인딩 드리프트0·GetSnapshot Ack제거 안전 확인, GO. **⚠️ phase4 게이트(비차단): PROTOCOL_VERSION 1→2 가 "신클라+살아있는 구데몬(v1)" → VersionMismatch 무한 'reconnecting' UX 갭을 실활성화**(행/데드락 아님, 현 영향0=데몬 opt-in). 데몬 운영 활성화 직전 VersionMismatch 를 reconnect 루프와 분리(종착 'down'+안내) 또는 graceful 교체 정책 필요. + sendCommand timeout 부재(기존 갭, phase4 안정화 retrofit).
- **데몬화 다음(phase4-2 잔여 + 그 뒤)**: ①**자동 부팅 daemon 모드 배선**(discover_daemon 자동, 현재 localStorage 수동 — 사용자 결정: **나중 UI 고도화 시 옵션 제공, 디폴트=daemon**). ②**#6 데몬 수명/콘솔 구현(ADR-0015 확정)**: windowless 데몬 spawn(`CREATE_NO_WINDOW`/`DETACHED_PROCESS`, 빈 콘솔 제거), 콘솔=detachable 디버그/명령 뷰어, 명시 `shutdown` 핸들(사람/LLM), ensure-on-open 정식 배선. ③**6-2 부팅 복원 구현(ADR-0008 보강)**: 3모드(자동/안함/물어보기) × per-profile auto_restore 플래그, 부팅 복원 1회성(기존 사다리가 가드, 카운터 불필요). **기본값 미정(사용자 결정 대기 — 추천 ③)**. ④**phase4 게이트**: PROTOCOL_VERSION VersionMismatch UX 분리(위 ⚠️), sendCommand timeout. ⑤**TODO(보류)**: **자동 재시작**(런타임 사망 에이전트 되살림 — backoff+최대횟수, 아직 사망 케이스 미관측, 코어 안정 후), **#3 truncated UI**(persist 데몬이라 로컬 드묾 → 원격/모바일 때 재논의), **#5 자동부팅 UI**(디폴트=daemon). 이어서 VT-framebuffer/roaming/predictive(모바일, 한참 뒤)·별건(ts-rs 바인딩 프론트 소비, AgentCommand→SpawnSpec 개명).
- **데몬 수명/콘솔/복원 정책(#6) — prior-art 조사 완료, ADR 후보 3건(사용자 방향 대기):** 타 도구(tmux/Zellij/Emacs/mosh/dtach) 전부 "PTY 쥔 프로세스 ↔ 뷰어/콘솔" 분리·런칭 터미널에서 데몬화 → 우리 "콘솔이 데몬 수명 소유" 버그는 무선례. 종료철학 2갈래(persist-until-kill[Zellij/Emacs] vs auto-exit-on-empty[tmux exit-empty], tmux 만 config 매트릭스). 추천=하이브리드: ①콘솔=detachable 뷰어 클라(데몬 spawn/kill 불가, Emacs `--alternate-editor=""` ensure-on-open 차용) ②복원=데몬 부팅 1회·per-profile auto_restore 구동·매번 안 물음·idempotent ③종료=persist 기본+opt-in auto-exit(0agent+0client+grace)+LLM `shutdown_daemon` 핸들. 선결(공통)=데몬 windowless spawn. ADR 후보: 콘솔=뷰어 / 종료정책 / (복원은 ADR-0008 보강 가능).
- ~~Step4b 원안~~(WS server: accept_hdr_async Origin allowlist→1s Auth frame→split→read_task(AgentCommand→manager)+단일 conn_tx write_task+연결 레지스트리 cleanup, WsOutputSink raw→encode_terminal_frame try_send, WsStatusSink). → Step5(WMI discovery) → Step6(격리 하네스). + 별건: 타입 드리프트 게이트(ts_export diff 비교, 권장 phase4). ①SubscribeAck 순서 보장 방식(데몬 단일 conn_tx[코어변경0] vs OutputCore SubscriptionSink[견고] — 권장 전자) ②seq u64 JS 표현(number 현재 vs bigint/string) ③port.json ACL 강도(보안 담당) ④송신 큐 모델(단일 conn_tx vs agent별+select! — 권장 후자) ⑤Tauri WebView2 실제 Origin 실측. 그 외 Step2(SpawnSpec 개명, serde 무영향=wire 안전)·Step4(daemon bin: mutex/port.json/WS server/auth/WsOutputSink/WsStatusSink)·Step5(WMI discovery)·Step6(하네스)는 결정 후 진행.

---

## 다음 (미진행)
- **[원칙→구현] LLM 제어 표면** — CLAUDE.md §5 신설(모든 메뉴가 LLM 제어 가능, LLM이 메인/사용자 UI는 서브, 손발/두뇌 분리). 현재 백엔드만 invoke로 제어되고 UI/레이아웃(분할·저장·트리 추가 등)은 프론트 전용. UI 액션을 LLM·사람이 같이 부르는 단일 control surface(command 버스)로 모으는 작업 필요. 새 UI 기능마다 제어 경로 동반.
- **[입주 1단계-b] UI 레이아웃/창 영속화** — **저장위치 결정 완료(D-7): 프론트 localStorage**(백엔드 아님). 다중창(창별 독립 layout+theme+좌표, 멀티모니터)·창 id별 키·Tauri JS `WebviewWindow`로 부팅 복원. 현 conf.json 정적 3창→동적 창 생성 신규 기능. **데몬화 뒤로 보류**(2026-06-14, 데몬 우선 결정). 상세: tracking.md D-7.
- **[입주 2단계] 에이전트 데몬화 (Rust 서버 + React 소켓 클라, tmux 모델)** — UI 재시작이 에이전트를 안 죽이게(서버가 세션 보유, 클라가 attach). **시점 결정: 에이전트 안정화(자동재시작·복원) 끝난 직후.** 공수 ~1~2주, `ptyApi.ts`/`eventBus`/`OutputSink`가 길목이라 facade swap(갈아엎기 X). 로컬 속도 영향 0(이미 직렬화 중, loopback 한 홉). 원격/모바일과 동일 인프라. **전제: "메시지 패싱·무상태 클라" 규율 유지**(공유메모리·동기 가정 금지)로 double-stabilization 방지 — facade 한 곳만 재검증.
- **[큰 것] 원격(WS) 프론트 = 모바일 제어** — 데몬화(2단계)의 자연 연장. 에이전트는 데스크톱(데몬)에서 돌고 폰은 원격 I/O 프론트로 attach. 인증/TLS 추가. 보안 1급.
- **[아이디어] 모드 시스템 (터미널/클로드/코덱스/api)** — 슬롯/에이전트의 "mode" 라벨이 [AgentCommand variant + transport + 기본 렌더러]를 묶어 고름. 터미널=Shell, 클로드·코덱스=콘솔(PtyTransport), api=ApiTransport(비-터미널, capability.output로 렌더러 분기). 모드 추가 = variant+backend(+api는 transport)+렌더러 하나. S10 추상화 위 UI 표현.
- **[아이디어] data-driven 우클릭 메뉴 (§5 구체 사례)** — SlotContextMenu를 데이터 트리(`MenuItem{label,children?,action?}`, 중첩 2·3단)로. 각 잎 항목=핸들(모드 spawn/분할/저장 등). 그 메뉴 config(JSON)를 LLM이 읽고(정보화)·수정(제어) → 사람 클릭과 LLM이 같은 핸들. "클로드에게 부탁해 메뉴 커스터마이징" = LLM이 메뉴 데이터 편집.
- **[✅ 해결] claude 콘솔 spawn Windows 실패** — `ClaudeBackend` program="claude"가 ConPTY/CreateProcessW로 확장자 없는 npm shim 못 띄우던 버그(error 193). 수정 완료(커밋 `adf80d7`): `backend/mod.rs::console_command`가 Windows에서 `cmd.exe /c <prog> …`로 래핑(claude.cmd shim 해석, claude 종료 시 cmd도 종료=수명 유지, JobObject 트리 kill), 비Windows 직접. claude/codex/gemini 공용 헬퍼. 스파이크(2026-06-12)로 발견(UI에 spawn 버튼 없고 테스트가 셸뿐이라 잠복) → 라이브로 실제 claude TUI spawn + `--session-id`/`--resume` 무손실 복원까지 실측 PASS. ※codex/gemini stub도 라우팅 시 console_command 채택 필요(현재 미적용).
- **codex/gemini CLI spike** — 실제 CLI 구독 후 플래그 확정 → `AgentCommand`에 Codex/Gemini variant 추가 + `backend_for` 라우팅 연결(현재 stub은 best-guess+미연결).
- **[게이트] 자동 재시작** — `restart_agent` 전용 태스크(사다리 resume→fresh→정지, backoff). 코어 안정 후.
- **실제 claude 복원 E2E** — headless는 shell만 실증. claude `--session-id`/`--resume` + `sessions/<pid>.json` PID 일치를 실제 claude로 실측(spike) 필요.
- 메시지 시스템(에이전트 간 통신) — 백엔드 추가 설계.
- Phase 3d (popup URL 전달 + monaco) + 프론트 상세(복원 배너 UX).
- `reference/` 정설 문서 집필 (시스템 안정화 후)
- **[정리] `pty/` 폴더명·구성 재고** — S10 후 `pty/`가 PTY 전용이 아니라 에이전트 코어 전반(AgentManager/AgentSession/OutputCore/transport/backend) 보유. 폴더명이 내용과 불일치(에이전트 공용 매니저가 pty/ 안). 다른 모듈 배치도 같이 점검(사용자 지적 2026-06-14, 트리 슬롯화 작업 끝난 뒤).
