# 이름·기록

> **상태:** 트리거 대기 · **정본 색인:** [`../todo.md`](../todo.md)

- **`epoch` 이름 교체 — 미룸.** 의미는 "순서 없는 화신 표딱지"인데 이름이 순서 카운터를 가리킨다. 실제 코드 식별자는 **885곳**(Rust 689 / 약 54파일 · TS 196 / 20파일)이고 무관한 것은 **18곳뿐**(`UNIX_EPOCH`, 0.9%) — 나머지 약 1000곳은 주석·산문이다(실측 2026-08-21. 옛 "600곳"과 "대부분 잡음"이라는 가정은 둘 다 틀렸다). **미루는 이유:** TypeScript는 속성 이름을 wire 키에서 떼어낼 수단이 없어 TS 196곳 중 **147곳이 `epoch`에 못 박힌다** — 나머지만 바꾸면 화신 방벽을 담은 바로 그 파일 안에서 어휘가 두 벌로 갈린다. 게다가 `epoch`는 결정 기록 말뭉치의 개념 이름이고 **파일명에 그 단어가 든 ADR 3건은 개명 불가**다(결정 기록이 테스트 이름을 포인터로 인용하는 곳도 있어 `docs/`를 같이 봐야 한다). ★재시도해도 직렬화(wire) 키는 `epoch`로 남긴다★ — 옛 빌드가 키를 못 찾으면 명부를 `.corrupt`로 밀어내고 빈 상태로 뜬다(ADR-0163 결정 5).
- **`epoch` 의미 오류(화신 표딱지를 순서 카운터·크기 비교 가능한 값으로 서술)가 이번 세션에서 고친 ADR 넷 밖에도 남아 있다 — 추적 포인터 재등록.** 판정 근거는 ADR-0163/ADR-0164, 여기 되풀이하지 않는다. **미루는 이유:** 이번 세션 수정 범위가 그 네 ADR에 한정됐고, 남은 잔존지는 미확정 ADR·wire 계약·크레이트 주석·테스트 이름·생성 바인딩·handoff까지 파일 성격이 흩어져 있어 한 번에 쓸어담을 작업이 아니다. ★**이 항목은 열려 있다**★ — 아래 하위 항목 중 참조 문서 몫만 2026-08-25에 닫혔고 나머지(ADR-0084 문구·TRD·messaging 주석·테스트 이름·생성 바인딩·handoff)는 그대로 미수정이다.
  - ~~★`docs/reference/architecture-overview.md` — 6곳(`:82`·`:197`·`:289`·`:521`·`:666`·`:684`).★~~ → ★**2026-08-25에 닫혔다**★. 그 문서 전면 재작성(브랜치 `v0.3.0/docs/architecture-overview`)이 여섯 자리를 전부 걷었다 — **순서 카운터** 서술(`+1`·`epoch↑`·`재spawn 카운터`)은 「화신마다 새로 뽑는 32비트 난수 표식 · 비교는 일치/불일치만」으로 갈렸고, 옛 `:82`의 **영속된다**는 서술은 「프로필에 안 실린다 — 읽기는 건너뛰고 쓰기는 `0` 자리채움」으로 뒤집혔다(ADR-0163 결정 4·5). 문서 어휘가 그 위에 얹혀 있던 것이 원래 문제라 개별 줄 교체가 아니라 문서 전체가 새 어휘로 다시 섰고, 용어 사전·핵심 불변식 절이 그 규약을 못박는다. ★**옛 줄 번호는 이제 무관한 내용을 가리킨다 — 그 포인터로 재확인하지 말 것**★.
  - **ADR-0084 — 도장 없음, 게다가 금지된 단언을 지시.** `:13`·`:15`·`:28`·`:30`이 존재하지 않는 `bump_epoch`를 필수 호출로 지목(실제 = `epoch_for_spawn`). `:9`·`:28`은 구독 deps에 그 값이 들어간다 주장. `:31`의 회귀 테스트 요구("새 값이 죽은 값보다 커야 한다")는 ADR-0163 결정 2가 금지한 비교이고, 실제 테스트(`crates/engram-dashboard-agent/tests/activation.rs:366-367`)는 차이만 잰다. `docs/decisions/README.md:134`도 확정 표기·도장 없음.
  - **`docs/process/S17-llm-control-surface/spec/trd.md:76`** — wire 계약이 LLM 호출자에게 크기 비교를 지시(`재활성화마다 증가, 신선도 판별용`).
  - **messaging 크레이트 주석 3곳** — `crates/engram-dashboard-messaging/src/ledger.rs:196` · `service.rs:2410` · `mailbox.rs:418`. 전부 `// ADR-` 탐색 표면 위. `mailbox.rs:418`은 이중 오류 — 그 분기가 ADR-0111 결정 6으로 이미 제거됨(`service.rs:1508-1511`).
  - **테스트 이름 2건이 자기 본문과 모순** — `src/api/protocolClient.test.ts:1350`(같은 파일 `:472`는 이미 교정) · `crates/engram-dashboard-agent/tests/activation.rs:369` `reactivate_after_kill_bumps_epoch`(바로 위 주석이 부정하는 것을 이름이 주장). 후자는 `docs/decisions/0127-턴-관측-명단-코어-승격-분류는-backend-seam-뒤-데몬은-중계만.md:33`가 **이름으로 인용**해 개명 시 그 포인터도 끌려간다.
  - **`src-tauri/bindings/` — 8개 중 6개에 기존 drift**(이번 세션 손으로 고친 1개 제외): `LayoutNode.ts:5-11` 고아 주석(뒤에 대응하는 Rust doc 없음) · `ViewMeta.ts:4` 다른 이벤트명 · `View.ts`·`ViewSnapshot.ts`·`SlotSpatial.ts`. ★게이트 갭 쪽은 2026-08-24에 닫혔다(「확인만 된 것」의 그 항목 참조) — 여기 적힌 것은 그때 **관측된 잔존 규모**다★. ★**그 뒤 그 8개를 재생성한 결과를 이 목록과 대조하지 않았다**★ — 재생성이 이 drift를 얼마나 걷어냈는지는 미확인이니, 착수 전에 현재 파일부터 다시 볼 것.
  - **`.claude/handoff/latest.md`** — `:6` 이미 끝난 일을 다음 세션에 하라고 지시, `:95` "참조 600곳"(이번 세션 실측 885). 다음 handoff로 교체될 것이라 우선순위 낮음.
- **ADR-0058의 전제가 거짓이 됐다 — ★결정은 유효하다★**(지속 근거는 그 문서 `:22`가 이미 갖고 있고 오늘도 참이다). 데몬이 띄우는 것은 셸이 아니라 **claude(`StreamJson`)**다(커밋 `27cb74a`, 2026-08-20). 같은 말을 하던 코드 주석 셋은 2026-08-24에 실제와 맞췄다. **지금 안 하는 이유:** ADR 개정·제목 변경은 사용자 결정이라 세션이 도장을 못 찍는다.
  - **전제를 적은 자리** = `:1`(제목 — 파일명도 같다)·`:7`·`:10`(★결정 본문★)·`:14` + 인덱스 `docs/decisions/README.md:108`(제목 사본). **낡은 앵커** = `:4`·`:17`·`:23`(`connection_core.rs:852` · `commands/layout.rs:346-374`·`:366-374` — 가드는 `src-tauri/src/layout/apply.rs`로 옮겼다). ★`:17` **본문은 참이다** — 낡은 것은 그 안의 앵커뿐이니 문장째 지우지 말 것★.
- **`CLAUDE.md` 「프론트 구조」의 「그런 모듈은 지금 없다」가 반증됐다** — `src/theme/uiSettings.ts`가 `@tauri-apps/api/core`의 `invoke`를 직접 부르고, 그 파일 헤더(`:6`)가 스스로 그 조항의 예외라 자인한다. **지금 안 하는 이유:** load-bearing 문서 문장이라 문장을 고칠지 예외를 명시할지가 사용자 결정이다(S20.15가 Step 4 문서 패스로 미뤘고, 그 패스는 같이 미뤘던 「등록 주체」 몫만 닫았다).
- **ADR 정합성 권고 2건** — ADR-0016의 옛 부분 폐기에 역방향 링크가 없다 · `src-tauri/src/tray/mod.rs`의 `// ADR-0027` 앵커가 폐기된 결정을 가리킨다. 둘 다 adr 스킬 lint가 권고로 잡은 것이고 오류는 0이다(실측 2026-08-21).
