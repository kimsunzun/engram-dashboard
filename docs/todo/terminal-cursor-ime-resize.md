# 터미널 커서·한글 조합·초기 치수 가드

> **상태:** 소유됨(브랜치 `v0.3.2/fix/terminal-cursor-ime-resize`) · **정본 색인:** [`../todo.md`](../todo.md)

평평한 분할 렌더러 머지 뒤 사용자 GUI 확인(2026-09-25)에서 갈라낸 것 + 그 렌더러 라운드가 「후속」으로 남긴 선재 결함 하나. 기록 = `../process/step-log.md` 의 평평한 분할 렌더러 구현 항목.

- **구독 직후 치수 보고에 숨김 가드가 없다** — `src/components/slot/TerminalSlot.tsx:304-305` 는 구독 직후 `fit()` + `resizePty` 를 그대로 보내는데, 나머지 세 송신 경로(`:99-101` · `:285` · `:355`)는 숨김·측정 불가 상태에서 보내지 않는다. 업계 규칙 = 잴 수 없는 치수는 보고하지 않는다(조사 = `../research/hidden-tab-terminal-subscription-survey-2026-09-25.md`). 재현은 안 했다(코드 판독).
- **터미널 커서가 너무 굵다** — 기본 블록 커서다. **스타일(막대·밑줄·블록 유지 등)은 사용자 선택 대기.**
- **한글 조합 표시가 깨진다** — 조합 중 커서가 사라지고 조합 글자 위에 작은 파란 선이 보인다. **원인 미조사.**

**연관:** 첫 항목은 [`terminal-sizing.md`](./terminal-sizing.md) 의 「전파 지점 네 곳」 중 초기 마운트 경로다 — 가드만 맞추는 수정이고 네 곳을 한 곳으로 접는 작업은 그쪽에 남는다.
