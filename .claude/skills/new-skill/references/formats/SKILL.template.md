<!-- 이 템플릿은 research/review/qa/adr/new-skill에서 공통 패턴을 추출한 것이다.
     기존 스킬이 변경되면 이 파일도 함께 갱신해야 한다 — 양쪽 중 하나만 갱신하면 rot한다.
     정본 비교 대상: .claude/skills/{research,review,qa,adr}/SKILL.md -->
---
name: <스킬명>
description: <한 줄 목적 + 트리거 문장 — 트리거 포함 한 줄로 작성>
---

# <스킬명>

**실행 전 `references/flow.md`를 반드시 Read 한다 — 안 읽고 진행 금지.**

한 단락 목적 요약 — "이 스킬이 무엇을 하는가"만. 절차·예시 금지(flow.md에 있으므로).

> Read 강제 문구는 **H1 직후, 첫 `##` 이전**에 둔다(lint 위치 검사 대상). `## 트리거` 안에 넣지 않는다.

## 핵심 설계 (불변)

이 스킬의 설계 원칙 2~4개. **왜 이렇게 설계됐는지**를 담는다 — 단순 기능 나열이 아니라 트레이드오프·판단 기준.

- **원칙 1** — 설명 (예: "단계 순서 불변 — 섞으면 flow.md가 껍데기가 된다")
- **원칙 2** — 설명
- **원칙 3** — 설명 (선택)

## <1차 축> 표 (강도/오퍼레이션/단계가 있을 때만)

> 1차 축이 없는 단일 흐름 스킬이면 이 섹션 생략.

| <축 값> | 언제 | 깊이/범위 |
|---|---|---|
| <값 1> | <상황> | <범위> |
| <값 2> | <상황> | <범위> |

## 트리거

`/<스킬명> [인자...]` — <한 줄 설명>. (Read 강제 문구는 위 H1 직후에 있다.)

## 프로젝트 바인딩 (bindings가 있을 때만)

> bindings가 없는 스킬이면 이 섹션 생략.

프로젝트별 특화(경로·명령·체크리스트)는 `references/bindings/<project>.md`.

## 자기개선 피드백 (usage-log 권장이 있으면 "· 사용 로그" 추가)

> 섹션명: usage-log.md 권장이 있는 스킬 → `## 자기개선 피드백 · 사용 로그`
>         없는 스킬 → `## 자기개선 피드백`
> 위 태그 줄은 생성 시 삭제한다.

결함·개선점은 그 자리서 고치지 말고 작업 종료 후 `feedback.md`에 누적. **검증 상태(근거 강도)도 SKILL.md에 `## ⚠️ 검증 상태` 절로 두지 않고 `feedback.md`의 "검증 상태" 항목에 정직히 기록한다**(단단함 ↔ 약함/미검증 쌍으로, 과장 금지). 전체 규약 = `../_shared/self-improvement-feedback.md` 참조.
