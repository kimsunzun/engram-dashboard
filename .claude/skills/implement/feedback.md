# implement — 개선 히스토리

이 스킬을 쓰다 발견한 결함·개선점을 누적한다(덮어쓰기 금지). 반영은 사용자 승인 하에. 규약 = `SKILL.md` "자기개선 피드백" 절.

## 이력

| 날짜 | 발견 | 상태 |
|---|---|---|
| 2026-07-03 | **갭② 게이트 호출 방식**: `/review`·`/qa`를 스킬-내-스킬 호출이 아니라 메인이 그 스킬의 렌즈·강도 규칙을 직접 적용해 스폰으로 수행(현 환경 제약). 동작은 등가나 flow 문구("`/review code <강도>`로 넘긴다")와 실행 방식에 갭. | 미반영 (문구 정밀화 후보) |
| 2026-07-03 | **재작성판 standard 첫 완주 (과업 = 골든셋 백테스트 하네스 v1 신설)**: 라우팅 한 줄 명시 → 코더(복잡) 스폰 → review full 2라운드(FIX 총 15건 적출·반영, 루프 카운터 2/2 소진) → qa 등가 게이트(테스트 56/56·실호출 2/2) → 커밋 준비 → 자율 모드 커밋 보고. 관찰 3건: ① **리뷰 verdict 대립(doc-aware FIX vs blind BLOCK)을 메인이 취합 판단으로 FIX 처리하고 자율 진행** — findings가 모순이 아니라 상호보완이었고 BLOCK이 항목 수(>5) 기인이라 판단했으나, flow 문면("불일치→사용자")과 갭. "라벨 대립 vs findings 모순"의 구분 기준 명문화 후보 + 이번 판단의 사후 비준 필요. ② 대립 finding 1건(Windows 공백 경로)을 **실증 회귀 테스트로 판가름**(blind 옳았음) — "실증 가능하면 테스트가 tiebreak" 패턴 후보. ③ 갭①(qa 바인딩 부재 → 메인 등가 번역) skill-lab에서 재발 — 재확인. | 검증 (standard 경로 실측 1회) |
| 2026-07-03 | **검증 상태** (SKILL.md ⚠️절에서 이동 — 방침 C): *실측 1회(위 dogfood 항목).* 이전엔 실측 0회였고, dogfood(simple 강도 · lint 스크립트 신설 과업) 완주로 파이프라인 전 구간 작동을 확인했다. *근거 있는 것:* 이 스킬은 수동 공정(engram-dashboard "구현 실행 규약" — 코더→리뷰어→QA 서브에이전트 분리, 검증돼 운영 중)을 오케스트레이션으로 재구성한 것 + 선례 경고 방어(무한 리뷰 루프 상한 2회 · 재귀 폭발 방어 = 코더는 서브에이전트를 못 낳음 · 모델 다운그레이드 방어 = 스폰 시 모델 명시 강제 · 리뷰 형식화 방어 = 생산자≠리뷰어 fresh 분리). *미검(정직):* 이 오케스트레이션이 수동 공정 대비 결함 검출·품질을 얼마나 올리는지는 단발 dogfood 1회로는 정량 미검 — "근거 있는 가설"로 취급(공정 자체는 검증됐으나 자동화 래퍼 실측은 1회뿐). | 기록 (검증 상태 정본) |
| 2026-07-07 | **4렌즈 감사 적출:** ① 실행자 티어 자기점검 가드 부재(opus급 하한이 문서 밖 지식으로만 존재) ② §0 인라인 예외·simple 자기분류가 무게이트(자기 선언만으로 파이프라인 전체 우회 가능). 실전에서 실제 뚫리는지 관측 후 반영 판단(선제 장황화 금지). | 미반영 |
| 2026-07-26 | **신설 문구 fresh 이해도 프로브 PASS(수확 batch 1 실측):** 신선한 컨텍스트(경량 슬롯)가 flow만 읽고 신설 규칙을 정확 인용 — §2 금지 bullet의 태스크 리스트 불가침 3분해 재현. 가시성·이해도 검증만이며 실행 준수는 다음 실주행이 실측(정직 구분). | 기록 (실측) |
| 2026-07-26 | **standard 실주행 2회차(adr.mjs 스탬프) — 개정 루프 카운터 첫 실측:** 리뷰 5라운드 수렴, 매 라운드 직전 findings 전부 닫혀 연속 카운터 리셋 반복(최대 1), **총 재수정 하드캡 5/5 정확히 소진**하고 종결. 07-12 개정(연속 카운터+하드캡)이 의도대로 동작 — 구 총량제(2회)였다면 정상 수렴을 3라운드에서 강제 중단했을 것. 관측: 수정이 새 표면을 만드는 라운드(blanking 접근)에서 하드캡 근접 시 **처방적 재지시**(접근을 메인이 지정)가 수렴을 끊어냈다 — 에스컬레이션 직전 유효 수단 후보. 테스트 브리핑(07-23 신설) 시작·끝 실측 완료. | 기록 (실측) |
| 2026-07-31 | **standard 실주행(engram, 테스트 보강 과업) — 문구-only 라운드가 카운터를 먹는다:** 리뷰 4라운드 수렴·최종 양쪽 PASS. **코드는 R1에서 사실상 확정**됐고 R2~R4는 전부 "주석이 코드가 증명 못 하는 것을 주장" 부류였는데, 이 문구 교정 라운드가 코드 수정 라운드와 **같은 하드캡(5회)을 소진**해 4/5까지 갔다. 문구-only 라운드의 계수 분리(또는 별도 상한) 검토 여지. 부수 관측: R2 지시서에 **메인이 직접 박은 인과 서술이 틀렸고 blind 리뷰어가 잡았다** — 지시서도 검증 대상이라는 실증(지시서 문구를 리뷰 면제로 넘기지 말 것). | 미반영 (실측) |
| 2026-07-31 | **코더 금지 목록에 결정 기록(ADR)이 없다 — 이번엔 운이 좋았다(engram 슬라이스 C 실주행):** 지시서가 ADR-0112를 "함께 갱신 대상"으로 지목했더니 코더가 ADR 두 건의 **헤더를 손으로 편집**했다. 결과물 자체는 옳았다(양방향 부분폐기 도장만, 결정 본문 무변경 — `/adr supersede --mode partial`과 동일 조치). 문제는 **§2 금지 목록이 태스크 리스트·커밋·서브에이전트만 막고 결정 기록은 안 막는다**는 것 — 코더가 ADR *본문*을 고쳐 결정을 바꿨어도 걸리는 가드가 없었다. ADR은 덮어쓰지 않고 누적하는 문서라 그 편집 권한은 호출자(메인/사용자) 소관이다. 후보: §2 금지에 "결정 기록(ADR) 불가침 — 필요한 도장은 호출자에게 보고" 추가, 그리고 호출자가 ADR 조치를 **명시 인가**할 때만 허용. 부수 관측: 메인 지시서의 "함께 갱신 대상" 같은 모호 문구가 코더에게 편집 재량으로 읽힌다(07-31 앞 행 "지시서도 검증 대상"과 같은 계열). | 미반영 (실측) |
| 2026-07-31 | **"기존 단언 바꾸면 멈추고 보고"가 결정 번복 구현에서 작동 불가(engram 슬라이스 C):** 지시서 골격대로 "기존 단언 수정이 필요해 보이면 stop and report"를 박았는데, ADR이 **정면으로 뒤집는 계약**을 단언하던 테스트가 하나 더 있어 멈추면 그 슬라이스가 **컴파일조차 안 되는** 상태였다. 코더는 진행 후 보고를 택했고 처리도 옳았다(단언 약화 대신 그 계약을 이제 소유하는 어휘로 이사 = 토큰만 교체·단언 보존). 결함 = §2/TESTS 문구가 **약화(weakening)와 이전(relocation)을 구분하지 않아** 정당한 이전에도 멈추라고 지시하게 되고, 멈출 수 없으면 코더가 지시를 어길 수밖에 없다. 후보: "단언 약화·삭제·반전은 stop / 결정 번복의 필연적 귀결인 계약 이전은 진행 + 명시 보고"로 분기. 부수: 메인이 범위를 과소 추정(커널 2파일+spec 예상 → 실제 11파일 — 프로젝트 spec이 프라이밍 문서 갱신을 슬라이스 필수 산출물로 명시하고 있었고 코더가 찾아냈다) → §0 컨텍스트 준비에 "프로젝트 spec이 코드 밖 산출물을 요구하는지 확인" 한 줄 후보. | 미반영 (실측) |
| 2026-08-04 | **🔴 내부 구현 선택을 사용자 결정으로 올리는 것을 막는 가드가 없다 — 사용자가 두 번 지적(engram dashboard ADR-0128 실주행):** review FIX 취합 후 "타입 판별자 도입 vs 테스트 추가"(순수 코드 구조)를 메인이 선택지로 사용자에게 올렸다. 소비처 CLAUDE.md §사용자 협업 방식이 *"안 드러나는 순수 내부 구현은 메인이 정하되 반드시 보고 — 결정 떠넘기기도, 무보고 독단도 아님"* 이라고 이미 규정하는데도 발생했고, 사용자 반응은 *"지금 뭐하는거야 … 자꾸 뭔소리가 나오는거야"* 였다(대화 3턴 낭비 + 사용자가 스스로 "복잡하게 가지 말라"는 일반 원칙을 다시 세워야 했다). **원인 추정:** flow §5의 mode-aware 절이 *커밋* 처리만 다루고, **게이트 findings를 취합한 뒤 "무엇이 사용자 결정이고 무엇이 메인 결정인가"를 가르는 절이 없다**. 후보 = §3 판정 처리(FIX 항목 반영)에 "findings 중 **동작·정책·데이터 위치**가 걸린 것만 사용자에게, 코드 구조·자료구조·테스트 전략은 메인이 정하고 보고" 한 줄. 부수: 사용자가 이후 준 일반 원칙(*"처음부터 완벽할 필요 없다 — 기능 추가되면 또 리팩토링, 지금 복잡하게 나가면 수정이 더 힘들다"*)이 프로젝트 CLAUDE.md §아키텍처 원칙 0("저위험+장기 → over-engineering 허용")과 **국소적으로 충돌**했고 사용자 판단이 이겼다 — 이 우선순위(사용자 당일 지시 > 프로젝트 상시 원칙)도 어딘가 명시될 후보. | 미반영 (실측 — 사용자 2회 지적) |
| 2026-08-18 | **FIX 라운드가 작아질수록 게이트 순서 규약과 비용이 충돌하는데 골격에 판단 기준이 없다(engram-dashboard, `/implement critical` 5라운드 실주행):** flow §4는 fix 라운드에서 코드가 조금이라도 바뀌면 `/review code`를 다시 돌리라 못박고, 강도축은 에스컬레이션 전용(하향 금지)이다 — 기본값으로는 옳다. **관측된 마찰:** 4라운드는 필드 하나의 스코프 + 주석만 바꿨고 5라운드는 주석 앵커 6개 추가 + 문장 하나 재서술뿐이었다. 이런 변경에 `deep`(리뷰어 3인) 형태를 통째로 다시 돌리는 건 비례가 안 맞는데, 골격이 사다리 중간을 하나도 허가하지 않아 이 세션은 그 라운드를 건너뛰고 라벨만 붙였다. **라벨이 샜다:** 이후 작성한 ADR이 "3라운드, 최종 PASS"라 적었는데 실제로 출하된 코드는 리뷰어를 한 번도 마주치지 않은 상태였다. `/review doc`을 그 ADR에 돌려 이 과장을 잡았고, 실제 갭을 메우려 종결 2인 리뷰 라운드를 별도로 돌렸다. 즉 스킵은 복구 가능했지만 순전히 운이었다 — 골격 안 어떤 장치도 이걸 표면화하지 못했을 것이다. **후보(기록만, 구현 금지):** 골격이 fix 라운드의 정당한 형태로 **델타 스코프 재리뷰**(같은 강도, 대상을 fix 델타로 좁히고 이전 findings를 첨부)를 명명하면 "건너뛰고 라벨만"이 유일한 저비용 선택지가 되는 걸 막을 수 있다. 강도 의미론 변경이라 owner 결정 필요. | 미반영 (실측) |

## 아카이브 (반영·기각 완료)

반영·기각이 끝난 행을 접어 둔 곳이다. append-only 규약상 지우지 않는다 — 같은 제안이 재등장할 때 이력이 근거다.

| 날짜 | 발견 | 상태 |
|---|---|---|
| 2026-07-03 | **dogfood 1회 완주** (simple 강도 · 과업 = skill-lab `scripts/lint-skill.mjs` 신설): 파이프라인 전 구간 작동 — 라우팅 한 줄 명시 → 코더(단순) 스폰 → review light FIX 5건 적출 → 재지시 1회(카운터 1/2) → 재리뷰 PASS → qa quick → 커밋 준비. 루프 상한 2회는 충분했음(1회로 닫힘). | 검증 완료 (배포 근거) |
| 2026-07-03 | **갭① qa 바인딩 부재 프로젝트**: 대상(skill-lab)에 `/qa` 바인딩이 없어 메인이 quick 정의를 번역 실행(node --test + 실대상 실행). flow §4는 "실명령은 qa 바인딩 정본"만 말하고 **바인딩 없는 프로젝트의 처리를 미규정**. | 반영 (2026-07-03 — flow §4에 "바인딩 부재 = 등가 명령 도출 + 명시 보고" 추가, 포괄 위임) |
| 2026-07-03 | **갭③ lint 스펙 해석 대기**: 코드블록·툴 식별자(`mcp__codex__codex` 등) 안 모델명 토큰도 위반으로 볼지 — v1은 엄격(스펙 문면대로). research 스킬에서 19건 검출됐는데 상당수가 이 부류. | 해소 (2026-07-26 사용자 결정 — 모델명 유지: 측정·실측 기록과 실행 리터럴은 위반 아님. lint 면제 규칙 반영 = 후속 작업) |
| 2026-07-06 | **TDD 억지 테스트 방지 탈출구 추가(사용자 결정)** — 테스트 기반 없는/자동 테스트가 구조적으로 안 맞는 환경(언리얼 에디터·실기기·체감 의존 등)에서 게이트가 테스트를 강요해 쓰레기 테스트를 양산하는 실패 모드 차단. 코더는 대신 수동 검증 절차를 반환하고 게이트는 그것을 검증물로 검사(flow §2 TDD 항목). 프로젝트별 세부 형태는 그 프로젝트 qa 바인딩이 정의(예: 언리얼 = 빌드 컴파일 자동 + 플레이테스트 체크리스트는 사용자 실측). 관련 장기 후보: 바인딩 남용(전면 면제 선언)의 기계 방어는 qa·review classbased(SEALED/HOLE)화 시 확보 — implement는 자체 바인딩 없는 설계라 classbased 대상 아님. | 반영 |
| 2026-07-07 | **공용 규칙 정합 정리(사용자 결정 — 승격 대신 뜻 통일):** §5 mode-aware에 모드 판정 근거 = 사용자 발화뿐(ADR-0006, review sealed 정본과 동일 구속 — implement엔 SEALED 마커 규약이 없어 prose로) 추가. 감사에서 이 구속 누락 적출. | 반영 (2026-07-07) |
| 2026-07-07 | **문구 담백화(사용자 지시 — 설계 DNA "문구 = 담백"):** SKILL.md 정체성 재서술 2건·강도 라우팅 중복 문장 삭제, 자체 바인딩 금지 이유 압축. flow §3·§5의 제목이 이미 말한 이유 재서술 4건 삭제. 의미·정량 불변. | 반영 (2026-07-07) |
| 2026-07-07 | **최종 보고 피드백 한 줄 의무(사용자 결정):** flow 최종/결과 보고 절에 "피드백: 없음"도 보고하는 한 줄 의무 추가(파일엔 발견 시만 — 조용한 스킵 관측). 규약 정본 = _shared/self-improvement-feedback.md. 게이트 = review doc full(Opus PASS · Codex FIX 반영: 축약 + "최종 보고" 통일) + qa 등가 실행 PASS. | 반영 (2026-07-07) |
| 2026-07-07 | **4렌즈 감사 반영(사용자 위임):** SKILL.md §0-1 데드 포인터 수정 + `_shared` 낡은 캐비앗 삭제 / flow §3 "simple→light = 단일 family" 명시(오독 차단) / §4 qa PARTIAL 분기 신설(PASS 아님 — 채워 재실행 또는 에스컬레이션, 커밋 진행 금지) / §5 열화 라벨 전파 규칙(자식 게이트 라벨 요약 세탁 금지). | 반영 (2026-07-07) |
| 2026-07-12 | **코더 스폰 dead letter 수정(review trd full 적출 — headless dogfood):** flow §2의 `subagent_type: general-purpose` 하드코딩이 사전의 코더(복잡)=worker-senior 프리셋 바인딩과 모순(프리셋이 코더 경로에서 영원히 미사용) → 호출 바인딩을 사전 "호출" 열 참조로 교체(§0 불변 문구 포함, live+staging). | 반영 (2026-07-12) |
| 2026-07-12 | **루프 상한 개정(사용자 결정):** 총량제(재수정 총 2회 + 승격 하드캡 3회)가 매 라운드 직전 findings를 전부 닫는 정상 수렴(실전 i18n 커밋 1: 6→3→1건)까지 막음 → 연속 미해소 카운터로 교체: 직전 findings 전부 닫으면 리셋, 못 닫거나 reopen이면 +1(상한 2회) + 총 재수정 하드캡 5회(무의미 토큰 소모 방어). flow §3·§4·가드레일 + SKILL.md 동기 수정(live+staging). | 반영 (2026-07-12 · 게이트 미실시 — 사용자 지시로 규칙만 반영, 다음 /implement 실주행이 실측) |
| 2026-07-20 | **코더가 공유 태스크 리스트 임의 삭제(engram S17 follow-up):** worker-senior 코더가 지시서 범위 밖인 메인의 태스크 리스트에서 "stale로 보인다"며 태스크 전체를 삭제(TaskUpdate deleted) — 다음 작업 항목이 날아가 메인이 복구. flow §2 지시서 골격 "금지"에 "태스크 리스트 불가침(생성·삭제·상태 변경 금지 — 메인 소관)" 추가 후보. | 반영 (2026-07-25 — §2 금지 목록에 추가. 게이트 미실시 — 사용자 직접 지시·경미 문구) |
| 2026-07-23 | **테스트 브리핑 시작·끝 추가(사용자 결정 — CL 시스템 대비):** 코더 스폰 직전 착수 브리핑(구현 접근 + 테스트 계획, 수동 검증 경로 포함) + 종결 보고에 테스트 결과 브리핑. 비차단(보고만, 승인 게이트 아님). 브리핑 눈높이(추상화 레벨)는 고정하지 않음 — 실사용 관측 후 조정(사용자 결정, 게이트 후 눈높이 제약·목적 문구 제거). SKILL.md §핵심 설계 불릿 1 + flow §2·§5 불릿 각 1. 게이트 = review doc full(둘 다 FIX → 수용 반영: 테스트 상존 가정↔수동 검증 fallback 모순 해소·중복 압축 / 기각 1: §1 라우팅 한 줄과 브리핑 병합 — 계약 상이·비용 무시 가능) + qa 등가(절대경로 0·diff 의도분만) PASS. | 반영 (2026-07-23 · 미실측 — 다음 /implement 실주행이 실측) |
| 2026-07-31 | **§2 지시서 골격에 주석 규율 항목 신설(clean-comment 배포 동승 — 사용자 승인):** 코더 지시서에 clean-comment 소비 계약 주입 한 줄 추가. 미설치 환경 = 생략+보고(데드 포인터 방지). steady-state 실소비는 다음 /implement 실주행이 첫 실측. | 반영 |
| 2026-08-19 | **★코더 지시서에 「자기 델타 자체 검토」가 없어 리뷰 회차가 늘어난다 — 4회 연속 재현(engram-dashboard-wt2, 제어 라우트 중계 critical)★:** 라운드마다 **직전 라운드의 수정이 새 결함을 만들었고**, 그것을 다음 리뷰 회차가 잡았다. 실측 사슬: R1 수정(자리 누출)이 자원 셋 중 **하나만** 치워 R2가 BLOCK → R2 수정이 **로그 위조 구멍**을 열어 R3가 적출(인증된 에이전트가 `\n`으로 가짜 로그 줄 위조, 우편 거절 줄은 이름 검증 *전*이라 완전 임의값) → R3가 넣은 이중 수거기 단언이 **자물쇠를 쥔 채**라 터지면 자기가 지킬 표를 오염시켜 R4가 적출. 즉 「고친 코드」가 리뷰 안 된 신규 표면인데 골격이 그것을 아무에게도 안 물린다. **R4 지시서에 처음으로 「반환 전 자기 델타를 적대적으로 검토하고 그 결과를 반환에 필수 항목으로 실어라」를 넣었더니 즉시 자기 결함 3건을 스스로 잡았다** — 어긋난 조립 2건(`priming_smoke`·`roundtrip_smoke`가 실 슬롯 + `without_commands()` 조합이라 그 bin의 `/control/call`이 `agent.*`를 조용히 「모르는 이름」으로 처리) + **자기가 쓴 테스트가 무효**(위조 이름이 이름순 정렬에서 잘리는 쪽에 들어가 손질기를 걷어내도 초록 — 실측). 그 셋은 넣지 않았으면 R5 비용으로 나왔을 것이다. **처방 후보:** flow §2 지시서 골격에 「자체 검토(self-audit) — 반환 전 자기 변경분을 적대 렌즈로 훑고 결과를 반환 필수 항목으로」 한 줄. 반환 형식 항목에도 그 칸을 신설(없으면 코더가 「했다」로만 답한다). **비용 축 관측:** 이번 과업의 최대 비용은 리뷰 회차였다(리뷰어 10 인스턴스 · 코더 4라운드 · 코더 라운드당 약 24만~37만 토큰). 회차를 하나 줄이면 리뷰어 2~3 + 코더 1이 절약된다. | 미반영 |
| 2026-08-19 | **리뷰어 대립 시 「구성 증명 > 부재 증명」 판정 규칙이 골격에 없다(같은 실행):** flow §3이 대립을 사용자 백스톱으로 보내는데, 이번엔 **판단 대립이 아니라 한쪽의 커버리지 누락**이었다 — 수거기 비공개화를 두고 doc-aware 렌즈는 "위험 제거됨"(비공개 동사를 못 부른다까지만 확인), blind 렌즈는 **공개 생성자 + 공개 접근자로 재구성하는 코드 한 줄**을 제시했다. blind가 옳았다(코더가 재현·수정 확인). 메인이 「구성 가능한 반례를 낸 쪽」을 채택해 사용자 백스톱을 쓰지 않았고 그 판단을 사용자에게 보고했다. **처방 후보:** §4 취합 절에 "한쪽이 구성 가능한 반례(코드·시퀀스)를 내고 다른 쪽이 부재 증명뿐이면 정면 대립이 아니다 — 반례 쪽을 채택하고 채택 사실을 보고한다" 한 줄. 이게 없으면 세션마다 재발명하거나, 실질 결함을 사용자 판단으로 떠넘긴다. | 미반영 |
