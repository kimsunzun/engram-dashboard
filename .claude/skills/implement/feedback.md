# implement — 개선 히스토리

이 스킬을 쓰다 발견한 결함·개선점을 누적한다(덮어쓰기 금지). 반영은 사용자 승인 하에. 규약 = `SKILL.md` "자기개선 피드백" 절.

## 이력

| 날짜 | 발견 | 상태 |
|---|---|---|
| 2026-07-03 | **dogfood 1회 완주** (simple 강도 · 과업 = skill-lab `scripts/lint-skill.mjs` 신설): 파이프라인 전 구간 작동 — 라우팅 한 줄 명시 → 코더(단순) 스폰 → review light FIX 5건 적출 → 재지시 1회(카운터 1/2) → 재리뷰 PASS → qa quick → 커밋 준비. 루프 상한 2회는 충분했음(1회로 닫힘). | 검증 완료 (배포 근거) |
| 2026-07-03 | **갭① qa 바인딩 부재 프로젝트**: 대상(skill-lab)에 `/qa` 바인딩이 없어 메인이 quick 정의를 번역 실행(node --test + 실대상 실행). flow §4는 "실명령은 qa 바인딩 정본"만 말하고 **바인딩 없는 프로젝트의 처리를 미규정**. | 반영 (2026-07-03 — flow §4에 "바인딩 부재 = 등가 명령 도출 + 명시 보고" 추가, 포괄 위임) |
| 2026-07-03 | **갭② 게이트 호출 방식**: `/review`·`/qa`를 스킬-내-스킬 호출이 아니라 메인이 그 스킬의 렌즈·강도 규칙을 직접 적용해 스폰으로 수행(현 환경 제약). 동작은 등가나 flow 문구("`/review code <강도>`로 넘긴다")와 실행 방식에 갭. | 미반영 (문구 정밀화 후보) |
| 2026-07-03 | **갭③ lint 스펙 해석 대기**: 코드블록·툴 식별자(`mcp__codex__codex` 등) 안 모델명 토큰도 위반으로 볼지 — v1은 엄격(스펙 문면대로). research 스킬에서 19건 검출됐는데 상당수가 이 부류. | 사용자 결정 대기 |
| 2026-07-03 | **재작성판 standard 첫 완주 (과업 = 골든셋 백테스트 하네스 v1 신설)**: 라우팅 한 줄 명시 → 코더(복잡) 스폰 → review full 2라운드(FIX 총 15건 적출·반영, 루프 카운터 2/2 소진) → qa 등가 게이트(테스트 56/56·실호출 2/2) → 커밋 준비 → 자율 모드 커밋 보고. 관찰 3건: ① **리뷰 verdict 대립(doc-aware FIX vs blind BLOCK)을 메인이 취합 판단으로 FIX 처리하고 자율 진행** — findings가 모순이 아니라 상호보완이었고 BLOCK이 항목 수(>5) 기인이라 판단했으나, flow 문면("불일치→사용자")과 갭. "라벨 대립 vs findings 모순"의 구분 기준 명문화 후보 + 이번 판단의 사후 비준 필요. ② 대립 finding 1건(Windows 공백 경로)을 **실증 회귀 테스트로 판가름**(blind 옳았음) — "실증 가능하면 테스트가 tiebreak" 패턴 후보. ③ 갭①(qa 바인딩 부재 → 메인 등가 번역) skill-lab에서 재발 — 재확인. | 검증 (standard 경로 실측 1회) |
| 2026-07-03 | **검증 상태** (SKILL.md ⚠️절에서 이동 — 방침 C): *실측 1회(위 dogfood 항목).* 이전엔 실측 0회였고, dogfood(simple 강도 · lint 스크립트 신설 과업) 완주로 파이프라인 전 구간 작동을 확인했다. *근거 있는 것:* 이 스킬은 수동 공정(engram-dashboard "구현 실행 규약" — 코더→리뷰어→QA 서브에이전트 분리, 검증돼 운영 중)을 오케스트레이션으로 재구성한 것 + 선례 경고 방어(무한 리뷰 루프 상한 2회 · 재귀 폭발 방어 = 코더는 서브에이전트를 못 낳음 · 모델 다운그레이드 방어 = 스폰 시 모델 명시 강제 · 리뷰 형식화 방어 = 생산자≠리뷰어 fresh 분리). *미검(정직):* 이 오케스트레이션이 수동 공정 대비 결함 검출·품질을 얼마나 올리는지는 단발 dogfood 1회로는 정량 미검 — "근거 있는 가설"로 취급(공정 자체는 검증됐으나 자동화 래퍼 실측은 1회뿐). | 기록 (검증 상태 정본) |
| 2026-07-06 | **TDD 억지 테스트 방지 탈출구 추가(사용자 결정)** — 테스트 기반 없는/자동 테스트가 구조적으로 안 맞는 환경(언리얼 에디터·실기기·체감 의존 등)에서 게이트가 테스트를 강요해 쓰레기 테스트를 양산하는 실패 모드 차단. 코더는 대신 수동 검증 절차를 반환하고 게이트는 그것을 검증물로 검사(flow §2 TDD 항목). 프로젝트별 세부 형태는 그 프로젝트 qa 바인딩이 정의(예: 언리얼 = 빌드 컴파일 자동 + 플레이테스트 체크리스트는 사용자 실측). 관련 장기 후보: 바인딩 남용(전면 면제 선언)의 기계 방어는 qa·review classbased(SEALED/HOLE)화 시 확보 — implement는 자체 바인딩 없는 설계라 classbased 대상 아님. | 반영 |
| 2026-07-07 | **공용 규칙 정합 정리(사용자 결정 — 승격 대신 뜻 통일):** §5 mode-aware에 모드 판정 근거 = 사용자 발화뿐(ADR-0006, review sealed 정본과 동일 구속 — implement엔 SEALED 마커 규약이 없어 prose로) 추가. 감사에서 이 구속 누락 적출. | 반영 (2026-07-07) |
| 2026-07-07 | **문구 담백화(사용자 지시 — 설계 DNA "문구 = 담백"):** SKILL.md 정체성 재서술 2건·강도 라우팅 중복 문장 삭제, 자체 바인딩 금지 이유 압축. flow §3·§5의 제목이 이미 말한 이유 재서술 4건 삭제. 의미·정량 불변. | 반영 (2026-07-07) |
| 2026-07-07 | **최종 보고 피드백 한 줄 의무(사용자 결정):** flow 최종/결과 보고 절에 "피드백: 없음"도 보고하는 한 줄 의무 추가(파일엔 발견 시만 — 조용한 스킵 관측). 규약 정본 = _shared/self-improvement-feedback.md. 게이트 = review doc full(Opus PASS · Codex FIX 반영: 축약 + "최종 보고" 통일) + qa 등가 실행 PASS. | 반영 (2026-07-07) |
| 2026-07-07 | **4렌즈 감사 반영(사용자 위임):** SKILL.md §0-1 데드 포인터 수정 + `_shared` 낡은 캐비앗 삭제 / flow §3 "simple→light = 단일 family" 명시(오독 차단) / §4 qa PARTIAL 분기 신설(PASS 아님 — 채워 재실행 또는 에스컬레이션, 커밋 진행 금지) / §5 열화 라벨 전파 규칙(자식 게이트 라벨 요약 세탁 금지). | 반영 (2026-07-07) |
| 2026-07-07 | **4렌즈 감사 적출:** ① 실행자 티어 자기점검 가드 부재(opus급 하한이 문서 밖 지식으로만 존재) ② §0 인라인 예외·simple 자기분류가 무게이트(자기 선언만으로 파이프라인 전체 우회 가능). 실전에서 실제 뚫리는지 관측 후 반영 판단(선제 장황화 금지). | 미반영 |
| 2026-07-12 | **코더 스폰 dead letter 수정(review trd full 적출 — headless dogfood):** flow §2의 `subagent_type: general-purpose` 하드코딩이 사전의 코더(복잡)=worker-senior 프리셋 바인딩과 모순(프리셋이 코더 경로에서 영원히 미사용) → 호출 바인딩을 사전 "호출" 열 참조로 교체(§0 불변 문구 포함, live+staging). | 반영 (2026-07-12) |
| 2026-07-12 | **루프 상한 개정(사용자 결정):** 총량제(재수정 총 2회 + 승격 하드캡 3회)가 매 라운드 직전 findings를 전부 닫는 정상 수렴(실전 i18n 커밋 1: 6→3→1건)까지 막음 → 연속 미해소 카운터로 교체: 직전 findings 전부 닫으면 리셋, 못 닫거나 reopen이면 +1(상한 2회) + 총 재수정 하드캡 5회(무의미 토큰 소모 방어). flow §3·§4·가드레일 + SKILL.md 동기 수정(live+staging). | 반영 (2026-07-12 · 게이트 미실시 — 사용자 지시로 규칙만 반영, 다음 /implement 실주행이 실측) |
