# adr — 개선 히스토리

이 스킬을 쓰다 발견한 결함·개선점을 누적한다(덮어쓰기 금지). 반영은 사용자 승인 하에. 규약 = `SKILL.md` "자기개선 피드백" 절.

## 이력

| 날짜 | 발견 | 상태 |
|---|---|---|
| 2026-06-26 | **dashboard 레거시 ADR 3건의 처리 방침 미정** — ① 단방향 자연어 링크(0023→0026 · 0027→0029)를 정형 키워드로 마이그레이션할지 lint을 자연어 허용으로 완화할지 ② 0016/0024 레거시 부분폐기에 본문 양방향 링크를 소급해 박을지 보존할지 ③ 인덱스 제목 16건이 본문 H1보다 풍부한 큐레이션인데 "본문 = 단일 출처"를 레거시에도 적용할지. 골격 결함이 아니라 소비처 데이터 큐레이션 결정이다. | 검토 대기 (사용자 결정) |
| 2026-06-26 | **하이브리드 재작성 완료** — `scripts/adr.mjs`(결정적: 채번·스캐폴드·supersede 양방향·index 재생성·lint) + 스킬 3파일 얇게. 위 6개 구멍 전부 닫힘(부분폐기→partial Amends / lint거짓양성→상태어휘만 / 채번→재스캔+lint중복 / rg노이즈→코드경로한정 / 제목drift→index재생성+보존 / 상태정본→index가 본문서 파생). 실데이터 32 ADR 무손실 검증(index --write idempotent). **남은 긴장(메인 결정 대기):** ① 레거시 단방향 0023→0026·0027→0029는 자연어("본 ADR이 대체")라 lint이 양방향 누락으로 잡음 — 정형 키워드로 마이그레이션할지 lint을 자연어 허용으로 완화할지 결정 필요. ② 0016/0024 레거시 부분폐기 본문엔 Amends 링크가 없어 인덱스 단서가 본문보다 정보 많음 — supersede --mode partial 재적용으로 본문 양방향 박을지 보존 유지할지. ③ 인덱스 제목 16건이 본문 H1보다 풍부한 큐레이션 — "본문=단일 출처"를 신규에만 적용(보존+경고)할지 레거시도 본문으로 통일할지. | 검토 대기 |
| 2026-07-03 | **검증 상태.** *보장 = 기록 정합성*(채번 충돌 없음 · supersede/link 양방향 일치 · 인덱스↔헤더 일치 · 템플릿 형식 준수) — 스크립트가 결정적으로 검사·강제한다. 근거 = 하네스 33/33 + 실데이터 133 ADR 사본 회귀 error 0(2026-08-12 실측). 이 보장은 전적으로 `adr.mjs` 정확성에 의존한다. *보장 안 함 = 결정의 옳음* — 빈약하거나 틀린 결정도 형식만 맞으면 기록은 PASS한다. 타당성은 호출자·review(적대 검증)·앞단(prd/trd)의 몫이다. *LLM 환각*(본문에 없는 API·가짜 근거·지어낸 대안)은 검증된 실패 모드 — 거부한 대안·근거는 호출자가 준 것만 박제하고 채운 본문은 호출자가 fact-check한다. | 기록 (검증 상태 정본) |
| 2026-07-06 | **경량(결합 메타줄) 형식은 supersede·link를 못 쓴다** — 독립 `- 상태:` · `- 관련:` 줄이 없어 도장·링크를 박을 자리가 없고 스크립트가 안전 거부한다. 팩토리는 번복이 드물어 수동 처리로 감수 중(바인딩에 명시). 자동화하려면 경량 템플릿에 관련 줄을 넣는 설계 변경이 선행돼야 한다. | 미반영 |
| 2026-07-06 | **공용화 개조: 스크립트를 스킬 내장(`scripts/adr.mjs`)으로 이동 + 플래그 파라미터화**(--root/--dir/--index-name/--status-vocab/--default-status/--template/--anchor-roots — 기본값 = dashboard 무플래그 동일 동작, 바이트 동일 회귀 검증). 소비처 2곳: dashboard(기본값) + skill-factory(경량 형식 — `bindings/skill-factory.md` 신설, 결합 메타줄 파싱 지원, full supersede는 결합줄에서 안전 거부·수동 안내). 팩토리 인덱스(doc/decisions/README.md) 신규 생성·멱등 확인. 잔여 후보: 경량 형식 supersede 자동화 · --amend-only(1신→다구 부분개정). | 기록 (개조) |
| 2026-07-07 | **바인딩 이식 개조 파일럿 (ADR-0004 규약)** — 바인딩을 스킬 폴더(`references/bindings/<project>.md`) → 소비처 프로젝트 트리(`<project>/.claude/skill-bindings/adr.md`, cwd-상대 Read)로 이관. factory 바인딩을 `.claude/skill-bindings/adr.md`로 옮기고(절대경로 `I:/…/doc/decisions` → 상대 `doc/decisions`), flow.md "바인딩 1회 선언" 블록을 cwd-상대 Read 지시로 개정(작업본 = `staging/adr-binding-rework/references/flow.md`). **dogfood PASS:** fresh Sonnet가 개정 flow만 보고 cwd-상대로 바인딩 로드 → lint 명령·플래그 자력 추출 → 실행 → 정합(4건, 0/0). **적출 갭(미반영):** flow.md가 `<skill>`(스킬 자기 폴더 경로) 해석을 미명세 — global 심링크+다중 사본 상황에서 근거 부족. 실 Claude Code 호출은 하네스가 스킬 base dir 제공(부분적 dogfood 아티팩트)이나 flow에 "`<skill>` = 호출 시 제공된 스킬 base dir" 한 줄 명시 권장. **미완(배포 전):** ① 개정 flow를 done/·live·dashboard로 반영 ② dashboard 바인딩도 프로젝트 트리로 이관 ③ 구 `references/bindings/` 제거는 global 이사 시. 배포 시점 = 사용자. | 검토 대기 (파일럿 통과·미배포) |
| 2026-07-26 | (engram-dashboard, C4 기록): ① supersede에 "기존 도장 쌍에 조항 추가/병합" op 부재 — 같은 새 ADR이 옛 ADR의 두 번째 조항도 대체함을 뒤늦게 발견했을 때 스크립트 경로가 없어 손편집(3곳 동일 문구) + lint로 양방향 검증했다(우회 성공, op 추가 검토 권고). ② index 생성기가 관련줄의 첫 Amended-by만 status 셀에 렌더 — 0103처럼 Amended-by가 2건이면 두 번째(0107) 개정이 인덱스에서 안 보인다(ADR-0066 선례 동일). | 미검증: 리뷰어 실측 + 손편집 lint clean 확인 |
| 2026-07-29 | (engram-dashboard, ADR-0111~0113): 한 결정이 옛 ADR 두 건(0103·0104)을 함께 개정하는 케이스 — supersede가 `--old` 1건만 받아 두 번째 옛 ADR용 연동 ADR(0112)을 별도 채번해 해소(양방향·lint clean, 연동 ADR에 실내용도 담아 무해). 기존 잔여 후보 `--amend-only`(1신→다구, 2026-07-06 행)의 실수요 첫 재발 데이터. | 우회 성공 1회 관찰 |

## 아카이브 (반영·기각 완료)

반영·기각이 끝난 행을 접어 둔 곳이다. append-only 규약상 지우지 않는다 — 같은 제안이 재등장할 때 이력이 근거다.

| 날짜 | 발견 | 상태 |
|---|---|---|
| 2026-06-26 | **부분 폐기(supersede-in-part) 미지원** — 실데이터 0016/0024는 상태 "확정" 유지 + 단서절. supersede가 통째 "폐기"로 덮어 살아있는 조항까지 죽일 위험. | 반영 (2026-06-26 하이브리드 재작성 — 같은 날 아래 행이 종결 기록. 셀 전이 2026-07-26) |
| 2026-06-26 | **lint 상태 비교가 복합 상태 문자열에서 거짓양성.** "상태 어휘(확정/제안/폐기/거부)만 비교, 단서절 자유서술 무시" 기준 필요. | 반영 (2026-06-26 하이브리드 재작성 — 같은 날 아래 행이 종결 기록. 셀 전이 2026-07-26) |
| 2026-06-26 | "상태 정본=본문 헤더"가 부분 폐기에선 깨짐 — 0024는 인덱스가 본문 상태줄보다 정보가 많음. | 반영 (2026-06-26 하이브리드 재작성 — 같은 날 아래 행이 종결 기록. 셀 전이 2026-07-26) |
| 2026-06-26 | **채번 race/번호 빠짐 무방비.** 단일 오케스트레이터 가정 명시 + lint가 중복 번호를 backstop으로 검출하게. | 반영 (2026-06-26 하이브리드 재작성 — 같은 날 아래 행이 종결 기록. 셀 전이 2026-07-26) |
| 2026-06-26 | 고아 앵커 `rg "ADR-"`가 `docs/`까지 긁어 거짓양성. `// ADR-` + 코드 경로(`crates/ src/`) 한정, `docs/` 제외 필요. | 반영 (2026-06-26 하이브리드 재작성 — 같은 날 아래 행이 종결 기록. 셀 전이 2026-07-26) |
| 2026-06-26 | 본문 H1 제목 ↔ 인덱스 제목 drift를 lint가 검사 안 함(0016에 실제 drift). | 반영 (2026-06-26 하이브리드 재작성 — 같은 날 아래 행이 종결 기록. 셀 전이 2026-07-26) |
| 2026-06-26 | **큰 방향:** adr을 하이브리드(결정적 스크립트 + 얇은 스킬)로 재설계 검토 중. 부분 폐기 형식은 OSS 리서치(adr-tools/MADR/log4brains) 대기 — 리서치 채번 갈래만 회수, 나머지 중단 상태. | 반영 (2026-06-26 하이브리드 재작성 — 같은 날 아래 행이 종결 기록. 셀 전이 2026-07-26) |
| 2026-07-07 | **문구 담백화(사용자 지시):** flow §역할 분담 말미의 SKILL.md §불변 중복 문장 삭제 · SKILL.md 오퍼레이션 절의 헤더 재서술 문장 삭제. 의미 불변. | 반영 (2026-07-07) |
| 2026-07-07 | **최종 보고 피드백 한 줄 의무(사용자 결정):** flow 최종/결과 보고 절에 "피드백: 없음"도 보고하는 한 줄 의무 추가(파일엔 발견 시만 — 조용한 스킵 관측). 규약 정본 = _shared/self-improvement-feedback.md. 게이트 = review doc full(Opus PASS · Codex FIX 반영: 축약 + "최종 보고" 통일) + qa 등가 실행 PASS. | 반영 (2026-07-07) |
| 2026-07-07 | **4렌즈 감사 기각:** "base dir 미제공 시 전 오퍼레이션 차단"(fresh 감사 HIGH 주장) — Claude Code 하네스가 스킬 호출 시 "Base directory for this skill"을 항상 제공하므로 실위험 아님. 하네스 밖 실행은 비지원 환경. | 기각 (하네스 보장) |
| 2026-07-08 | **스크립트 날짜 = UTC 결함:** `adr.mjs`가 메타줄 날짜를 UTC로 찍음 — KST 새벽(예: 07-08 02:42)에 `new` 실행 시 07-07로 하루 어긋남(ADR-0007 채번 시 실측, prose 단계에서 수동 정정). 수정안 = 로컬 날짜 사용 또는 `--date` 플래그. | 반영 (2026-07-26 — scaffold() 로컬 날짜 전환. 실측: KST 새벽(로컬 07-26/UTC 07-25) 재현 조건에서 로컬 날짜 정확 + lint·index 회귀 clean. UTC 전환은 필요 시 재론 = 사용자) |
| 2026-07-11 | **부분 폐기 마커가 폐기당한 ADR의 상태줄에 안 박힘** — `supersede --mode partial`이 옛 ADR에 `Amended by`(관련줄) + 인덱스 단서만 넣고 **상태줄은 "확정" 그대로**. CLAUDE.md rot-방지 규칙("폐기는 폐기당한 ADR의 `상태:` 줄에 박는다")이 상태줄을 지목하는데 스크립트는 관련줄에만 → 본문 헤더만 읽는 세션이 폐기된 조항을 살아있는 줄 안다. 실측 = ADR-0066 부분폐기 by 0067(doc 리뷰 FIX-1 적출, 상태줄 수동 보강). 선례 0007/0064도 index-only라 systemic. 수정안 = partial이 상태줄에도 `· 부분 폐기 by ADR-N (조항)` 스탬프(단 index --write 재파생과 이중마킹 안 나게) 또는 CLAUDE.md 규칙을 "관련줄+index 허용"으로 완화. | 반영 (2026-07-26 — 스탬프 자동화, A안 사용자 확정. 상세 = 아래 07-26 행) |
| 2026-07-26 | **partial 상태줄 스탬프 구현(/implement standard, staging 격리 — ADR-0010 첫 완주):** `supersede --mode partial`이 옛 ADR 상태줄 head 끝에 ` · 부분 폐기 by ADR-N (조항)` 자동 도장(어휘·기존 토큰 불파괴, 멱등 = head 내 depth-0 매치만, clause 위생 = 타입 스택 + 예약어 거부, `ADR_LIB_ONLY` 게이트 엄격화, index 잘림 경고 신설 — 파생 출력 불변). flow §1(clause 제약)·§2(스탬프 서술)·§3(statusStamp 보고) 동기. **게이트:** review code full 5라운드 수렴(cross-family — HIGH 2·MED 5·LOW 6 적출·전부 닫힘. 라운드3 blanking 접근이 결함 2건을 새로 만들어 라운드4에 접근 교체 — "원문 불변 + 위치 메타데이터 판정"이 스캐너 기본값이라는 교훈) + qa 등가(하네스 24/24 · 실데이터 사본 회귀 error 0 · diffs 18 불변 · lint-skill 4/4). **잔여(선존·별건):** 인덱스 셀 첫 Amended-by만 파생 + 반각 괄호 잘림 13건(경고로 노출, 파생식 변경 = 사용자 결정 대기) · 결합 메타줄(factory 경량) 도장 보류. | 반영 (2026-07-26) |
| 2026-07-31 | **결함 2건(engram 실주행 — ADR 3건 신규 + 부분폐기 3건):** ① **인덱스 합성이 개정 1건만 표시한다** — 한 ADR이 둘 이상에게 부분 폐기당하면 `index --write`가 **첫 `Amended by`만** 셀에 넣는다. 손으로 채워 넣어도 `index --check`가 `rewritten`으로 잡혀 다음 `--write` 때 다시 지워진다(보존 규칙이 이 경우를 못 막는다). 실피해: 이번 실행의 `index --write`가 ADR-0108의 0118 개정 표기를 **조용히 삭제**했고(회귀), 리뷰가 잡았다. CLAUDE.md가 "다음 세션은 인덱스를 본다"고 규정한 진입점이라 rot 직결. ② **기존 두 ADR 사이에 개정 링크를 추가할 방법이 없다** — `supersede`는 항상 새 ADR을 만든다. 이번엔 0120↔0116·0119↔0113·0121↔0111 링크를 손으로 박아야 했고, 이는 골격 가드레일("스크립트 우회 금지")과 정면 충돌한다. `link --old N --new M --clause "..."` 류 오퍼레이션 필요. | 반영 (2026-08-12 — `matchAll` 다중 조항 합성으로 근본 수정. 9회 재발 기록이 근거) |
| 2026-07-31 | **위 ① 근본 원인 확인(리뷰 실측):** `scripts/adr.mjs`의 인덱스 합성이 `Amended by ADR-N (...)`을 `matchAll`이 아닌 **단발 `.match()`** 로 잡아 첫 링크만 쓰고 곧바로 `derivedFully: true`로 early return한다 → 손보존 분기(`legacyPartial`·`curatedStatus`)에 **도달조차 못 해** 수작업 셀이 파생값으로 덮인다. 즉 "손보존이 안전장치"라는 전제가 이 경로에선 거짓. 같은 원인으로 **선재 오염 6행**(0019·0066·0076·0094·0103·0107 — 각각 둘째 개정 누락, 0103은 셋 중 둘 누락)이 이미 있고, 0103은 상태줄 도장 자체가 없는데 `lint`가 관련줄만 검사해 안 잡는다. 수정 방향: `matchAll` + 다중 조항 합성, 그리고 lint에 상태줄 도장 검사 추가. | 반영 (2026-08-12 — `matchAll` 다중 조항 합성으로 근본 수정. 9회 재발 기록이 근거) |
| 2026-07-31 | **"기존 두 ADR 사이 개정 링크" op 부재가 같은 날 3회째 재발 — 이번엔 서브에이전트가 가드레일을 어겼다:** 앞 행(0120↔0116 등 메인 손편집)에 이어, 슬라이스 C 구현 코더가 ADR-0112↔0121 부분폐기 도장을 **손으로** 박았다(결과는 정확·본문 무변경). 새 데이터포인트 = 이 op 부재가 "스크립트 우회 금지" 위반을 **메인 밖 서브에이전트까지 전파**시킨다는 것 — 스크립트 경로가 없으면 누가 하든 손편집이 유일한 길이 된다. `link --old N --new M --clause "..."` 필요성이 하루 3회 실수요로 확인됐다. 부수: 0112가 이제 `Amended by` 2건(0116·0121)이 돼 위 결함 ①(단발 `.match()`)의 **선재 오염 7번째 행 후보**가 됐다 — 다음 `index --write`가 0121 표기를 지울 것으로 예상(이번 실행에서 확인 예정). | 반영 (2026-08-13 — link 오퍼레이션 신설. 손편집 유발이 근거) |
| 2026-07-31 | **위 결함 ①의 심각도 상향 — 이제 `index --write`가 사실상 봉인됐다:** 슬라이스 C 코더가 `index --check`로 회귀를 미리 확인하고 **`index --write` 실행을 정당하게 거부**했다(돌리면 0108·0111·0116 세 행의 두 번째 개정 표기가 삭제됨). 즉 결함이 "나쁜 출력을 낸다"에서 "인덱스 재생성 오퍼레이션 자체를 쓸 수 없다"로 올라갔다 — 다중 개정 ADR이 하나라도 있으면 그 뒤로 아무도 인덱스를 스크립트로 갱신할 수 없고, 손편집만 남는다(가드레일과 정면 충돌). 오늘 0112가 개정 2건이 되어 다중 개정 행이 7개로 늘었다. `matchAll` 수정 우선순위 상향 근거. | 반영 (2026-08-12 — `matchAll` 다중 조항 합성으로 근본 수정. 9회 재발 기록이 근거) |
| 2026-08-01 | **★기각 사유가 약한 「거부한 대안」이 곧 정답이었다 — 2연속 발생(engram dashboard S18, 실측)★:** ① ADR-0121 이 「정통 FIFO」를 *"재시도 루프에 묶인다"* 로 기각하며 그 문장에 스스로 *(미검증)* 딱지를 달았다 → ADR-0124 가 그대로 채택. ② ADR-0124 가 「동기 드레인」을 *"남의 백로그를 발신자가 치른다"* 로 기각 → **몇 시간 뒤 사용자가 그 안을 직접 제시**해 ADR-0125 로 채택되고 0124 는 전면 폐기. **두 번 다 기각 사유가 정량 없이 서술뿐이었고, 두 번 다 그게 정답이었다.** 비용: 0124 는 코드 착수 전 폐기라 구현 손실 0 이지만 **ADR + spec 개정 + `/review doc` 2인 3라운드 = 문서 파이프라인 1회분이 통째로 낭비**됐다(리뷰어 1회 12~24만 토큰 × 6~8회). **후보:** §1 입력 수령에 "거부한 대안마다 기각 사유의 **강도**를 자평하라 — 정량·실측·코드 근거가 없고 서술뿐이면 `약함` 표시" + "약함으로 표시된 대안은 ADR 확정 전에 **사용자에게 선택지로 올린다**". ADR 은 재론을 막는 장치인데, **약한 기각 사유는 재론을 막기는커녕 다음 세션이 뒤집을 좌표를 찍어 주는 셈**이다. | 반영 (2026-08-13 — flow §1 기각 근거 강도 자평 신설) |
| 2026-08-02 | **결함 ①(단발 `.match()`) 4회째 실측 — 손실 4행이 인계 기록과 정확히 일치:** engram dashboard 에서 ADR-0126(0099 부분 폐기) 박제 중 `index --check` → `--write` → 수동 복구 절차를 그대로 밟았다. `--write` 가 지운 행 = **0108·0111·0112·0116**(각각 둘째 개정 표기), 이전 세션 인계가 예고한 목록과 동일. 이번엔 **본문 상태줄에는 두 도장이 모두 정상 존재**함을 직접 확인해(0108 = 0114·0118, 0111 = 0117·0121, 0112 = 0116·0121, 0116 = 0121·0120 + 취소선 철회분 1건) **원인이 본문이 아니라 인덱스 파생 로직임을 재확인**했다. 부수 관찰: 0116 의 취소선 처리된 철회 도장(`~~부분 폐기 by ADR-0124~~`)은 파생이 올바르게 무시했다 — `matchAll` 수정 시 이 취소선 제외 동작을 회귀시키지 말 것. 워크플로 비용 = ADR 1건 박제마다 `--check` 파싱 + Edit 4회 + lint 재확인이 고정 오버헤드로 붙는다. | 반영 (2026-08-12 — `matchAll` 다중 조항 합성으로 근본 수정. 9회 재발 기록이 근거) |
| 2026-08-03 | **결함 ①(단발 `.match()`) 5회째 실측 + 새 결함 ②(다중 supersede 미지원):** engram dashboard 에서 ADR-0127(0110·0113 동시 부분 폐기) 박제. **①** `index --write` 가 이번엔 **5행**을 잘랐다 — 기존 4행(0108·0111·0112·0116)에 더해 **이번에 두 번째 도장을 받은 0113**(0119 + 0127)까지. 즉 다중 개정 ADR이 늘 때마다 손실 행이 1씩 는다(예고대로 단조 증가). 수동 복구 Edit 5회. **②(신규)** 스크립트 `supersede` 가 `--old <N>` **단일**만 받아, 한 결정이 여러 ADR을 동시에 부분 폐기하는 실제 사례(ADR-0127 이 0110 결정 3·4 와 0113 결정 3 을 함께 대체)를 못 다룬다. 두 번 돌리면 새 ADR 이 2개 생기므로 우회 불가 — 주 대상만 스크립트로 박고 **나머지 양방향 링크·상태줄 도장은 손으로** 넣었다(가드레일 「스크립트 우회 금지」와 정면 충돌, 형식 불일치 위험을 사람이 떠안음). lint 은 손편집분도 정상 인식(양방향 error 0). **후보:** `--old` 를 반복 플래그로(`--old 110 --clause "..." --old 113 --clause "..."`) 또는 `--also-old N:조항` 추가. | 반영 (① 2026-08-12 `matchAll` 근본 수정 · ② 2026-08-13 supersede 다중 대상 지원) |
| 2026-08-03 | **결함 ①(단발 `.match()`) 6회째 + 새 변종 ①-b(손편집분은 파생조차 안 됨):** engram dashboard 에서 ADR-0128(0126·0099 동시 부분 폐기) 박제. **①** `index --write` 가 지운 행 = **0108·0111·0112·0113·0116** — 5회째와 정확히 동일 5행(이번엔 새로 다중이 된 ADR이 없어 증가 없음). 수동 복구 Edit 5회. **①-b(신규 변종)** 결함 ②(다중 supersede 미지원) 때문에 손으로 박은 **0099 의 두 번째 도장(0128)이 인덱스에 아예 반영되지 않았다** — 잘림이 아니라 **미출현**이다. 즉 두 결함이 곱해진다: ② 때문에 손편집이 강제되고, ① 때문에 그 손편집이 인덱스로 전파되지 않아 **복구 대상이 5행 → 6행으로 늘어난다**(스크립트가 박은 0126 은 정상 파생, 사람이 박은 0099 는 누락 — 같은 실행 안에서 갈렸다). 다중 supersede 1건당 수동 복구가 +1 행 고정 비용이라는 뜻. **후보 불변:** `matchAll` + 다중 조항 합성(①), `--old` 반복 플래그(②) — 둘이 독립이 아니라 ②를 고치면 ①-b 는 자동 소멸한다(손편집 경로가 사라지므로). 우선순위 판단에 반영할 것. | 반영 (2026-08-12 — `matchAll` 다중 조항 합성으로 근본 수정. 9회 재발 기록이 근거) |
| 2026-08-05 | **★결함 ① 7회째 — 그런데 이번엔 회귀가 커밋 직전까지 갔다(engram dashboard, ADR-0130)★:** 앞의 여섯 실측과 다른 점은 손실 자체가 아니라 **누가 잡았느냐**다. 이전 회차들은 세션이 `index --check` 를 먼저 돌려 손실을 **예측하고** 복구했는데, 이번 세션은 그 절차를 모른 채 `--write` 를 바로 돌렸고 **손실을 스스로 알아채지 못했다** — 잡은 것은 후행 `/review doc` 리뷰어였다(BLOCK 판정 근거 1). 지운 행 = **0099·0108·0111·0112·0113·0116**(6행, 본문 상태줄엔 두 도장 모두 정상 존재). 복구 = `git checkout -- README.md` 후 신규 행 추가 + 개정 대상 상태 갱신만 손편집. **새 관찰:** `--check` 선행은 스킬 flow 에도 바인딩에도 **절차로 적혀 있지 않고** 지금까지 세션의 인계 지식으로만 전달돼 왔다 — 즉 이 결함의 방어선이 문서가 아니라 구전이었고, 세션 교체 한 번에 뚫렸다. 리뷰 게이트가 없었으면 그대로 커밋됐다. **후보(수정 전 임시 방어):** 바인딩 §호출 순서에 "`index --write` 전에 반드시 `--check` 로 `rewritten` 행을 확인하고, 다중 개정 행이 잡히면 `--write` 를 쓰지 말고 손으로 최소 편집" 한 줄 추가 — `matchAll` 수정 전까지의 스톱갭. | 반영 (2026-08-12 — `matchAll` 다중 조항 합성으로 근본 수정. 9회 재발 기록이 근거) |
| 2026-08-11 | **결함 ① 8회째 — 7회째가 남긴 스톱갭 후보가 아직 바인딩에 없어 같은 순서로 재발(engram dashboard, ADR-0132):** `supersede --old 128 --mode partial` 후 `index --write` 를 `--check` 선행 없이 바로 돌렸고, 지운 행 = **0099·0108·0111·0112·0113·0116**(6행 — 7회째와 동일 목록). 이번 세션도 `--check` 선행 절차를 사전에 알지 못했다(바인딩 §호출 순서는 "prose 먼저, 인덱스 나중"만 말하고 `--check` 를 요구하지 않는다 — 2026-08-05 행의 후보가 6일째 미반영). **새 관찰(방어선 후보):** 7회째는 후행 리뷰어가 잡았지만 이번엔 **스크립트 자신의 JSON 출력이 방어선이 됐다** — `index --write` 응답의 `diffs[]` 에 `field:"status", action:"rewritten"` 6건이 그대로 실려 있어, 그것만 읽어도 리뷰 없이 즉시 탐지된다. 즉 이 결함은 "출력을 읽으면 반드시 보이는" 종류이고, 놓치는 경로는 **JSON 을 안 읽고 ok:true 만 보는 것** 하나뿐이다. 스톱갭을 `--check` 선행 대신 "`--write` 응답의 `action:\"rewritten\"` 행을 반드시 확인" 으로 잡으면 왕복이 1회 줄고 방어력은 같다. 복구 = Edit 6회 후 diff 로 의도 2행만 남음을 확인. | 반영 (2026-08-12 — `matchAll` 다중 조항 합성으로 근본 수정. 9회 재발 기록이 근거) |
| 2026-08-12 | **★결함 ① 근본 수정 완료 — 9회째 재발을 마지막으로 닫았다(engram dashboard, ADR-0133 박제 중 사용자 지시)★:** `deriveIndexStatus` 의 단발 `.match()` → `matchAll` 다중 조항 합성으로 교체(구분자 `" / "`, 1건일 때 출력은 종전과 **바이트 동일** — 회귀 없음). 9회째 손실 6행(0099·0108·0111·0112·0113·0116)은 종전대로 손복구했고, 고친 뒤 `index --check` 가 **여태 아무도 몰랐던 8행을 추가로 드러냈다**(0019·0066·0076·0094·0103·0107·0112·0115 — 선재 오염이 실측 기록보다 많았다). **이어서 같은 뿌리의 둘째 결함도 닫았다:** 조항 캡처 `\(([^)]*)\)` 가 조항 안의 반각 괄호에서 끊겨 셀 문구가 어절 한복판에서 잘리던 것(경고로만 노출되던 13건)을 **괄호 균형 스캔**(`extractAmendedBy`, 전각 포함·불균형 시 종전 동작 폴백 + truncated 표시)으로 교체 — 잘림 경고 13 → 0. **최종 상태(실데이터 133건):** `index --check` rewritten 0 · 잘림 0 · `lint` error 0(advisory 2 = 선재 레거시 0016 링크 미마이그레이션 · 폐기 ADR-0027 코드 앵커). **취소선 도장 무시 동작(2026-08-02 행이 회귀 금지로 지목)도 유지** — 균형 스캔은 `Amended by ADR-N (` 매치에서만 출발하므로 `~~…~~` 안의 철회 도장을 새로 집지 않는다. **남은 미반영(독립):** 결함 ②(`supersede --old` 단일만 지원 — 다중 부분폐기 시 손편집 강제) · `link --old N --new M --clause` op 부재 · 2026-08-01 행(약한 기각 사유 자평). ②가 살아 있는 한 손편집 경로는 남지만, ①-b(손편집분 미출현)는 이번 수정으로 자동 소멸한다(파생이 손편집 링크도 전부 읽는다). | 반영 (2026-08-12 — 실데이터 검증) |
| 2026-08-13 | **`link` op 첫 실주행 성공 — 그런데 조항 문구에 길이 지침이 없어 인덱스 표가 비대해졌다(engram dashboard, ADR-0134가 0081·0022를 부분 폐기):** 2026-07-31 행이 요청한 `link --old N --new M --mode partial --clause "..."`가 이번에 실제로 쓰였고 **손편집 0회로 양방향 링크 + 상태줄 도장이 정확히 박혔다**(양쪽 문구 동일, lint error 0, `index --write` rewritten 2건이 의도분과 정확히 일치 — 근본 수정 이후 첫 다중 개정 실주행에서 손실 0 확인). **새 결함(경미):** 메인이 「무엇이 죽고 무엇이 사는지」를 조항에 다 넣어 250~280자 clause를 만들었고, 인덱스 셀이 그것을 **그대로** 실어 표가 무너질 뻔했다. flow §1의 clause 제약 절은 **금지 문자만** 말하고 길이를 말하지 않는다 — 조항이 인덱스 셀로 직행한다는 사실 자체가 골격에 안 적혀 있어, 조항을 「사유」로 오해하기 쉽다. 복구 = 짧은 라벨로 되돌리고 살아 있는 조항 목록은 **새 ADR의 영향절**로 이관(그쪽이 정본으로 더 적절했다). **후보:** §1 clause 제약에 한 줄 — 「조항은 **라벨**이다(인덱스 셀에 그대로 실린다). 짧은 명사구로 쓰고, 무엇이 죽고 무엇이 사는지의 서술은 **새 ADR 영향절**에 둔다.」 | 미반영 |
