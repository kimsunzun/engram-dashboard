# TRD review round 3 — consolidated findings (orchestrator, 2026-09-24)

Target: TRD sha256 `ad812823…4812` (round-2 revision; diff vs round-2 snapshot = scratchpad `review-r2/r3-trd.diff`, 380 lines). Scope = revision only; 76 changed pointers pre-verified by script (76/76).
Reviewers: codex Designer (blind, resumed thread — confirmed same thread, 58k / 2) **PASS, 0 findings** · Claude concurrency lens (239k / 23) **FIX** · Claude Architect-breaker doc-aware (308k / 40) **FIX**.
Aggregate: **FIX** — label split only (codex didn't look at these spots; no finding contradicts another), autonomous mode → heavier label. Round-2 [A] items 1–5: RESOLVED (doc-aware 5/5; lens: #4, #5a PARTIAL → items below). [B] block faithful and self-contained (both).

## [A] fix now (all low / low-medium, one file)
1. **ADR-A amend list gaps (doc-aware N1 medium, N2 low).** A2's claude handle-less Resume → `spawn_fresh_settled` changes a user/LLM-visible outcome (`SpawnProfile{resume:true}` on a claude profile whose 0-turn id D1 did not persist: today `Failed`+`NoConversationToResume`; after A `Started` fresh). Contradicts ADR-0076:14 (「명시적 resume=true 는 그대로 존중」) and ADR-0082:16 (「…시스템이 임의로 새 대화를 만들지 않는다」); ADR-0082:37 names `new_session_id` as the Fresh path. Orchestrator ruling: covered by user decision D1 (no stored id ⇔ no conversation ⇒ Fresh) and matches today's codex bypass `manager.rs:1989-1999` (commented 「사용자 결정」) — it is NOT an author 「고름」; say so. Add to ADR-A: amend ADR-0076 d1 and ADR-0082 d1, scoped to "a resume request with no handle to resume = 0082:21's 「이어받을 게 없는 … 정상 생성」". Also ADR-0145 **decision 1** (0145:14 empty-state rule) is amended, not only its rationale :37 — new rule: `'live'` + no rendered item + not continuing → empty state; continuing → loading.
2. **Mail path note placement (lens N1).** `submit_input_observed` notes before the body; body write + `confirm_written` (≤5 s `INPUT_FLUSH_BUDGET`) + `SUBMIT_PACING` 0.5 s precede the CR → a kill in that window persists S with no turn (A alone → P2 failure next activation). Parked mail flushes right after roster publish (`manager.rs:1447` → `messaging_host.rs:450-490`), so spawn+quick kill / `shutdown_all` during boot restore hits it; same for body/`confirm_written` errors. Fix: in `submit_input_observed` do the UserKill check + note right before `send_input(submit)` inside `if let Some(submit)` (after pacing). Raw always has `Some(CR)` (`backend/mod.rs:917-923`); JSON encoders return `None` and already note inside `write_input_observed`. §4-1 assertion becomes "Raw: note after body, before CR · JSON: note before body". Restate residual ① width; shrink the §3-6 「영속했는데 send_input 이 실패」 row.
3. **Structure test ③ misses the latch builder (both).** `SpawnIncarnation` forces the latch in as an argument but not onto the session (`latch: _` compiles) → forgetting `.with_session_id_latch(` = nothing ever persists = every resume silently vanishes (§3-2-2 worst case); only other catch is the unconfirmed PATH test (Q4). Fix: ③ also asserts `.with_session_id_latch(` in `spawn_session` fed from the same latch, and/or destructure `SpawnIncarnation` so both fields must be used. Update §4-2 「셋이 함께 진다」 accordingly.
4. **Capture timing + GUI step 7 uses a timer (both).** 0.5/1.5/3.5/7.5 s are lower bounds (sleep then scan; one Restart Manager session per lock file `thread_lock.rs:246`); the only GUI run measured ≈6 s with 10 foreign locks (`step-log.md:2455`); `MAX_DELAY` doc records an unreproduced 21 s. After A, the only "id arrived" log is in the commit port (`manager.rs:512-526` today) → a 0-turn incarnation logs nothing; step 7's `null` passes vacuously if capture hadn't happened; field diagnosis also loses that line. Fix: latch `offer` logs (agent, epoch, 「제출 전이라 영속 보류」) when no submission yet; step 7 waits for that line (trusted folder, `RUST_LOG` as needed) instead of ≥8 s (ADR-0038); reword timing as "at least 1.5/3.5 s + scan time; one GUI run ≈6 s".
5. **Sticky-offer vs poison rule contradiction (both).** Rule + ⑤ say offer-after-submission commits at once; unwind paragraph + ⑨ say after a panicked commit later offers pass without calling the port. Sink contract = exactly once per incarnation (`backend/mod.rs:189`; `run_capture` stops after `Found`; codex `record_session_id` once per handshake; claude offers once at latch creation) → state that, then one rule: after any commit attempt (ok or poison) the port is never called again. Add the port's `expected` cell to the lock order: `latch → port expected → profiles → store`.
6. **Small wording/table (both):** UserKill skip also covers daemon shutdown (`shutdown_all` → `kill_agent` sets UserKill, `manager.rs:2780-2786/:2715`) — correct outcome; `tear_down_failed_activation` doesn't set UserKill — harmless (no offer on codex rejection; claude Resume offer == stored, CAS no-op) — one line. §6-1 A2 row: add `session.rs` (or move the skip into A1, behaviour-neutral). Memo A 「거부한 대안」: tag each item's source (사용자 결정 vs 리뷰 지적·고름) — CLAUDE.md 「결정 날조 금지」. §6-3 bold headline scope to handle-bearing resumes; §3-2-6 「이 고름이」 → Q1 is a user decision now. Optional: note that the CAS "expected follows each commit" clause is defensive (one offer per incarnation) — the 0216:61 argument rests on the first offer.

## Reviewer-suggested process improvement
Doc-aware: most expensive = reading ~8 ADRs clause by clause to test the amend list; an author-emitted "ADR clause ↔ Phase A change" matrix would reduce it to spot checks. (Round-2's claims-TSV suggestion was applied this round: pointer checking dropped out of reviewer cost.)
