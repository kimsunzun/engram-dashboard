# TRD review round 2 — consolidated findings (orchestrator, 2026-09-24)

Target: `docs/process/S21-codex-backend/trd-resume-after-first-turn.md` sha256 `7d91750e…4f36` (unchanged during review). Stage/intensity: trd / deep.
Reviewers: codex Designer (blind, fresh thread, 62k tok / 2 tool calls) · Claude Architect-breaker (doc-aware, worker-senior, 400k / 79) · Claude concurrency/kill/lifetime lens (worker-senior, 322k / 48).
Verdict: **FIX** (3/3). No contradictions between reviewers. Nothing blocks Phase A as a design; items below must land in the TRD before coding.

## Settled
- **Round-1 item 3 (RichSlot `onReset` window) — author rebuttal HOLDS, 3/3 independently** (onReset only at `protocolClient.ts:380` inside `flushToLive`, gated by `restartPending`; `'live'` synchronous at `:398`; `restartPending` set only at `:450` after `'buffering'` `:459`; phase `'buffering'` assigned only at `:447` and initial `SubState` `:757` with pending false). No user escalation needed.
- Round-1 audit: doc-aware 16/16 RESOLVED; lens reviewer rates #9 PARTIAL (see B-1). U1–U5 all became §7 questions (Q5 decided by user = (a); Q3/Q6/Q7/Q8 open, not decided by the author).

## [A] fix in the TRD before Phase A
1. **ADR plan (doc-aware N1, the one substantive item).** Phase A contradicts un-amended ADR text: ADR-0008:10 (persist sid at spawn), ADR-0076 (`new_session_id` for "Fresh=새 sid" — not linked anywhere), ADR-0185 d2 (codex receipt via `observe_session_id`), ADR-0216 d7 + its rejected alternative "빈 칸에만 쓴다" (0216:40,61) carried by ADR-0217 d6·d7 and ADR-0218 d6 (overwrite policy) — 0216/0217 not cited, ADR-0145:37 ("no protocol/backend change"). Fix: two ADRs — **ADR-A (D1+D2+D5) written in A5**, amending 0008, 0076, 0185-d2, 0216-d7/0217-d7/0218-d6, 0145, linking 0083/0172/0204, with an explicit paragraph why CAS is not 0216's rejected "empty-only" (`expected` follows the incarnation's own last commit, so a new id still replaces); **ADR-B (D4) amends 0082, written in B3**. `commit_session_id` shares `observe_session_id`'s guard helper (ADR-0217 d6 "우회로를 새로 만들지 않는다").
2. **Commit panic/failure semantics (doc-aware N2 + lens A-1).** The only `catch_unwind` is inside codex `record_session_id` (`transport.rs:1355`); commits triggered by `note` run on the submitter thread (WS ordered lane `connection_core.rs:1087`, mail flush `messaging_host.rs:135/753`, codex-terminal capture thread `thread_lock.rs:445`) with no guard. Panic sources: `profiles.lock().expect` (`profile.rs:395/406`), `write_lock.expect` (`persistence/mod.rs:101`). ★Doc-aware says release builds are `panic = "abort"` (`transport.rs:1320` comment) → a commit panic aborts the daemon in release; lens reviewer proposes `catch_unwind` in the latch — ineffective under abort. Reviser: verify the profile setting and reconcile.★ State: latch state after failure (committed+settled, log, turn still sent, no retry); disk IO error = `save` swallows it (`persistence/mod.rs:102-104`), bool reflects memory, next successful save repairs. Rewrite the poison paragraph for both call sites. §4-2: the tests `a_recording_failure_ends_the_session` / `the_session_id_is_recorded_before_the_gate_opens` now pin only "offered before the gate"; "persisted before the first turn" is pinned by latch test ⑦ + session order tests + structure test — say so.
3. **Duplicate epoch + daemon test construction (lens A-2 + doc-aware N3).** `with_incarnation(Incarnation{epoch,..}, latch)` adds a second epoch next to `AgentSession.epoch` (`session.rs:28`, used by `WriteOutcome` `:222`, `agent_info`, `write_stdin_observed_if_epoch`) — against the ownership split. Builder takes `continues_conversation: bool` (+ crate-private latch setter); reply = `Incarnation { epoch: self.epoch, continues_conversation }`. `SessionIdLatch` is `pub(crate)` → the §4-1 daemon test "ack `continues_conversation: true`" has no construction path. Preferred: shell profile with a seeded handle + `spawn_agent(Resume)` (flag true because `manager.rs:1350-1358` reads the handle for every backend) — verify it works. Otherwise a `test-harness` hook = new ADR-0012 seam = **user decision** (§4-3 currently says "없다").
4. **Kill window persists a garbage id (lens A-3).** `kill_agent` sets `UserKill` (`manager.rs:2715`) then shutdown+join ≤5 s (`:2722`); the session stays in the map until reaped (`reaper.rs:51-60`). A WriteStdin or mail flush in that window → note → commit S (epoch guard passes) → `send_input` fails. With A only → next activation hits the exact P2 failure. Fix: skip note when `termination_intent()==UserKill`; document the residual in-flight race.
5. **Precision/doc items (minor, batch):**
   - lens A-4: codex-terminal "대개는 offer 가 먼저" is wrong — capture scans ~0.5/1.5/3.5/7.5 s (`thread_lock.rs:242/254/466`, TRD §1 says ≈6 s), parked-mail flush fires sub-second at roster publish (`manager.rs:1447` → `messaging_host.rs:450-490`) → with parked mail the first turn always precedes the commit. Not a regression; fix wording.
   - N4: codex handle-less Resume moving from `resume_no_fallback` (`manager.rs:1999-2066`) to `spawn_fresh_settled` (`:1687-1700`) changes: no 3 s verdict window for no-link terminal, `note_spawn_result` immediately (clears `last_failure`), link failures recorded `Other`, Fresh release runs; `opens_a_new_conversation` permanently false in Ready/Alive arms (`:2181-2196`) → dead branches. List in §6-3; say whether dead branches are removed. "지금 codex 가 이미 그렇게 보고한다" is true of the outcome word only.
   - N5: stale code docs to add to §5: `thread_lock.rs` `run_capture` doc (~410-425), `manager.rs:1328-1345`, `profile.rs:507-525`, `manager.rs:2965` test comment; `protocol/lib.rs:99-102` → the no-bump judgment goes on the `continues_conversation` field doc.
   - N6: with Q5=(a), B2 (not A2) introduces the `reservation` parameter of `spawn_agent_watching_link` — update §6-5.
   - Line fixes: §3-2-2 "운영 밖 호출자가 17곳" → 16 outside production (17 incl. `manager.rs:1859`); §3-2-6/§4-1 `session_tracker.rs:168` → comparison is `backend/claude/session_file.rs:209-222`; the §4-1 precondition test belongs in `session_file.rs`.

## [B] carry into the Phase B round (TRD edits now are fine, low priority)
- B-1 (lens, medium): Terminal-arm Arc acquisition after the verdict races the reaper (`core.finish` → ReapMsg `manager.rs:1826-1838` vs verdict 100 ms poll `:2293-2310`) → `get_session` often NotFound; no rule for missing Arc. Carry `Arc<AgentSession>` out of `spawn_agent_watching_link` on Started (`:1423`) or from the verdict (`:2272`); recheck on that Arc. Must grab before LinkFailed teardown (`:2107-2111`).
- codex (medium, 0.88): shutdown can start between step 5's `shutting_down` check and step 6's registration → `shutdown_all`'s one-time snapshot misses the fresh session. Fence shutdown admission + registration, or shutdown waits for in-flight activations and re-drains. Test pausing between check and registration.
- B-2 (lens, low): `tracker.unwatch(id)` keyed by AgentId only (`session_tracker.rs:131-136`) while teardown is incarnation-guarded (`manager.rs:2215-2232`) → may unwatch a successor. Unwatch only when the teardown matched the epoch.
- N7 (doc-aware, uncertain): claude drifted id Y reported before Y has a transcript → next `--resume Y` NoConversation → D4 drops S. Add to Q3 false-positive list or rule out.
- N8 (doc-aware, low): concurrent claude activation → step 6 `Moot(Some(e3))` → A reports `Resumed` for a doomed e3. Document in §3-6 or tie to Q7(a).
- Note (LLM-first): `continues_conversation` is only on the replay marker; Q3(d) should reuse this per-incarnation fact instead of a new field.
- PRE-EXISTING (lens, low, no change): reaper never unwatches (only `kill_agent` does, `:2725`); with CAS the stale tracker now refuses the next incarnation's first commit; end state same as today.

## Reviewer-suggested process improvement (not recorded — review skill folder is in the shared staging repo)
Most expensive part = doc-aware hand-verifying ~140 `file:line` pointers across ~20 files (400k tokens). Have the TRD author emit a machine-checkable claims list (`path:line` + expected token) that a scout script verifies, leaving reviewers only the reasoning.
