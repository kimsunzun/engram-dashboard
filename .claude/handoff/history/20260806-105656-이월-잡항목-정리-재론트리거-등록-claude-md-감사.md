# 핸드오프: **이월 잡항목 정리 + ADR-0130 재론 트리거 등록 + CLAUDE.md 감사(수정 미착수)** — 커밋 2개 미푸시 · 워킹트리 클린

> **★이 파일은 앞 세션 것과 3곳이 다르다 — 옛 latest.md 를 기억하고 있다면 여기가 이긴다★**
> ① S18.22 이월 쟁점 2건 = **종결**(더는 "사용자 판정 대기" 아님) ② ADR-0130 재개 조건 = **②만 게이트 등록됨**("세 조건 모두 미등록" 은 낡은 문장) ③ `AgentCommand` 혼동 쌍 = **등록 완료, 그리고 두 곳이 아니라 세 곳**이다.

## 한 줄 상태 · 다음 첫 액션

- **상태:** 이월돼 있던 자잘한 항목을 전부 털었다. 굵은 작업은 착수 안 했다.
- **repo:** `master`, 워킹트리 **클린**, **미푸시 2개**(`9e389f7`·`e111f8b`) — 푸시는 사용자 지시 대기.
- **★다음 첫 액션 = CLAUDE.md 수정 6건★.** 감사는 끝났고 **수정만 남았다**(아래 §CLAUDE.md 감사 결과에 항목·근거·참값이 다 있다 — 다시 조사하지 말 것). 그 다음이 **wire 타입 빌드타임 배선**.
- **날짜 주의:** step-log·ADR 본문은 `2026-08-05` 로 적혀 있고 커밋은 `08-06` 에 났다(세션이 자정을 넘겼다). 불일치가 아니라 그냥 그렇다.

## 이번 세션 커밋 2개 (둘 다 미푸시)

| 커밋 | 내용 |
|---|---|
| `9e389f7` | ADR-0130 재개 조건② **재론 트리거** 등록 + `AgentCommand` 혼동 쌍 + `architecture-overview` 중복 수치 제거 |
| `e111f8b` | S18.22 이월 문서 쟁점 2건 **종결** — 사본 삭제가 아니라 **역할 선언**으로 |

전말 = step-log **S18.25**. 커밋 밖 변경 1건: `.engram-data/agents.json` 오염 프로필 3건 제거(15 → 12, 백슬래시 먹힌 cwd — cdp 실측 산물). 데이터라 git 밖.

## ★CLAUDE.md 감사 결과 — 수정 대기 6건 (조사 끝, 근거 포함)★

경량 워커 2인이 **76개 주장**을 코드에 대조했다. STALE 6건, 전부 메인이 재측정해 확인.

**A. 실질적 거짓 — 세션을 잘못 이끈다:**

1. **★§5 "LLM 제어 표면 없음" 이 틀렸다 — 이미 있다★.** 현재 문장은 *"UI/레이아웃은 현재 프론트(Zustand) 전용 = LLM 제어 표면 없음"* + command 버스를 **"설계 지향"**(앞으로 할 일)으로 서술. 실제: `src-tauri/src/layout/`(manager·tree·spatial·types)로 레이아웃이 **백엔드 소유**, `commands/layout.rs` 에 `#[tauri::command]` **16개**, 프론트 `src/commands/` 에 registry·dispatch·contributions + 슬롯/탭/프리셋/테마/키바인딩 커맨드 10종이 `window.__engramCmd` 로 노출(ADR-0035·0055·0057). **가장 해롭다** — 모르는 세션이 **두 번째 제어 표면을 짓는다.** §5 원칙 자체는 살아 있고 틀린 건 "현 갭" 진단부다. **문장 교체가 아니라 그 문단을 다시 써야 한다**(있는 걸 없다고 말하는 중이라 부분 수정이 더 헷갈린다).
2. **`/popup` 라우트가 살아 있다.** 현재 문장 *"`/popup` 라우트·PopupPage 삭제 후 dead route 였다"*. 실제 `src/pages/PopoutPage.tsx` + `App.tsx` 에 등록(ADR-0057, 탭 소유 팝아웃). **절반은 맞다** — `tauri.conf.json` 정적 창은 여전히 main·agent-tree 2개고 팝아웃은 런타임 생성. 틀린 건 "라우트 삭제·dead" 뿐.

**B. 사실 오류:**

3. `backend/claude.rs` → 실제 **`agent/backend/claude.rs`**(`src/backend/` 는 아예 없음).
4. `src/` 폴더 목록에 **`commands/` 누락** — 하필 1번이 말하는 그 폴더. 실제 최상위 = api·**commands**·components·i18n·lab·lib·pages·store·styles·theme·util, `components/` 엔 `ui/` 도 있음.
5. 의존성 목록의 **`base64` 는 직접 의존이 아니다** — 어느 `Cargo.toml` 에도 없고 소스에서 `use` 도 안 한다(전이만). 제거 대상.

**C. 누락:**

6. 기술 스택에 **`react-markdown`·`remark-gfm`·`remark-math`·`rehype-highlight`·`rehype-katex`·`katex` 없음** — 챗 마크다운·수식 렌더에 실사용.

**감사에서 VERIFIED 로 확인된 것**(다시 재지 말 것): 빌드·검증 명령 전체 · 워크스페이스 멤버 · 격리 게이트 명령과 기대값 전부 · 7개 crate 소유 범위 · **핵심 불변식 7개 전부** · S9 세션 복원 · 나머지 의존성 8건 · `scripts/cdp.mjs` 포트·서브커맨드 · 이벤트 이름 3종 · 테마 값. **기조·원칙 층은 건강하고 썩은 건 "현재 상태" 서술과 경로·목록이다.**

## 검증 상태

**재실행 명령(워크스페이스 루트):**
```bash
cargo build
cargo test --workspace --exclude engram-dashboard   # 34타깃 ok (루트 bare cargo test 는 0xc0000139)
cargo test -p engram-dashboard-net                  # 게이트5a → 6 passed
cargo test -p engram-dashboard-net --all-features   # 게이트5b → 31 passed
cargo fmt --check
npx tsc --noEmit && npm test                        # 클린 / 41파일 634
# 격리 게이트 정본 = crates/engram-dashboard-net/src/lib.rs 헤더 (다섯 게이트를 전부 담은 유일한 곳)
rg "^\s*use tauri" crates/engram-dashboard-core/src/                                          # 0
rg "engram_dashboard_(core|daemon|protocol|discovery)" crates/engram-dashboard-messaging/src/  # 0
# ADR-0130 재개 조건② 재론 트리거 (게이트 아님 — 매치 = 멈추고 ADR 재론)
rg -U -n "(crate|(super::)+)::[^;]*\b(connection_core|agent_conn|status_fanout|messaging_host)\b" crates/engram-dashboard-daemon/src/control/
```

**★검증 안 된 것★:**
- **에이전트 spawn/kill 실왕복은 이번에도 안 했다** — 이번 변경이 문서뿐이라 생략. **코드가 실제로 바뀌면 반드시 돌 것.**
- **GUI 실측(cdp) 미실시** — 프론트 경로 무변경이라 qa 바인딩 기준 full 대상이 아니었다.
- **소스 0 변경이라 이번 초록불은 "기준선이 초록" 이지 "변경이 안전" 이 아니다.** 실질 검증은 리뷰어의 사실 대조였다.
- 승계(여전히 유효): src-tauri 핸드셰이크 6건 컴파일만·실행 0(`0xc0000139` — **초록 CI 를 그 커버리지로 오독 말 것**) · JS 발신자 둘(`wsTransport.ts`·`scripts/engram.mjs`)이 `protocol_version` 에코, 테스트 0건 · `dep:windows` 비Windows 해소 미측정 · `protocol::` 일반 allowlist 게이트 부재 · **ADR-0130 재개 조건 ③(순환)은 여전히 미등록**(①은 기계화 불가) · `write_handle.abort()` 미검증·제거 금지 · ws_e2e `case09` flaky · 허수 테스트 1건.

## ★실패한 접근 (do-not) — 이번 세션 신규★

이 세션은 **리뷰 5라운드 중 5번 BLOCK/FIX** 를 먹었다. 코드가 아니라 **전부 메인이 쓴 산문**에서 나왔다.

- **★"자잘한 문서 한 줄" 이라는 자평이 곧 위험 신호다★.** 사실오류 3건이 전부 그렇게 자평한 편집에서 나왔다. **특히 *위험을 경고하는 문장* 은 방향이 뒤집혀도 그럴듯하게 읽힌다** — 1R 에서 "tsc 가 안 잡는다" 라고 정반대로 썼는데 리뷰 없이는 안 잡혔다. 사실 주장은 **쓰기 전에** 기계로 확인할 것.
- **★결정을 옮기면 그 결정을 서술한 *모든* 표면을 같은 커밋에서 쓴다 — 이 세션이 이 교훈을 적어두고도 3번 더 어겼다★.** 적어두는 것으로 행동이 바뀌지 않았다. 유일하게 통한 것 = **고치기 전에 기계적 전수 스윕**.
- **★그 전수 스윕에도 구멍이 있다 — `rg` 는 기본적으로 숨김 디렉토리를 건너뛴다★.** 첫 스윕이 `.claude/` 를 통째로 놓쳤고 하필 거기가 최악의 잔존 텍스트였다. **`--hidden` 필수**(+ `--glob '!.git'`). 그리고 **grep 은 산 주장과 인용부호 안 인용을 못 가른다** — 히트는 읽어서 판정할 것.
- **★`latest.md` 도 쓸어야 할 표면이다★** — 5R BLOCK 이 이걸로 났다. 다음 세션이 **가장 먼저** 읽으므로 결정이 바뀌면 여기부터 낡는다.
- **★"결정 당시 스냅샷이니 갈려도 결함 아님" 류의 면제 논리를 만들지 말 것★** — 3R BLOCK. *결과*(0줄·날짜)는 얼려도 되지만 ***명령*은 도구라 옳고 그름이 날짜와 무관하다.** 그 프레이밍이었으면 1R 의 옳은 명령 교체를 억눌렀을 것이다.
- **★발췌 문서를 정본으로 지목하지 말 것★** — 4R BLOCK. CLAUDE.md 는 스스로 "발췌" 라 밝히고 net 게이트 4·5 명령이 없다. 그리로 보내면 세션이 자기적중 함정 패턴(`_(이름)` 괄호형)을 네 번째 파일에 복붙한다.
- **★`crate::` 만 무는 grep 은 미탐이 있다★** — `control/mod.rs` 에서 `super::` 는 크레이트 루트라 같은 간선이 빠져나가고, rustfmt 가 쪼갠 그룹 import(`use crate::{`⏎`  connection_core::A,`)는 행 단위 패턴을 빠져나간다. **`(crate|(super::)+)` + `-U`.** 이 repo 엔 `rustfmt.toml` 이 없어 기본 래핑이 그 형태를 만든다.
- **★양성 대조가 죽은 갈래를 못 잡을 수 있다★** — net 실간선이 전부 `crate::` 형태라, 경로를 넓히는 대조는 `super::` 갈래가 죽어도 통과한다. **대조 통과로 패턴 축소를 승인하지 말 것.**

**승계 do-not (여전히 유효 — 실측 이력 있음):**

- **★산문이 코드보다 훨씬 자주 틀린다 — 6세션 연속★.** 대응 = 요약 문장을 다시 쓰지 말고 **걷어내라**. 모든 문장은 ① 실행 가능한 명령 + 기대값 ② 포인터 ③ 수량어 없는 문장 셋 중 하나.
- **★썩은 개수는 갱신이 아니라 삭제★** — 정본 포인터로 대체.
- **★게이트를 새로 만들면 그 자리에서 qa 바인딩에 등록★** · **게이트 텍스트는 `_(괄호)` 형태로**(자기적중 함정).
- **★파일 편집 워커와 `tauri dev` 동시 금지★** — 저장마다 리빌드로 CDP 타깃이 `about:blank` 가 된다. GUI 실측은 편집 끝나고.
- **★`/adr index --write` 전에 반드시 `index --check`★** — 상태줄 개정 도장이 2개인 행에서 둘째를 조용히 지운다(알려진 결함).
- **★cross-family 리뷰어(codex) 프리앰블 필수★** — "너는 오케스트레이터가 아니다 + 환경 스킬·에이전트 정의 전부 무시 + 되묻기 불가" 를 맨 앞에. 호출형 = `MSYS_NO_PATHCONV=1 codex exec --sandbox read-only -c model_reasoning_effort="high" "$(cat <prompt>)" < /dev/null > <log> 2>&1`.
- **★워커가 끊기면 `SendMessage` 로 이어붙인다★** — 새로 스폰 금지. **델타 재리뷰에 특히 잘 통한다**(이번 세션 4회 연속 사용, 전부 유효).
- **★병렬 워커는 빌드 권한을 한쪽에만★**(`.cargo-lock` 충돌) · **★프론트·src-tauri 에 `file:line` 포인터 금지★**(심볼로. **Rust crate 도 안전하지 않다**).
- **커밋 전 `git checkout -- crates/engram-dashboard-protocol/bindings/`** — build/test 가 ts-rs 로 23파일을 **줄바꿈만** 더럽힌다(내용 diff 0 — 이번에도 재확인).
- **`git add -A`/`commit -a` 금지** · bare `cargo test` 금지 · **Fable 워커 금지** · **Agent 스폰 시 model/프리셋 명시 필수** · `.gitattributes` 임의 추가 금지 · **git-bash `taskkill /PID` 는 `MSYS_NO_PATHCONV=1` 필요**.
- **GUI 실측 함정:** xterm 은 canvas 라 `innerText` 0 · `cdp.mjs eval` 길면 조용히 깨짐 · **cwd 를 셸 경유로 넘기면 백슬래시가 먹힌다**(이번에 그 산물 3건 정리함) · 자기가 띄운 **PID 트리째** 정리, **공유 데몬 불가침**.

## 정지 조건

- **ADR 본문 수정 = 사용자 승인.** (이번 세션은 "코드 수정 아니면 알아서" 로 **일회 위임받아** 진행했다 — 기본값은 여전히 승인 필요.)
- **데몬 crate 추가 분리 착수 = 사용자 결정**(ADR-0130 보류 — 재개 조건 관측 선행).
- **wire JSON 변경이 필요해지면 멈추고 재론.**
- **프로덕션 코드에 test-only 훅 추가 = 사용자 결정.**
- **리뷰 정면 대립은 메인이 근거로 갈랐다면 올리지 않는다** — 못 가릴 때만.
- **푸시는 사용자 지시 시에만**(커밋은 자유).

## 사용자 판정 대기 (실제로 열린 것 — 1건뿐)

- **`net → core` 간선 존폐** — **코드 변경이라 문서 위임 밖**이다. 대상은 `agent::platform::` 아래 프로세스 liveness 헬퍼 2개, 비차단 판정 — 그냥 둬도 막히는 것 없음.

*(S18.22 이월 2건 — 격리 게이트 3문서 중복 · `architecture-overview` net 문단 — 은 `e111f8b` 에서 **종결**됐다. 옛 핸드오프가 "대기" 라 적은 건 낡은 것.)*

## 열린 것 (다음 후보)

- **★CLAUDE.md 수정 6건 — 조사 끝, 착수만 남음★**(위 §감사 결과). 1번(§5)만 문단 재작성이고 나머지 5건은 단순 정정. **1번 착수 전 ADR-0035·0055·0057 을 읽을 것**(제어 표면의 실제 경계).
- **★wire 타입 빌드타임 배선 — 방향 사용자 확정★.** 생성 바인딩 23개 중 프론트 소비 1개. 선례 = 레이아웃 타입의 `src/api/layoutTypes.ts` 파사드 재수출(ADR-0035). **★착수 전 주의: 그 작업이 고치려는 두 경로(`src/api/wsTransport.ts`·`scripts/engram.mjs`)가 지금 테스트 0건이다 — "관측점 먼저" 로 들어갈 것.★** 이름 정책(`AgentCommand` 3중 충돌)은 그때 사용자 결정 사항.
- **ADR-0130 재개 조건 ③(순환 탐지) 스크립트** — grep 으로 안 되고 `#[cfg(test)]` 경계 파싱 필요. 만들지 여부는 사용자 결정.
- **CLAUDE.md 주석 규약 자리** — 재료(clean-comment 기준 v2)는 생겼으나 **wt2 세션 소관**(사용자 지시).
- 승계분: `reply_failed` 조회 표면 부재 · `profiles_cb` 주입 잔존 · `--disallow-mcp` 존폐 · ws_e2e `case09` flaky(임계값 튜닝 금지 — ADR-0038) · 허수 테스트 1건 · `architecture-overview.md` mermaid 노드가 net 모듈 책임을 나열(헤더의 "모듈별 책임은 각 파일 헤더가 정본" 과 충돌 — 리뷰어가 note 로만 남김).
- **스킬 피드백 적립됨:** review(`light` 강도 선택이 doc 에서 빗나감 + light+BLOCK 이 정지 조건에 걸려 그 턴에 못 닫히는 상태 표현 부재).

## 참조 (읽어야 할 것만)

- **흐름:** `docs/process/step-log.md` **S18.25** — 이번 세션 전말(쟁점 종결·리뷰 5라운드·루트 원인).
- **결정 정본:** `docs/decisions/0130-*.md` — §영향의 **"등록 상태" 줄**(②만 등록 — 이 줄이 정본) · 재개 조건 3개 · §근거 ③의 재확인 명령.
- **게이트 정본:** `crates/engram-dashboard-net/src/lib.rs` 헤더 — 다섯 게이트를 전부 담은 유일한 곳. 역할 분담 서술 = `docs/testing-strategy.md` §net.
- **실행 게이트:** `.claude/skill-bindings/qa.md` — 강도별 실명령 + ADR-0130 재론 트리거(판정 규칙이 다른 유일한 블록).
