# 핸드오프: Cline 시각 충실도 + thinking 활성화 완결 (커밋 82a8108·8b844ed, 푸시 미완) · opus thinking = 업스트림 한계

## 한 줄 상태 · 다음 첫 액션
- **상태:** 채팅 렌더 Cline 시각 충실도(base layer CSS 복원·badge 유저박스·usage 칩 제거) + JSON 모드 thinking 기본 활성화(ADR-0049) **전부 게이트 통과·커밋 완료**. 이전 세션 미커밋분(레이아웃 재구성·MarkdownBlock 견고성·NOTICES)도 함께 커밋됨.
- **다음 첫 액션:** ① `git push origin master` — **푸시는 권한 차단으로 미실행**(기본 브랜치 직푸시, 사용자 승인 필요). 로컬 2커밋 ahead. ② 사용자 실화면 확인(앱 실행 중) ③ 잔여 항목(아래) 중 우선순위 결정.

## 커밋 (로컬, 미푸시)
- `82a8108` **feat(ADR-0048): 채팅 렌더 Cline 시각 충실도** — cline.css base layer 복원(행간 1.25·h1~h4 스케일·ul/ol 불릿·th 가시화·pre 20px·문단 마진 net 재현) + StructuredTextView 루트 13px/1.25·badge 유저박스(3테마 `--badge-bg/fg`)·usage 칩 제거 + MarkdownBlock 견고성(stripZeroWidth)+marked 의존성 제거 + THIRD_PARTY_NOTICES + 테스트(vitest 242).
- `8b844ed` **feat(ADR-0049): JSON 모드 thinking 기본 활성화** — claude.rs json 모드에 env `MAX_THINKING_TOKENS=8000` 기본 주입(프로필 우선 explicit-skip·대소문자 무구분·터미널 모드 미주입) + 회귀 테스트 4 + ADR-0049·인덱스·step-log 2026-07-06 섹션.

## ★핵심 발견 — opus thinking은 업스트림 한계 (재디버깅 금지)★
- env 주입은 **끝까지 실증됨**(자식 프로세스 env 실존 + thinking 블록 방출). 그러나 **opus-4-8은 headless stream-json에서 thinking을 암호화(signature-only)로만 방출 — 평문 `"thinking":""`**. `--include-partial-messages`의 thinking_delta도 평문 없음(스파이크 실측). UI는 설계대로 빈 reasoning 억제 → **opus 에이전트에서 thinking이 안 보이는 건 정상이며 우리 코드로 해결 불가**.
- **sonnet-4-6은 평문 방출** → ThinkingRow 접힘/펼침 라이브 실측 PASS(`qa-thinking-sonnet.png`). thinking 보고 싶으면 프로필 extra_args `--model claude-sonnet-4-6`.
- 후속 정책 선택지(사용자 결정): 빈 thinking도 "Thinking (내용 비공개)" 식 표시 vs 현행 억제 유지(Cline 충실).

## repo 상태 (브랜치 master, origin보다 2 ahead)
- **미커밋 — 의도적 유지(게이트 미통과 echo 작업):** `crates/.../backend/claude.rs` 잔여 hunk(user_text_echo_json·디코더 text 억제·테스트 6) + `agent/session.rs` + `backend/mod.rs` — 입력-시점 유저 에코 기능, **리뷰/QA 안 거침**. 커밋하려면 별도 라운드.
- **미커밋 — 무관:** `Sidebar.tsx`, `run-dashboard-clean.bat`, `structuredToRichMessages.ts`(오펀·RichSlot에서 미사용), `docs/reference/architecture-overview.md`(미추적), `phase2-*.png`·`qa-*.png`(스샷 아티팩트 — 정리 대상 후보).

## 검증 상태 (쌍)
- **돌린 것(PASS):** `/review code full`(doc-aware opus + Codex, 재수정 2회 반영) · `/qa full` — cargo 멤버별 전부 green(core 143+16·protocol 42·discovery 44·daemon 82), tsc·vitest 242·build, **cdp 실측 11 단언 PASS**(제목 스케일·행간·불릿·th·pre 20px·badge 3테마·usage 부재·13px), ThinkingRow 라이브(sonnet). ADR-0049는 `/review doc light` fact-check 통과. 빈 pre 직사각형 = fresh 대화 재현 0건(구 펜스함정 잔재로 종결).
- **재실행:** `npx tsc --noEmit` · `npm test` · `cargo test -p engram-dashboard-core` · cdp = `node scripts/cdp.mjs eval|shot`(9223).
- **검증 안 된 것(정직):** ① 루트 `cargo test`는 src-tauri 테스트 바이너리가 DLL 로더 실패(STATUS_ENTRYPOINT_NOT_FOUND, WebView2Loader — **선재·변경 무관**)로 중단 — 멤버별로 돌 것. 바인딩 드리프트로 보고됨. ② 격리 게이트 `rg "use tauri"`는 lib.rs:9 **주석 문자열** 1건 매치(가짜양성, 실제 import 0 — 주석 리워딩 후보). ③ opus thinking 가시화(위 한계).

## 함정 (do-not — 이전 핸드오프 계승 + 신규)
- **opus thinking 재디버깅 금지** — 위 ★핵심 발견★. 우리 파이프는 정상.
- **재기동 함정(계승):** 포트 1420 vite node + `engram-dashboard.exe` + **데몬**(`engram-dashboard-daemon`, portfile `.engram-data/daemon.json` pid) 셋 다 kill 후 재기동 — 구 데몬이 살아있으면 새 백엔드 코드가 안 탄다.
- **Json 프로필 id 변경:** 구 `181e99d7`은 QA 중 소실(kill이 프로필까지 purge — 비영속 프로필) → 동등 재생성 `8690560a`(name "Json") + `JsonSonnet`(sonnet-4-6) 추가. 이전 핸드오프의 181e99d7 레시피는 폐기.
- **마크다운 렌더 테스트는 자연 프롬프트로**(계승 — "마크다운만 출력해줘"는 코드펜스 랩 함정).

## 앱 상태
dev 앱 실행 중(백그라운드 task `bta3e901t`, CDP 9223) — Json·JsonSonnet 에이전트 Running, JsonSonnet 슬롯에 thinking 실측 대화 있음. 종료 무방(재기동은 위 함정대로).

## 잔여 (우선순위는 사용자)
1. **push** (로컬 2커밋).
2. **echo 작업 게이트**(claude.rs 잔여+session.rs+mod.rs) — 리뷰/QA 후 커밋 or 폐기 결정.
3. replay 빈 버그(ADR-0046 별건) 조사.
4. opus thinking 표시 정책(빈 thinking 표시 여부) — 사용자 결정.
5. 스샷 아티팩트(phase2-*·qa-*) 정리, `structuredToRichMessages.ts` 오펀 제거 검토.

## 참조 (읽을 것만)
- `docs/decisions/0049-*.md`(thinking 결정·한계 근거) · `docs/process/step-log.md` 2026-07-06 섹션(전 과정) · `src/components/slot/cline/cline.css`(base layer 복원분) · `qa-thinking-sonnet.png`(실측 증거).
