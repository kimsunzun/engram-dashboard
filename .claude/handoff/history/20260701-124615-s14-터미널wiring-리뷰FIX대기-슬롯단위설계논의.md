# 핸드오프: S14 모듈③ 터미널 wiring 코더완료·리뷰FIX 반영대기(미커밋) + 슬롯단위 설계 논의

> master, HEAD = **09ee5b8**(이번 세션: CLAUDE.md 용어 규약 격상). 터미널 wiring은 **미커밋 working tree**(리뷰 FIX 반영 전이라 커밋 불가). `.claude/continue` gitignored(로컬).

## 한 줄 상태 + 다음 첫 액션
메인창 슬롯 stub → `TerminalSlot` 실연결(모듈③ 1차 = 출력 가시화) 코더 구현 완료, `/review code full` = **FIX 판정**. 미커밋 상태.

**다음 첫 액션:** 리뷰 FIX 3건 반영(코더) → 리뷰 재확인(반영분만) → **cdp 실측**(실제 화면에 터미널 출력 뜨는지) → 커밋.

## repo 상태
- HEAD = **09ee5b8** (master). 이전 = ffcd766(출력 평면 2단계).
- **미커밋 working tree (터미널 wiring — 다음 세션이 이어받을 것):**
  - `M src/components/layout/ViewLayoutRenderer.tsx` (slot stub→TerminalSlot 분기 교체 + 평면B known-limit 주석)
  - `?? src/components/layout/ViewLayoutRenderer.test.tsx` (신규 렌더분기 테스트)
  - `M vitest.config.ts` (`*.test.tsx` include + `@vitejs/plugin-react`)
  - `M package.json / package-lock.json` (devDep: `@testing-library/react`, `jest-dom`)

## 이번 세션 완료
- **CLAUDE.md 용어 규약 격상 (커밋 09ee5b8):** 묻혀있던 "설명 추상화 레벨" 불릿 → `### 용어 추상화` 소제목(예제 `<examples>` + 혼동쌍 고정용어: 에이전트/클라이언트/데몬 재시작 · 웹뷰/컴포넌트/슬롯). self 점검만(사용자 직접 검토).
- **터미널 wiring 코더 구현** (general-purpose sonnet): `agent_id != null` → `<TerminalSlot agentId>` 마운트, null → "— empty —" 유지. 렌더분기 테스트 7~8개.
- **리뷰 code/full** (opus reviewer-deep doc-aware + Codex blind): **FIX**. 빌드 green, 불변식(ADR-0011 seam / 0035 레이아웃 권위) 위반 없음.

## 리뷰 FIX (반영 대기 — 다음 첫 작업)
1. **key 추가:** `<TerminalSlot key={node.agent_id ?? node.id} agentId={node.agent_id} />` — React 정체성 모호 제거(1줄).
2. **죽은 jest-dom 제거:** `@testing-library/jest-dom` 미사용(setupFiles 미등록) → devDep 제거(react만 남김).
3. **구독 번호표 가드 (stale-unsubscribe):** `protocolClient.ts` unsubscribe(306)가 agentId로만 `subs.delete` → 재구독(에이전트 재시작=epoch 교체 / dev StrictMode) 시 늦게 온 옛 취소가 새 구독 삭제 → "재시작 후 빈 터미널". SubState에 owner token 필드 + unsubscribe 가드("현재 subs 엔트리가 내 토큰일 때만 delete"). handleOutput/resubscribeAll 무변경 → 작고 안전. **사용자 A안 채택(지금 고침).**

## 미결 결정 · 다음 슬라이스
- **평면 B (같은 창 같은 agent 다중 슬롯):** 현재 출력 경로는 **(창, 에이전트) 단위** — `output_channel.rs:48` `ViewSlotKey=(WindowLabel, AgentId)`, **slotId 없음**. 같은 창 다중 슬롯이 백엔드 cursor·프론트 subs 공유 → 충돌. 방향 미정: **막기**(같은 창 중복배정 금지, 작음) vs **슬롯 단위 재설계**(중간). 재평가: **데몬 무변경**(agent frame 슬롯 무관) + src-tauri generic 키 `S=(창,슬롯,agent)` 확장 + 프론트 subs 다중콜백 + cursor/dedup/replay/deliverable **불변식 재검증**이 진짜 비용. "데몬부터 재설계"는 과장이었음. (`sync_viewers`/replay 정밀 확인은 다음)
  - **슬롯 키 형태(통찰):** 슬롯 id는 고유 Uuid(레이아웃 말단, `ViewLayoutRenderer` node.id)라 **슬롯 하나가 창·agent를 유일 결정** → 키에 slot id 하나면 충분(창·agent는 파생), `(창,슬롯,agent)` 3-튜플은 과잉. 단 deliverable 게이트가 **창 단위**(registered_labels=window 집합)라 `(창, 슬롯)` 2-튜플이 실용적일 수 있음(창 정보를 키에 남기면 게이트 직접 매칭, agent는 값에 품음). fan-out 조회(`slots_for_agent`)는 키가 아니라 값의 agent_id로 전체 순회 필터라 **키 조각 수와 무관**(현 선형 스캔 그대로). 최종 키 모양은 게이트 매핑 방식에 달림 — 구현 시 결정.
- **FreshFallback 복원실패 UI 알림:** resume 실패 → 새 세션(대화 날아감)이 **조용히 빈 터미널**. report는 백엔드→프론트 이미 전달(`restore-result`), 프론트가 `console.info`로만 버림(`eventBus.ts:134`). 슬롯 배너 표시만 추가하면 됨(배관 있음, 작음). 미룸.
- **ADR 앵커 부착 (이전 세션 이월):** ffcd766 코드에 `// ADR-0041~0043` 앵커 5곳. 위치 조사 완료 — `agent.rs:199`(0041) · `protocolClient.ts:298`(0041) · `output_router.rs:80`(0042) · `layout.rs:99`(0042) · `output_view_store.rs:158`(0043) · `connection.rs:1176`(0043). 부착만 남음.

## 검증 상태 (쌍)
### 돌린 것 (green)
- 터미널 wiring: 코더가 `npx tsc --noEmit` 통과 + `npm test` 135 green. 재실행 = 프로젝트 루트에서 그대로.
- CLAUDE.md: 커밋됨(09ee5b8).
### 검증 안 됨 (오신뢰 금지)
- **터미널 wiring cdp 실측 안 함** — 실제 화면에 터미널 출력 뜨는지 미확인. 코드 PASS ≠ 동작 보장.
- **리뷰 FIX 3건 미반영** — key/jest-dom/구독가드 아직 코드에 없음.
- 멀티윈도우 fan-out(평면 A) 화면 확인 안 함.

## do-not / 주의
- **Explore가 `ViewSlotKey` 이름에 속음:** "slot별 독립 cursor N개, 다중 slot 지원"이라 회수했으나 **틀림** — 실제 키 `(WindowLabel, AgentId)`라 (창,agent) 단위, 같은 창 다중 슬롯 미지원. 코드(`output_channel.rs:40-48`)로 확정. 이름 오해 주의.
- 리뷰 재확인은 FIX 반영분만 얕게(전면 재리뷰 불필요).

## 참조 (읽을 것만)
- 출력 단위: `src-tauri/src/output_channel.rs:40-48`(ViewSlotKey) · `crates/engram-dashboard-core/src/output_view_store.rs`(content/cursors/on_frame/deliverable).
- 구독 근원: `src/api/protocolClient.ts`(subs=52 · subscribeOutput=297 · unsubscribe=306 · handleOutput=122) — stale-unsubscribe·평면B 근원.
- 대상/연결: `src/components/layout/ViewLayoutRenderer.tsx`(미커밋) · `src/components/slot/TerminalSlot.tsx`(`[agentId,epoch]` 재구독).
- ADR: 0035(레이아웃 권위) · 0040~0043(출력 평면) · 0007(epoch) · 0011(seam).
