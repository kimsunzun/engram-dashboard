# 핸드오프: `core`→`agent` 개명 리뷰 `FIX` 수령 — 후속 정리 9건 뒤 푸시

> **앞 기록 `20260825-201317-*.md` 를 대체한다.** 그 문서는 "리뷰 결과 미수령 → 다시 돌려라"라고 적었는데, **리뷰어가 그 뒤 완주해 결과가 왔다.** ★리뷰를 다시 돌리지 말 것★(약 175k 낭비).

## 한 줄 상태 · 다음 첫 액션

**ADR-0175 5단계 중 1단계(개명) 코드는 통과, 문서·주석 후속 정리 9건이 남았다.**
**다음 첫 액션 = 아래 §후속 정리 9건을 코더에 넘긴다.** 그다음 QA → 푸시 → CI.

## repo 상태

- **브랜치 `v0.3.0/refactor/crate-boundaries`** (master `5b1e499` 에서 팜). 작업 트리 **깨끗**, stash 없음.
- `266c6f3` — ADR-0175 박제 + 번호 선점. **푸시됨.**
- `8be3b5b` — 개명 본체(112파일 +574/-596). ★**미푸시**★ — 아래 정리 9건을 얹은 뒤 민다.
- 별건: `v0.3.0/chore/misc` 에 스킬 피드백 2커밋 푸시됨.

## 리뷰 결과 — `code light` / doc-aware Adversary, 판정 **FIX**

**핵심 통과.** 독립 2패스 교차검증에서 **의미 변화 0건**:
- 숫자 리터럴·조건·인자 순서·문자열·가시성·`mut`/`async`/`.await`·ADR 앵커·doc 주석 변화 **0**
- Rust 78파일의 46종 토큰 개수 완전 일치 · 리터럴 다중집합 파일별 일치 · `///`/`//!` 줄 수 일치
- ci.yml 임계값(`-ne 1`·`-ne 2`·`-ne 3`·`-gt 64`) 전부 불변
- 사라진 파일 = `src/agent/mod.rs` 하나뿐 · 공개 표면 297종 대조 차이 = `pub mod agent` 소멸 1건뿐

**게이트 생사 판정 — 하나 빼고 전부 「살아 있다」:** tauri import 0 · 메시징 격리 정규식(알파벳에 `agent` 등재, 실측 0줄) · net 게이트 1·2·3·4·5(2는 `engram_dashboard_agent::` 재조준 + 기대값 2 유지, 실측 정확히 2 — **0 으로 내렸다면 죽었을 자리**) · messaging·command 의존 상한 · ts-rs 바인딩 sync **두 줄 모두** 이동 확인 · agent 단독 테스트 스텝.

## 후속 정리 9건 (전부 문서·주석 — 로직 아님)

1. ★**`.claude/skill-bindings/qa.md:125` 경로 부재 가드가 fail-close 안 된다**★ — 출력엔 "FAIL" 이 뜨지만 **종료코드는 항상 0**. 이 세션에서 새로 넣은 가드인데 반쪽이다. 그 파일의 판정 규약(출력 유무로 읽는다)에는 맞지만 종료코드로 읽는 소비자에겐 통과로 보인다.
2. `qa.md:130` 실행 채널 열거가 개명 반영 안 됨.
3. **ADR-0175 오류 ①** — 「영향/불변식」이 net 게이트 2를 `engram_dashboard_agent::` **0 기대**로 재조준하라고 적었는데, **실제로는 2 를 유지하는 것이 옳다**(net 이 그 두 심볼을 계속 쓴다). 커밋이 옳고 ADR 이 틀렸다. **결정이 아니라 사실 오류이므로 본문 정정 가능**(ADR 덮어쓰기 금지는 *결정 변경*에 걸리는 조항이다).
4. **ADR-0175 오류 ②** — 「관련」줄이 `docs/refactoring/core-split-plan-2026-08-25.md` 를 참조하는데 그 문서는 이 세션에서 **삭제했다**(ADR 이 정본이 되면서 출처 이중화를 피하려고). 참조를 걷는다.
5. `CLAUDE.md:123` — *"옛 기록의 `core` = 이 crate"* 라는 취지의 문장이 **거짓**이다. 이 저장소의 맨 `core` 는 「코어 격리」(원칙 이름)·`OutputCore`·`connection_core` 등 다른 뜻으로도 쓰인다.
6. `CLAUDE.md:194` — *"아래 core 줄"* 포인터가 개명으로 끊겼다.
7. `crates/engram-dashboard-net/Cargo.toml:131` — 죽은 경로 `agent::platform` 을 지시한다. **같은 crate 의 `lib.rs` 헤더는 `platform::` 으로 고쳐져 있어 두 기록이 어긋난다**(그 헤더가 게이트 기대값 정본).
8. 매니페스트 **현재형** 주석의 맨 `core` 잔존 약 17곳. (역사 기록은 제외 — 아래 do-not)
9. `docs/testing-strategy.md:98` 반쪽 개명.

**별도 판단 1건:** `ci.yml:714` 에 **개명이 아닌 내용 변경**이 섞였다 — 라이브러리 crate 목록에 `command` 가 새로 들어가고 "5종"이 빠졌다(원래 누락의 정정, 실행 무영향). ADR-0175 「검증 잣대」가 이 커밋의 검토 기준을 *"이름 말고 바뀐 게 있나 → 없다"* 로 못 박았으므로 그 기준으로는 위반이다. **그대로 둘지(정정이 맞으니) 별 커밋으로 뺄지 사용자 판단.**

**수용:** `crates/engram-dashboard-agent/src/manager.rs:736-738` — 경로가 짧아져 rustfmt 가 match arm 몸통을 중괄호로 감쌌다(후행 쉼표 소멸). 리플로우 훅 안의 유일한 구조 변화이고 무해.

## 검증 상태

**돌린 것:** `cargo test --workspace -- --test-threads=4` → **2,102 통과 / 0 실패**(변경 전과 동일) · `cargo build` · `cargo fmt --check` · 격리 게이트 11종 실측.

**검증 안 된 것:**
- ★**위 2,102 는 코더 자기보고이고 리뷰어가 재현하지 않았다**★ — 리뷰는 정적 대조만 했다. QA 단계에서 다시 돌릴 것.
- **CI 미실행**(미푸시).
- **GUI 실측 안 함.**
- 리뷰 2패스의 사각: 매크로 토큰 트리 내부 공백 의미 변화 · `r#"…"#` 다중 `#` 문자열 · `.md` 들여쓰기 변화.
- `qa.md` 새 가드의 fail-close 여부 = 위 1번(미검이 아니라 **깨진 것으로 확인됨**).

## 다음 할 일 (ADR-0175 실행 순서)

1. 개명 ← **여기.** 정리 9건 → QA → 푸시 → CI 초록.
2. **`base` 신설** — `logging`(692줄) + `platform/process.rs` PID 헬퍼(325줄). **Job Object(`platform/windows.rs`)는 `agent` 잔류**(소비자가 agent 안뿐). 딸려 이사 = `regex`·`tracing-subscriber`·`windows`. 가시성 승격 불필요(`pub(crate)` 0건 실측). 헤더에 **입주 조건 3개**(소비자 2 이상 · 도메인 지식 0 · 서로 참조 0). 게이트를 **세 곳**(ci.yml · CLAUDE.md · qa.md)에 등록.
3. **`replay_flight` → 셸 모듈** — 4줄 재수출을 실물로. 헤더의 죽은 근거("headless 에서 테스트가 실행된다" — ADR-0174 로 죽음)와 `daemon/src/connection_core.rs:1494` 주석 경로도 같이.
4. **`ui.` 접두** — 웹뷰가 답하는 명령에만. ★착수 전 사용자에게 적용 범위를 물을 것★(미확정).
5. **`COMMAND_SPECS` 간선 제거 + 충돌 시 앱 표면 고지** — 4번이 끝나야 가능.

## 정지 조건 (사용자에게 물어야 하는 자리)

- **4단계 `ui.` 접두의 적용 범위** — 미결. 사용자 결정.
- **`ci.yml:714` 처리**(그대로 둘지 별 커밋으로 뺄지) — 위 별도 판단 1건.
- **`base` 에 세 번째 입주자를 넣고 싶어질 때** — ADR-0175 「거부한 대안」 첫 항목을 다시 여는 지점(리뷰어 둘 다 `base` 묶음에 반대했고 사용자 판단으로 채택했다).

## 실패한 접근 / do-not

- ★**`git stash pop` 충돌을 `git add` 로 삼키지 말 것**★ — 이번 세션에서 실제로 저질렀다. 충돌 마커 6줄이 커밋·푸시됐고 후속 커밋으로 걷었다. **리팩터 브랜치와 `chore/misc` 는 피드백 파일 이력이 갈라져 있어 그 사이 이동은 항상 충돌한다.**
- **역사 기록의 `core` 를 고치지 말 것** — `docs/decisions/` 본문 · `docs/process/step-log.md` · `.claude/handoff/history/` 는 append-only 다. 그때는 그 이름이 맞았다.
- **`--exclude engram-dashboard` 붙이지 말 것** — 2026-08-25 에 걷혔다.
- **net 게이트 2 를 「0줄 기대」로 내리지 말 것** — 죽은 게이트가 된다(위 정리 3번이 그 오지시를 걷는 일이다).
- **codex(blind) 리뷰어를 전경으로 띄우지 말 것** — Bash 기본 10분 타임아웃에 죽는다. 처음부터 백그라운드로.
- **리뷰를 다시 돌리지 말 것** — 이미 받았다(이 문서 §리뷰 결과).

## 참조 (읽어야 할 것만)

- `docs/decisions/0175-코어를-agent-와-base-로-가른다.md` — 이 작업의 정본. 「영향/불변식」이 남은 게이트 작업 목록(단 위 정리 3·4번 오류 포함).
- `docs/decisions/0151-*.md` §영향 — 「덩어리별로 따로 판정한다」. 0175 가 여기서 갈라졌고 그 사실이 0175 에 명시돼 있다.
- `CLAUDE.md` 「빌드·검증 명령」 — 재실행 명령 정본.
- `crates/engram-dashboard-net/src/lib.rs` 헤더 — net 게이트 기대값 정본(개명 반영 완료).

## 이어가기 프롬프트

```
engram-dashboard 리팩토링 이어간다. /handoff load 로 맥락 받아라.
개명 커밋 8be3b5b 는 리뷰 FIX 를 이미 받았다 — 리뷰를 다시 돌리지 말고,
핸드오프의 「후속 정리 9건」을 코더에 넘겨 처리한 뒤 QA → 푸시 → CI 확인,
그다음 2단계 base 신설로 간다. ci.yml:714 처리는 나한테 물어라.
```
