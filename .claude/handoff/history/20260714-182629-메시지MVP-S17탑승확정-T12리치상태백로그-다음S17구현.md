# 핸드오프: 메시지 MVP="전송 길만"(S17 탑승) 확정 + T-12(리치 상태) 백로그 저장. 다음 = S17 구현 세션(메시지 시나리오 포함)

> ⚠ S17 병렬 스레드와 latest.md 공유 중. 이 파일은 **메인(구 ADR-0083/0084) 스레드**의 후속 — 오늘 이 스레드는 리서치·스코프 결정만 하고 코드 0. S17 설계 승계는 history `20260714-141054-s17-*` 로드.

## 한 줄 상태 · 다음 첫 액션
- **상태:** ① "wait" 표시 정식 재설계 리서치 완료 → **T-12**로 백로그 박제(착수 보류) ② 메시지 시스템 스코프 **사용자 확정** = "전송 길만 뚫기"(풀 메일박스 보류) → **T-13** 박제. 코드 변경 0(문서 2건: tracking.md T-12/T-13).
- **다음 첫 액션:** **S17 구현 세션** — S17 핸드오프(`20260714-141054-s17-*`) 로드 → 인터페이스 상세 표 작성(**message-send 수용 시나리오 포함**: 에이전트A Bash→engram-ctl→WriteStdin→에이전트B 유저 턴 수신) → `/review trd` 재검 → 구현+TDD. 그다음 = resume-wait 버그(아래).

## 이번 세션이 정한 것 (근거는 tracking.md T-12/T-13이 정본)
- **T-12 (JSON 라이브 상태 표시 재설계):** 리서치(medium, 설계-결정 모드 + codex 적대리뷰 BLOCK→반영) 완료. 사용자 방향 = 리치(gemini-cli급, "뭘 하는지" 노출). **선결조건**(--include-partial-messages 미활성·turn_id 부재·Error 판별자·경과 5시계 의미 미정의·done fallback=목록제거만)이 tracking.md T-12에 전부 박힘. 착수 = resume-wait 버그·메시지 후.
- **T-13 (메시지 시스템):** MVP = 전송 길만. **미구현 조각 = engram-ctl뿐**(JSON write_input이 이미 평문→stream-json 유저 턴 wrap + 합성 에코, `session.rs:106`/`session.rs:338`). 별도 PRD 불필요 — S17 인터페이스 표에 시나리오로 탑승. TRD 때 얇은 결정 2개(발신자 표기 규약 · busy 시 stdin 큐잉) = 사용자.
- **성숙도 정정(중요):** 메시지 시스템은 독립 PRD/TRD **없음**(사용자가 "TRD까지 됐다"고 기억했으나 S17 TRD와 혼동). 있는 건 서베이 2건 = `docs/research/agent-messaging-survey-2026-06-28.md` · `orchestration-survey-2026-07-12.md`(§6 "메시징 단독은 용처 적음" — 조율자=LLM이 engram-ctl로 해소).

## resume-wait 버그 (보류 중 — 메시지 다음)
- 증상: resume 후 JSON 모드 "Wait … Ns" 카운트 안 멈춤. 구조 파악됨: WaitRow(임시 스톱워치)는 `streaming = awaiting || (!turnDone && items>0)`(`RichSlot.tsx:162`)이 참인 동안 마운트 — **버그 = turnDone이 done으로 전이 실패해 streaming 고착**. epoch 미bump 원인은 ADR-0084로 수정됐는데 버그가 남아있다면 **다른 원인** — 조사 서브에이전트로 근본원인부터(ADR-0038, 솔로 추측 금지). 수정 자체가 T-12의 최소판(A)과 같은 지점.

## repo 상태
- **미커밋(내 것):** `docs/tracking.md`(T-12/T-13 추가) — 문서 only, 커밋 대기.
- **남의 것(S17 병렬 — 건드리지 마라):** `.claude/handoff/latest.md`(이 save가 덮음—공유 hotspot) · `docs/reference/architecture-overview.md`.
- **정리 대기(throwaway, 삭제=사용자 확인):** repo 루트 `*.png` 7개 · `scripts/qa-daemon-probe.mjs` · untracked handoff history들(정상, git 추적 여부는 관례대로).
- 최근 커밋: `27898f1`(ADR-0083/0084, 푸시됨).

## 검증 상태 (쌍)
- **한 것:** T-13의 write_input wrap 계약 = 코드/테스트 직접 확인(`session.rs:338` 테스트 존재 확인, 실행은 안 함 — 기존 green 스위트 소속). 메시지 시스템 성숙도 = Explore 서브에이전트 전수 탐색으로 확인. T-12 리서치 = 수집자 3(sonnet) + codex 적대리뷰 1회.
- **안 한 것:** 코드 변경 0 → build/test 해당 없음. resume-wait 버그 재현·조사 0. S17 구현 0.

## do-not / 주의 (이전 세션 승계분 유지)
- **member-scoped 테스트만**(bare `cargo test` = WebView2 크래시). **커밋 시 `git status`로 내 파일만 stage**(S17 병렬). **win32 커밋 메시지는 `-F <file>`**. **GUI 검증 전 데몬 kill→재빌드→재기동**(옛 바이너리 트랩). 앱 상태는 이전 핸드오프(`20260714-162045-*`) 기준 새 코드로 떠있음(PID는 stale 가능).
- S17 spec 문서는 S17 구현 세션에서만 수정(이 스레드에서 안 건드림 — 오늘도 안 건드렸음).

## 참조 (읽을 것)
- `docs/tracking.md` **T-12·T-13**(오늘 결정 정본) · S17 핸드오프 `history/20260714-141054-*`(구현 착수점·코드 앵커) · `docs/research/agent-messaging-survey-2026-06-28.md`(풀 메일박스 착수 때) · `latest.md` 직전판 = `history/20260714-162045-*`(ADR-0083/0084·qa 상태).
