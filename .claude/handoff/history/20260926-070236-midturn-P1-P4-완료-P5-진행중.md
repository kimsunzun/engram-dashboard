# 핸드오프: mid-turn input — 구현 P0·P1·P4 게이트 통과(push) · P5b WIP · P5a 코더·P4 로컬 QA 진행 중 · 남은 것 P5a·P5c·P3·P2·P7·P8·P6

(English body — user decision 2026-09-24. The user delegated TRD confirmation + implementation on 2026-09-26: 「나 자러갈테니깐 쭉쭉 진행해. 충분히 얘기한것같으니 구현 너가 알아서하고 후에 최종 보고하는걸로」. Master merge and `v*` tags are left for the user.)

## One-line state

Implementation per `.claude/handoff/attachments/20260926-midturn-impl/plan.md` (26 chunks). **Done and gated:** P0+P1 (`fcba626` — review deep PASS after fixes · CI green · local QA PASS) and P4 (`96ead26` — review deep PASS after fixes · CI green; local QA was running at save). **Coded, not gated:** P5b (`43618c0` WIP, local only — MUST land together with P5a). **In flight at save:** P5a coder (uncommitted edits in the tree) and the P4 local QA runner (isolated worktree). Remaining: P5a → P5c → P5 gate → P3a → P3b → P3 gate → P2a…P2f → P2 gate → P7 (M15) → P8 (only if M15 green) → P6 (docs + final QA) → final report.

## Next first actions

1. **Check the two in-flight workers' leftovers** (they die with the old session):
   - P5a coder: `git status` shows its partial edits (at save: `agent/src/{output_core,types}.rs`, `daemon/src/agent_conn.rs`, `protocol/src/{codec,lib,messages}.rs`, `protocol/tests/codec_golden.rs`, `src-tauri/src/daemon_client/connection.rs`). Either spawn a new P5a coder with the same brief to finish from the diff (brief = plan row "P5a Rust relay" + contacts "P5b" section), or discard those files and redo. Do not commit a partial P5a.
   - P4 local QA: if no result, re-run it (brief pattern: isolated worktree at `96ead26`, real-claude tests from CI `--skip` list, ADR-0130 trigger vs parent, GUI+CLI smoke with shell AND daemon built from that worktree — PROTOCOL_VERSION 6). Clean up any leftover worktree under the scratchpad (`git worktree list`, `git worktree prune`).
2. P5a done → P5c (UI list + reattach reconciliation; carried items in contacts) → P5 gate: `/review code deep` on `96ead26..HEAD` → fixes → squash (P5a+P5b+P5c, keep the WIP P5b inside) → push → CI → local QA (isolated worktree).
3. Continue P3 → P2 → P7 → P8 → P6 per plan §1/§2, same per-phase pattern.

## Working pattern (keep)

- Coders: `worker-senior` (critical) / `worker-scout` (simple); every brief starts with `.claude/handoff/attachments/20260926-midturn-impl/coder-brief-common.md`; landed shapes + carried review items are in `.claude/handoff/attachments/20260926-midturn-impl/contacts.md` (authoritative over plan §3). Each chunk ≈ 250–390k tokens, 70–110 tool calls, 15–25 min.
- WIP-commit each chunk by explicit paths; at the phase gate `git reset --soft <phase base>` → commit docs separately from code → push → CI (`gh run list --branch v0.3.2/feat/json-midturn-queue`).
- Review per phase = `/review code deep`: codex blind (`codex:codex-rescue`, `--fresh`, read-only, effort high, no `--background`) + Claude doc-aware (`worker-senior`) + one extra lens (concurrency / API contract). Re-verify fixes by `SendMessage` to the same reviewers (cheap, context kept).
- QA per phase: CI covers build/test; local = real-claude tests (CI `--skip` list) + ADR-0130 trigger + GUI/CLI smoke, always in an isolated worktree at the gated commit.

## Decisions made this session (authoritative records)

- User answers + design direction + delegation: `.claude/handoff/attachments/20260926-midturn-answers.md`; PRD 6판 §3/§6; TRD 8판 §10-1; ADR-0231.
- Orchestrator rulings (delegated): one mail gate = kernel `TurnFact.last_end_failed` for both backends and ALL versions (old codex also stops mail after failure — user-visible); interrupt neither sets nor releases; turn-end probe removed; late echo after turn end → bubble, no automatic answering turn; plan §6 decisions; P1 review: halt fold has its own cursor, finish does not ring `inputs_drained`; P4: errors advertised `[NOT_FOUND, CONFLICT]`, WS reply `QueuedInputCancelReply`.

## Repo state

- Branch `v0.3.2/feat/json-midturn-queue` (wt1). Pushed to `96ead26`. Local: `43618c0` WIP P5b (unpushed). Uncommitted at save: P5a partial edits (above), `contacts.md`/`plan.md` edits (docs — commit them), and line-ending-only noise in `src-tauri/bindings/*.ts` (no content diff — don't commit).
- Phase base for the P5 squash = `96ead26`.

## Verification state

- P0+P1: review deep (codex FIX → deferred by design to P5b; doc-aware FIX → fixed → PASS; concurrency lens FIX (lost halt) → fixed → PASS) · CI green `fcba626` · local QA PASS (real-claude 6/6, ADR-0130 matches all pre-existing, GUI smoke claude/codex/terminal unchanged).
- P4: review deep (codex PASS · doc-aware PASS · API lens FIX → fixed → PASS) · CI green `96ead26` · local QA: NOT confirmed at save.
- P5b: coder gates only (tsc + full vitest 1264 pass); NOT reviewed; not runnable without P5a (rejects 30-byte markers).
- Measurements: M9/M10/M13/M16 green. M15 NOT measured (needs P2e2).
- No user-visible feature yet: every backend still reports policy `None` until P3b (claude) / P2c (codex).

## Traps

1. TRD lines are 3–5 KB — never let a worker read it whole.
2. Builds/tests only via `scripts/run-detached.ps1`, judged by `__EXIT`; agent/daemon/base tests need `-- --test-threads=4`.
3. Phase 0 harness `.js` fails in-repo (ESM) — copy as `.cjs` outside.
4. Shared worktree: WIP-commit only the finishing worker's files; never `git add -A`; `src-tauri/bindings/*.ts` show line-ending-only "M" — leave them.
5. P5b + P5a must land in the same phase commit (marker 38 bytes; empty-ring `replay_from`).
6. A worker died once on an API SSL error — resume it with `SendMessage` (partial edits stay in the tree).
7. `nmfc-origin-dev-harness` hooks misfire every turn — ignore; this project's handoff is the user's `handoff` skill.

## Stop conditions (autonomous mode)

- `/review code` BLOCK, a direct reviewer contradiction, or the implement loop cap → stop that phase, leave it for the user.
- M15 red → no P8, keep `HandOverPolicy::Immediate` (decided; report it).
- Never: master merge, `v*` tag push, history rewrite/force-push, touching wt2 or apps not started by us.

## For the final report (collected)

- ★**Privacy:** the repo is PUBLIC. The first push of this branch (2026-09-26) published older committed Phase 0 raw logs (`.claude/handoff/attachments/20260925-midturn-phase0/logs/claude-M*.jsonl`, `codex-M6/M7` …) containing the OS account name (= GitHub handle) and internal plugin names, and a 2026-09-25 handoff mentioning the company account domain. master already had similar content (fixtures `claude/fixtures/claude_{text,tool}.jsonl`, `.claude/handoff/attachments/codex-control-2026-09-16.md`, a June handoff). Options for the user: scrub forward (new commit) and/or history rewrite + force-push (destructive — user decision). New logs/fixtures from this session are scrubbed.
- Old codex now also stops mail after a failed turn (delegated ruling R1).
- Late echo after turn end → bubble but no automatic answer (differs from what was said when N12 was explained).
- ADR-0130 trigger has pre-existing production matches (commands.rs, mcp_server.rs, registry.rs, catalog.rs) — worth a separate look.
- P6 owed docs (full list in contacts.md): CLAUDE.md invariants (ownership, lock order `input_order → replay → registry → pending`, seq hold/contiguity), TRD §5-0 halt cursor / finish no-ring / marker 38 B / L560+L748 error sets / L378 ownership wording, ADR-0006 one line, catalog `queued` wording.
