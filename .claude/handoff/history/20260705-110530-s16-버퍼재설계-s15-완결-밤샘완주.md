# 핸드오프: S16 버퍼 재설계 + S15 DEFER 완결 — 밤샘 자율 완주 (커밋 5개, 버그 B 실측 종결)

## 한 줄 상태 · 다음 첫 액션
사용자 취침 전 결정 4건을 받아 **밤샘 자율로 두 과업 완주**: ① S16 PC 미러 버퍼 제거 재설계(리서치→TRD→ADR→구현→cdp 실측, de96ed9·ce8f0a8·9f3ae17) ② S15 DEFER 완결(비텍스트 칩 렌더+파서 제거, 1edbf63) + 기록(8b06953). **다음 첫 액션: 사용자 아침 보고 확인 → push 여부 결정**(push는 미승인이라 로컬만). 새 작업은 백로그(step-log 2026-07-05 항목 ⑧건)에서.

## ⭐ 규약 (사용자 결정 — 지속)
- 커밋 = 명시 승인만(이번 밤은 "게이트 통과분 단계별 커밋" 사전 승인 소진). push 별도 승인.
- 굵은 아키텍처 전제는 반드시 사용자 컨펌 후 착수. 결함 반복 시 증상이 아니라 구조 의심 → 사용자 에스컬레이션.
- 언어 규율(2026-07-05 신규, global-rules에 박음): 내부(thinking·서브에이전트 지시서·오케스트레이션) = 영어, 사용자 대면 = 한글, 프로젝트 산출물 = 프로젝트 규약(한국어).

## 사용자 결정 (2026-07-05 취침 전 — ADR-0046에 박제)
① 뷰별 직구독(O1) ② 갭 정책 = 보관 하한 replay(이력 정본 = claude 세션 `--resume`, 데몬 스냅샷 거부) ③ JSON 비텍스트 렌더 = 칩+클릭 펼침 ④ 단계별 커밋 사전 승인(push X).

## 완료 (전부 게이트 통과 + 커밋)
- **de96ed9** S16 설계: TRD rev4(`docs/process/S16-view-direct-replay/`) + ADR-0046(0040 전체 폐기, 0007/0037/0043 부분 개정 양방향+lint 청정). `/review trd deep` 3라운드(BLOCK→BLOCK→FIX 반영).
- **ce8f0a8** M0+M1: src-tauri 미러 전삭제 → 무상태 통과 + single-flight gen 펜스(replay_flight = core crate, 순수·테스트 14 실행) + tag=255 마커(전 BE). 리뷰 3인 2라운드(Codex BLOCK→좀비 의미론으로 해소→PASS) + qa full(cdp 스모크 PASS).
- **9f3ae17** M2+M3: ProtocolClient 뷰 단위 재작성(상태전이표·gen 펜스·사다리 3회+백오프·getViewOutputState) + 잔재 전삭제 + CLAUDE.md carrier 정정(TauriTransport). 리뷰 3인 3라운드(Codex BLOCK 2회 적출·반영→PASS) + qa full(cdp 8시나리오 PASS — **버그 B 종결**).
- **1edbf63** S15 DEFER: structuredAccumulator 아이템 스트림 + StructuredItemStream 칩/구분선 + streamParse/parse 제거(-189). 리뷰 2인(Codex FIX 3 반영→PASS) + qa full(cdp J1~J6 PASS — S15 §5 완료 기준 최초 충족).
- **8b06953** step-log 사후 기록(지난 세션분 포함) + 백로그 8건.

## 검증 상태 (쌍)
**돌린 것(전부 PASS):** cargo build(전 workspace) · cargo test --workspace --exclude engram-dashboard(replay_flight 14 포함 실행) · cargo test -p engram-dashboard --no-run · cargo fmt --check · 코어격리(use tauri 0) · npx tsc --noEmit · npm test(최종 214) · adr lint(error 0) · cdp 실측 3회(Stage A 스모크 / Stage B 8시나리오: remount×3·버그B 2슬롯+리로드·엇갈린 mount·epoch 재장전·LLM 표면·로그 / JSON J1~J6: json 스폰→칩 펼침/접힘→구분선→리로드 복원).
**재실행 명령:** 위 그대로 + cdp는 `$env:WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS="--remote-debugging-port=9223"; $env:RUST_LOG="debug"; npm run tauri dev` → `node scripts/cdp.mjs eval "..."`.
**검증 안 된 항목(정직):** ① cdp 1회 통과 = smoke지 race-free 증명 아님(핵심 동시성은 replay_flight 단위테스트+리뷰 3라운드가 커버) ② WS 재연결 실단절 시나리오(데몬 강제 kill 후 재연결)는 cdp 미실측 — connected 전이 재요청 로직은 vitest만 ③ 모바일/원격 seam은 자리만(구현 없음) ④ wsTransport(legacy)는 근사 구현(운영 경로 아님).

## 수용한 회귀·내부 결정 (아침 보고 대상 — 상세는 최종 보고 메시지)
- **재연결 = tail-resume → 전량 재replay 회귀 수용**(PC loopback·희소. src-tauri에 seq 남기는 절충은 ADR-0046 위반이라 거부).
- **wire 다중화 = 에이전트당 구독 1개 + 전량 재replay + 뷰별 dedup**(frame sub_id 프로토콜 확장 거부) — "뷰별 직구독"은 논리 계층에서 성립.
- **BLOCK-1 carve-out**: WsTransport(직결 legacy)는 자체 wire Subscribe 허용(ADR-0046 명시).
- Codex BLOCK을 사용자 에스컬레이션 없이 재작업 처리한 판단 3회: findings 무모순 수렴+국소 기계 결함+수정방향 합의 → rework+재리뷰(방향 결함 아님). 이의 있으면 리뷰 기록 재감사 가능.

## 실패한 접근 (do-not)
- 마커 gen 결합을 셈(마지막값 각인/FIFO 카운팅)으로 — 둘 다 리뷰 실증 격파(유실/영구 desync). 정답 = single-flight sole-outstanding(타임아웃이 대기열 안 진행).
- 절대시간 deadline — healthy-slow replay 오탐. 정답 = 진행 기반(frame/Ack마다 리셋) + acked 게이트.
- biased select(stream 우선) — 연속 출력 시 cmd arm 기아. 정답 = 공정 폴링(거짓 실패 마커는 좀비 의미론상 무해).
- 오버플로 시 buffer만 폐기 — 옛 gen 성공 마커가 빈 버퍼 flush. 정답 = myGen/heldMarker도 무효화.

## 미커밋 (의도적)
- `.claude/skills/research/SKILL.md`(선세션부터, 이번 작업 무관) · `.claude/skills/research/feedback.md`·`.claude/skills/adr/feedback.md`(이번 밤 스킬 피드백 기록 — 커밋 여부 사용자 몫).
- `I:\Engram\core\claude-global-shared\rules\global-rules.md` 언어 규율 절 추가(별도 트리, 커밋 지시 없음).

## 앱 상태
dev 앱 실행 중(백그라운드 b0t7yaiww, CDP 9223, RUST_LOG=debug) — 실측 잔여 테스트 에이전트 몇 개 남아있을 수 있음(리로드/kill로 정리 가능). 종료해도 무방.

## 참조 (읽을 것만)
- ADR-0046 + TRD rev4(`docs/process/S16-view-direct-replay/view-direct-replay-trd.md`) — 상태전이표·single-flight 계약이 정본.
- step-log 2026-07-05 항목 — 백로그 ⑧건(assignAgent JS 표면 no-op·테마 부채·onState 미배선·Error 귀속화·재등록 순서·LayoutRenderer stale·subs 맵 성장·M2 spawn UI json 노출).
- 핵심 신규 코드: `crates/engram-dashboard-core/src/replay_flight.rs`(single-flight) · `src/api/protocolClient.ts`(뷰 상태기계) · `src/components/slot/structuredAccumulator.ts`+`StructuredItemStream.tsx`(칩 렌더).
