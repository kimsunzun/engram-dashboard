# 핸드오프: S14 T6b 출력 평면 완료·커밋·push (C1~C4 닫힘) — 다음 = T7 cutover (dashboard2/master)

> ⚠️ `.claude/continue`는 gitignored(로컬). db2=worktree engram-dashboard(master), db1=engram-dashboard-a1(wip/a1). 이건 db2 관점. (사용자 명시 `/continue save` — T6b 완료 분기점에서 세션 종료.)

## 한 줄 상태 + 다음 첫 액션
S14 모듈① **T6b(출력 평면) 완료·커밋·push**(HEAD `0723214`, origin 동기, tree clean). 데몬 출력→src-tauri→창 fan-out 배선 + 리뷰 FIX 4건(C1~C4) 닫힘. `cargo test --workspace` **148 green**. **잔여(N1 등)는 의도적으로 T7로 이연**(현재 dead path — 프론트가 아직 wsTransport 직결, 이 경로 미사용).
**다음 첫 액션:** **T7 cutover** 진입 — `docs/process/S14-multi-page-layout/module1-transport-spike.md` **§10(T6b 완료+잔여)·§4(T7 정의)** 정독. T7 = 프론트를 `wsTransport`→`TauriTransport`로 교체(`clientFactory`) → T6b 배선이 **실제로 켜짐** → **GUI 실측(G2: 실제 창에 출력 도달, `cdp.mjs`)**. ★T7은 굵은 설계라 PRD/TRD부터(개발 스텝 규약) — 아래 N1 결정 포함.

## repo 상태
- HEAD = master `0723214`(= `15b68c8` T6b + `0723214` CLAUDE.md 브리핑규약), **origin/master 동기(push 완료)**, tree clean.
- 미커밋(untracked): `.claude/skills/research/study-notes/20260629-multi-viewer-terminal-buffer.md` — research 임시 학습물(사용자 "이해됐다" 시 research 스킬 학습섹션과 함께 삭제 규약, 아직 미확인). git 안 올림.

## 검증 상태 (쌍)
- **green·커밋됨:** `cargo test --workspace` src-tauri lib 148(기존 141 + T6b 7) / 전 멤버 0 failed. `cargo fmt --check` 0. 재실행=`cargo test --workspace`.
- **검증 안 됨:** **G2(tokio task→Tauri Channel→실제 창 도달) GUI 미실측** — dead path라 지금 실측 불가, **T7 cutover에서 프론트 연결 후 `/qa full`+cdp.mjs**. N1/N4/N5/N6도 T7에서 닫고 검증.

## T7에서 할 것 (= 잔여 결함 + cutover)
1. **cutover:** `src/api` `clientFactory`를 `WsTransport`→`TauriTransport`로. ensure/start는 Rust(DaemonClient)로 이전됨. connect 호출 자리 배선(현 lib.rs `TODO(T6/connect)`).
2. **★N1 (굵은 설계 — PRD/TRD + 사용자 결정):** 멀티창 같은 agent 동시표시 + 늦은 mount 시 출력 정합. **근본(리서치 2026-06-29, study-notes):** 우리 seq 큐 소비 모델 vs 터미널 표준 state-render 모델 미스매치 → **seq(전송 무손실)와 렌더(창 채우기) 분리**. **새 창 채우기 갈림: (A) 데몬 재요청**(업계 표준·단순·2홉 loopback) **vs (B) src-tauri 게이트웨이 캐시**(2계층 적합·비표준·동기화 비용). 리서치=중앙보관+재요청 표준(tmux/Zellij/VS Code), 게이트웨이 캐시는 web 렌더러 편의일 뿐.
3. **N5** subs 운영중 정리(retain은 재연결시만 — 연결 유지중 누수). **N6** resubscribe중 동시 layout mutation 중복 Subscribe → **데몬 ws.rs Subscribe 멱등성 확인 필요**(diff 범위 밖). **N4** fan_out Binary 배선 통합 테스트(seam=`trait FrameDelivery`로 headless 가능 / 또는 GUI 실측).
4. **T8:** 옛 wsTransport/protocolClient/slotStore 청소 + InProc mock(D4).

## 실패한 접근 / do-not (carry-forward)
- **green ≠ correct (재재재확인):** T6b도 148 green인데 `/review code deep` 2라운드가 C1~C4 + N1~N6 적출. **동시성 변경=`/review code deep` 3인 유지.** Codex blind가 opus 2인이 놓친 결함(C4 seq전진·멀티창seq) 단독 적출 — cross-family 효과 실측.
- **코더 자기보고 불신:** FIX 코더가 "이미 구현돼 있었다"고 오보고(실제 자기가 함) — 메인이 git diff·grep으로 직접 verify해 코드 정확성만 확인. **매 라운드 working tree 직접 검증.**
- **Codex** model 미지정, config `model_reasoning_effort` + `tools.web_search`(research). sandbox read-only·approval never.
- cargo 동시 실행 금지(빌드락). 출력 박스/와이드 아스키 금지. LF→CRLF 경고 정상.
- **N1은 "안 고친 버그"가 아니라 "아직 안 켠 기능의 설계"** — dead path라 T6b 커밋해도 문제 안 떠안음(사용자와 합의). T7에서 PRD/TRD로.

## 참조 (읽을 것만)
- 정본: spike `module1-transport-spike.md` **§10(T6b완료+잔여)·§8(F-B)·§9(G1~G3)** · `trd.md`.
- 리서치: `.claude/skills/research/study-notes/20260629-multi-viewer-terminal-buffer.md`(멀티뷰어 버퍼 = N1 근본).
- 코드: `connection.rs`(fan_out·resubscribe·mark_delivered) · `output_router.rs`(current_agents) · `output_channel.rs`(registry) · `protocol_state.rs`(decide_output/mark_delivered).
- ADR: 0035(레이아웃권위)·0036(전송중계)·0037(전송의미론)·0006(락순서)·0007(epoch). T7 N1 결정 시 새 ADR.

## a1 트랙 (worktree engram-dashboard-a1, wip/a1 — 만지지 말 것)
- a1=메시징 data-plane(목표⑤) 연기. 공통파일(SlotPane·SlotContextMenu)=모듈③, 건드리기 전 db1 핑.

## 정리 사항
- 떠 있는 앱/데몬 없음(포트 9223 free). T7 GUI 실측 시 새로 띄움.
- 컨텍스트 ~48%에서 마무리(T6b 커밋 분기점). T7은 새 굵은 작업이라 깨끗한 세션 권장.
