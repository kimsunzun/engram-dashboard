# 핸드오프: 출력 평면 2단계 — 데드락無·근원2개 닫힘·잔여FIX 완료 → 5차 국소검토 + cdp 실측 + 커밋 대기

> ⚠️ `.claude/continue` gitignored(로컬). master, HEAD = **e845daf**. 2단계 전부 **미커밋(디스크 영속, 유실 아님)** — 5차 검토·cdp 실측 미통과라 커밋 안 함. 직전 핸드오프(20260630-192019)의 후속(그 뒤 자율 진행으로 2근원 수정 + 4차 잔여FIX까지).

## 한 줄 상태 + 다음 첫 액션
멀티뷰어 출력 재설계 2단계(공유 버퍼 통합): 구현 → 1차리뷰 BLOCK2 수정 → 2차리뷰 2근원 수정 → **3차리뷰(데드락 없음·근원2개 닫힘 확인)** → **4차 잔여FIX 정리**까지 완료. 코드 게이트 전부 green. **cdp 멀티뷰 실측 0 + 4차 FIX 5차검토 미실시**가 유일 잔여.

**다음 첫 액션(택1, 가벼운 것부터):**
1. **5차 재리뷰(4차 FIX 국소 — light/full).** 4차가 도입한 `reconcile_slots`(양방향)·`ReplaySlots{fresh}` 플래그·`cmd_tx` 512가 새 결함 없는지. 국소라 light(Adversary 1인) 또는 full이면 충분(deep 불필요 — 데드락/근원은 3차에서 닫힘).
2. **`/qa full` cdp.mjs 멀티뷰 실측** — 앱 실행 필요(사용자 환경). 시나리오: 같은 agent 2·3창 / 새 창 늦게 mount / 재연결 중 새 창 / 미렌더 창 재연결 무손실 / terminal 후 정리 / **replay→live 순서** / 빠른 toggle·drag(근원1·FIX-1 검증) / webview reload(FIX-2).
3. 둘 통과 → **커밋**(2단계 전체 묶음 — 아래 repo 상태). step-log + ADR(↓미결).

## 핵심 설계 (정본=문서, 여기는 포인터)
- **ADR-0040 유효** — 공유 콘텐츠 버퍼 1벌 + per-view cursor + 두 축 분리(데몬 재구독 after_seq=버퍼 최신 ↔ 창 read=per-view cursor).
- **데몬 구독 소유 = Rust(layout 델타)** — 프론트 직접 구독 차단(ADR-0035/0037 이행, 1차 BLOCK-1).
- **통합부 순수 로직 = core `OutputViewStore<S>`**(Channel 안 듦·snapshot 반환, headless 테스트) — 사용자 결정(검증 가능성).
- **동시성 골격(3차 재리뷰로 검증됨):** 락 안 enqueue(근원1) 데드락 없음 — try_send non-blocking·actor가 ViewManager 락 안 잡음·router lock-free·registry↔buffer 한 방향. deliverable 게이트+resubscribe_slot(근원2)로 cursor advance ⟺ Channel delivery.

## 완료 + repo 상태 (전부 미커밋, 디스크)
- HEAD master **e845daf**. 직전 T7 보류분도 미커밋 유지.
- **신규:** core `output_view_store.rs`(`OutputViewStore` — on_frame(deliverable)/subscribe/resubscribe_slot/sync_viewers/unsubscribe/reconcile_slots/sweep_orphans/resubscribe_after_seq + 테스트 125 중 다수). **삭제:** core `output_window_seq.rs`.
- **수정:** core `lib.rs`·`output_view_buffer.rs`(1단계 계약) · src-tauri `output_channel.rs`(AgentBufferStore·flush_snapshot·registered_labels)·`daemon_client/connection.rs`(main_loop Binary arm+deliverable·`ReplaySlots{fresh}`/`DropSlots`/`Resync` arm·resubscribe_and_sweep→reconcile_slots·viewer-0 가드)·`daemon_client/mod.rs`(replay_slots(fresh)/drop_slots/resync·cmd_tx 512)·`commands/layout.rs`(send_subscription_delta 배정=fresh:false)·`commands/agent.rs`(subscribe_output 등록=fresh:true·forward_daemon_command Subscribe drop)·`output_router.rs`(SubscriptionDelta slot 델타·flatten_slots·current_slots·slots_for_window) · 프론트 `src/api/protocolClient.ts`(subscribeOutput Subscribe send 제거)·`protocolClient.test.ts`.

## 검증 상태 (쌍으로)
### 돌린 것 (green — 4차 후)
- `cargo test -p engram-dashboard-core` = lib 125 + 통합(reaper/session/transport) 전부 pass. 신규 회귀: deliverable/resubscribe_slot/reconcile 양방향/mount 1회 멱등/reload fresh 등.
- `cargo test -p engram-dashboard-protocol` 32 pass. `cargo build`(루트) OK(기존 ViewListPayload 경고만). `cargo fmt --check` OK. `rg "use tauri" crates/engram-dashboard-core/src/` 실제 import 0. `cargo test -p engram-dashboard --no-run` 컴파일 OK.
- 프론트 무변경(4차는 전부 Rust측). (직전 라운드 npm 127·tsc OK는 유효.)
### 검증 안 됨 (오신뢰 금지)
- **4차 FIX(reconcile 양방향·ReplaySlots fresh 분기·cmd_tx 512) 5차 재리뷰 미실시.** 국소지만 동시성 영역.
- **cdp.mjs 멀티뷰 실측 0** — 화면 동작 전무 미확인. src-tauri lib test `0xc0000139`(WebView2 DLL) 여전 → 통합부 의미 회귀는 core 단위 + cdp가 본판.

## 동시성 골격 (3차 재리뷰 확정 — 다음 세션 신뢰 가능)
- **데드락 없음(확실):** 근원1 락 안 enqueue 안전(try_send non-blocking·actor는 buffer/registry 락만, ViewManager 락 0·router ArcSwap lock-free·registry⊃buffer 한 방향 중첩).
- **근원2 닫힘(확실):** on_frame `deliverable` 게이트(append은 게이트 *전*=축A 비회귀, advance/snapshot만 게이트) + resubscribe_slot(Channel 등록 시 fresh).
- **축 A viewer-0 가드 무해(메인 핀포인트 확정):** router 빈=어느 창도 안 봄 → 데몬도 곧 Unsubscribe(layout 소유)+데몬 원본 보유 → append 스킵해도 재배정 시 복구. Codex BLOCK은 blind가 이 정합 미인지한 과대평가(opus 2인 무해 + 메인 확정).

## 4차 잔여 FIX 내용 (5차 검토 대상)
- **FIX-1 유실 복구:** `reconcile_slots`(양방향 — keep에 있는데 cursor 없는 slot=유실 replay 복구 + keep에 없는 cursor=sweep, cursor 있으면 불가침 멱등). connect/재연결/Resync 진입마다. cmd_tx 64→512(완화). 
- **FIX-2 mount 이중 트리거 멱등화:** `ReplaySlots{fresh}` — 배정 트리거(layout)=`subscribe`(불가침), 등록 트리거(subscribe_output)=`resubscribe_slot`(fresh). 정상 mount=전체 replay 1회 / reload=fresh 1회 / 재연결=frontend dedup 2차 안전망(ADR-0037 정신 충족, 완전 제거는 아님).
- **FIX-3:** viewer-0 가드·sweep 주석 보강 + flush registry miss `tracing::debug`.

## do-not (실패/금지)
- **새 테스트 크레이트 파지 말 것**(컴파일 더 느림 — 기존 core `-p`로 좁히는 게 정공법, 사용자 합의). 코더 수정루프 `-p core`만, 최종 전체 1회.
- **두 축 합치지 말 것** / **버퍼 락 보유 중 Channel send 금지**(락 밖 flush) / **버퍼 락 ⊃ registry 락 중첩 0** / **actor가 ViewManager 락 잡지 말 것**(근원1 데드락 회피) / **데몬 `agent/output_core.rs` ReplayBuffer/C4 건드리지 말 것** / **프론트 직접 데몬 구독 부활 금지**(ADR-0035) / **min 모델(output_window_seq) 부활 금지**.
- 기존 회귀 테스트(deliverable·reconcile·mount 멱등 등) 깨지 말 것.

## 미결정·기록 (커밋 전)
- **ADR 거리:** ① 데몬 구독 layout 소유(ADR-0035/0037 이행 — 한 줄 박제 권장) ② SubscriptionDelta slot 단위 확장(새 자료구조) ③ mount replay actor 경유 + fresh 분기. 커밋 시 step-log + 위 ADR 판단.
- **무손실 강등:** 재구독 after_seq=버퍼 최신이라 미렌더 창 무손실=버퍼 잔량 한정 = **TRD §3 gap "불가피 유실·잘림 미표시"에 이미 명시된 의도된 trade-off**(메인 확정, min_render_seq 회귀 금지). 사용자 이견 시에만 재론.
- (직전 후속: app:None ADR 채번, CLAUDE.md `/review doc`, tauriTransport.ts 주석 중복.)

## 이번 세션 서브에이전트 이력 (누적)
- **코더 opus ×5:** 2단계 1차 / mount-replay·생명주기 갭 / BLOCK·FIX(데몬 구독 Rust 이전+slot 델타+actor) / 2근원(enqueue 락 안+deliverable 게이트) / 4차 잔여FIX(reconcile+fresh 멱등).
- **`/review code deep` ×3 라운드, 각 3인**(opus 불변식 + opus 동시성/락 + Codex blind). **cross-family 가치 3회 실증:** 1차=Codex가 프론트 직접 구독 BLOCK(opus 놓침) / 2차=opus동시성+Codex가 enqueue 순서·cursor 생명주기 수렴 / 3차=Codex 축A BLOCK은 blind 과대평가(메인 핀포인트로 무해 확정 — doc-aware가 정합 맥락 우위). opus불변식은 매 라운드 FIX 관대.

## 참조 (읽어야 할 것만)
- TRD `docs/process/S14-multi-page-layout/output-view-buffer-trd.md`(§1 락·§3 두 축+gap·§4 생명주기·수용기준).
- ADR `docs/decisions/`: 0040·0035/0036/0037·0006·0007.
- 5차 검토 지점: `output_view_store.rs`(reconcile_slots·resubscribe_slot·subscribe 멱등) · `daemon_client/connection.rs`(ReplaySlots{fresh} arm·reconcile_and_sweep) · `commands/layout.rs`/`commands/agent.rs`(fresh:false/true 트리거).
- 계약 정본: core `output_view_buffer.rs`(cursor 의미·clamp).
- `docs/process/step-log.md` 최신.
