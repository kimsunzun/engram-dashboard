# 핸드오프: 벤치마크 교정(Cline→Claude Code 확장) + 채팅 재작성 라운드 스펙 확정(미착수) · SmartScreen 픽스 완료 · 푸시 3커밋 대기

## 한 줄 상태 · 다음 첫 액션
- **상태:** 오전 작업(Cline 충실도+thinking, `82a8108`·`8b844ed`) 완료 후 **벤치마크가 교정됨** — 사용자가 원하던 룩은 Cline이 아니라 **Claude Code 공식 VSCode 확장**(스샷 비교로 확정). 이에 따른 **채팅 재작성 라운드의 스펙·결정·탐색이 전부 끝났고 코더 스폰 직전에 세션 마감**(컨텍스트 예산). 보안팀 요청 SmartScreen 픽스는 당일 처리·커밋(`c5d987e`).
- **다음 첫 액션:** ① `git push origin master`(3커밋 ahead — 권한 차단으로 미푸시, 사용자 실행 or 승인) ② **채팅 재작성 라운드 실행**(아래 확정 스펙 → `/implement` standard, 코더 opus → review full → qa full).

## 사용자 확정 결정 (재론 금지 — 전부 사용자가 직접 고름)
1. **벤치마크 = Claude Code VSCode 확장 룩**(소스 비공개 → 스샷·실측 벤치마킹, 코드 포트 불가). Cline 기준 폐기.
2. **Cline 포트 코드 완전 분리·재작성** — "이름만 바꾸기" 아닌 진짜 자체 구현으로 새로 쓰고 귀속 3종(THIRD_PARTY_NOTICES.md·LICENSES/cline-Apache-2.0.txt·파일 헤더) 삭제. 사유: 654줄(실질 3~400줄)에 라이선스 공지를 끌고 다닐 가치 없음 + 벤치마크 변경으로 존재 이유 소멸.
3. **빈(암호화) thinking도 행 표시** — opus에서도 "Thought" 라벨이 보이게(확장과 동일).
4. **KaTeX 수식 렌더 추가**(remark-math + rehype-katex + katex — 현재 $$...$$ 생문자열 노출).
5. **전송 버튼 제거**(Enter 전송 유지).
6. 진행 방식: "일단 구색 맞추고 그 이후 하나하나 떼거나 붙이기" — 1차 근사 후 사용자 스샷으로 반복 조정.

## 재작성 라운드 확정 스펙 (코더 지시서 골자 — 이대로 스폰)
- **신규 `src/components/slot/chat/`:** ① `Markdown.tsx` — react-markdown+remarkGfm+remarkMath+rehypeHighlight+rehypeKatex(+katex css), zero-width 소독(U+200B류 — 스트리밍이 펜스 앞에 흘림), 단일 ReactMarkdown(블록 쪼개기 금지), 언어 별칭 정규화, pre에 hover 복사버튼, 래퍼 `.chat-markdown` ② `ThoughtRow.tsx` — 접힘 "Thought" 13px muted+셰브런, 내용 있으면 토글(펼침 = border-l pl-3 muted, max-h 200px 스크롤), **내용 비면 비상호작용 라벨만**(tooltip "내용 비공개"), streaming이면 animate-pulse ③ `CopyButton.tsx` — lucide Copy→Check 1.5s, ui/button·cva·radix 무의존 ④ `chat.css` — 1차 근사값: 13px/1.45, p mb 0.5em(last 0), h1 1.3em/600·h2 1.15em/600·h3 1.05em/600·h4 1em/700(m 0.8em 0 0.4em), ul/ol 마커+pl 1.5em, pre: bg-secondary+border+radius 6px+p 12px+lh 1.5, 인라인 code **중립색**(accent 아님), table th/td 6px 10px, hr border-color var(--border).
- **삭제:** `src/components/slot/cline/`(5파일), `src/components/ui/button.tsx`(임포터 = Cline 잎 2개뿐 — 탐색으로 확인), 귀속 3종. 의존성: cva·@radix-ui/react-slot·unist-util-visit 임포터 없으면 제거, remark-math·rehype-katex·katex 추가, `npm install` 후 **BOM 검사**(이전 라운드 회귀 전례).
- **StructuredTextView 재스타일:** 유저 버블 = `rounded-md border border-border bg-surface px-3 py-2 my-1`(badge 토큰 `--badge-bg/fg`·`--color-badge*`는 제거), thinking→ThoughtRow(빈 내용 포함), 스트리밍 어포던스 = ThoughtRow streaming(키 `__streaming__` 유지), 루트 leading-[1.25]→[1.45], 툴/에러 행은 이번 라운드 유지(후속 조정).
- **RichSlot:** 전송 버튼 JSX 삭제, textarea 전폭. **Enter/Shift+Enter/IME 가드(isComposing||229)/stopPropagation/종료 비활성/send() awaiting-catch 로직 절대 불변**(load-bearing, 회귀테스트 있음).
- **테스트:** cline/MarkdownBlock.test.tsx → chat/Markdown.test.tsx 이전(제목·표·코드·U+200B 케이스 유지 + KaTeX `.katex` 스모크), StructuredTextView.test.tsx 갱신(버블 클래스·빈 thinking "Thought" 라벨 = 신규 동작 테스트·usage 무렌더 유지).
- **금지:** structuredAccumulator 로직(특히 Date.now 금지 — replay 멱등 불변식)·api층·Rust·무관 미커밋 불가침. marked/styled-components 재도입 금지.
- **후처리(메인):** ADR-0048 폐기(supersede full) + 신규 ADR("채팅 렌더 자체 구현 + Claude Code 확장 벤치마크"), step-log, `rg -i cline src/` 잔존 언급 정리 확인.

## 탐색 산출 (이번 세션 완료 — 재탐색 불필요)
- **폭발 반경 국소 확정:** cline 6파일 임포터 = StructuredTextView + 자기들끼리 전부. lab 무사용. ui/button 임포터 = ThinkingRow·CopyButton뿐.
- **프론트 지도:** 조감도 초안 `docs/reference/architecture-overview.md`(미추적 — 프로세스·배선·replay 상태기계까지 우수, **컴포넌트층 절만 얇음**). 컴포넌트층 요약: 스토어 4개(agentStore/viewStore[백엔드 권위 캐시]/slotStore[레거시—팝업·트리]/themeStore), eventBus가 이벤트 2갈래(레이아웃=Tauri listen, 에이전트=agentClient 핸들러) 1회 등록 + `window.__engramLayout` 등 LLM 표면 노출, ViewLayoutRenderer가 capability로 렌더러 선택(rich/terminal/dom). RichSlot 입력바 = RichSlot.tsx ~188-214행.
- **오펀(정리 후보):** `structuredToRichMessages.ts`(무임포터), 레거시 LayoutRenderer.tsx.

## SmartScreen 픽스 (완료 — 보안팀 회신용)
- 감지된 `--disable-features=…msSmartScreenProtection` = **Tauri(wry) Windows 기본 인자**(우리 소스 무존재 — 프로세스 커맨드라인+레포 전수검색 실측). `tauri.conf.json` 창 3개에 `additionalBrowserArgs` 명시로 기본값 대체(SmartScreen만 재활성, msWebOOUI/msPdfOOUI·autoplay 유지) → 재기동 실측으로 플래그 소멸 확인. 커밋 `c5d987e`.
- **회신 문구:** "직접 설정한 옵션이 아니라 앱 프레임워크(Tauri)가 WebView2에 주입하는 기본 인자였음을 확인했고, 해당 플래그만 제거하도록 설정을 수정·적용 완료했습니다."

## repo 상태 (master, origin +3: 82a8108·8b844ed·c5d987e)
- **미커밋 유지(의도):** echo 작업(claude.rs 잔여 hunk·session.rs·mod.rs — 게이트 미통과), Sidebar.tsx, run-dashboard-clean.bat, structuredToRichMessages.ts, architecture-overview.md(미추적 초안), phase2-*·qa-*.png(아티팩트).
- **검증 상태:** 오전 게이트 전부 PASS(직전 핸드오프 참조). SmartScreen = 실측 PASS. 재작성 라운드 = **미착수**(코드 무변경).

## 함정 (do-not — 계승 + 신규)
- **opus-4-8 thinking = 암호화(평문 빈 문자열) 업스트림 동작 — 재디버깅 금지.** sonnet은 평문 정상(ThinkingRow 실측 PASS).
- 재기동: vite(1420)+engram-dashboard.exe+데몬 3종 확인 kill(백엔드 변경 시 구 데몬이 새 코드 막음). 이번 SmartScreen처럼 **클라이언트만의 변경은 데몬 생존 OK**.
- Json 프로필 id: 구 181e99d7 소실 → `8690560a`(Json)·`JsonSonnet`(sonnet-4-6, thinking 실측용) 사용.
- 마크다운 검증 = 자연 프롬프트(펜스 랩 함정), 루트 `cargo test`는 src-tauri DLL 로더 선재 이슈 — 멤버별 실행.

## 앱 상태
dev 앱 실행 중(task `bhhqmzd5k`, CDP 9223, 새 additionalBrowserArgs 적용 상태). 데몬 pid 23504.

## 참조 (읽을 것만)
- 재작성 대상: `src/components/slot/StructuredTextView.tsx` · `slot/cline/*` · `RichSlot.tsx`(입력바) · `src/index.css`·`styles/theme.css`(badge 토큰 제거분)
- 벤치마크 스샷: 사용자 이미지 2장(이 세션 대화) — 다음 세션서 추가 스샷 요청 가능(유저버블·Thought 펼침·코드블록 3장이 이상적)
- `docs/reference/architecture-overview.md`(프론트 배선 정본급 초안) · `docs/decisions/0048`(폐기 예정)·`0049`
