# 핸드오프: mid-turn input — TRD 8판 확정(6차 FIX 반영 · 7차 PASS) · ADR-0231 · 사용자 위임으로 구현 착수 — P0·P1a WIP, P1b·P1c 진행 중

(English body — user decision 2026-09-24. Mid-session checkpoint written while workers run; the user is asleep and delegated everything.)

## One-line state

User delegated TRD confirmation + implementation (2026-09-26 「나 자러갈테니깐 쭉쭉 진행해. 충분히 얘기한것같으니 구현 너가 알아서하고 후에 최종 보고하는걸로」). Design is done (PRD 6판, TRD 8판, ADR-0231, pushed). Implementation follows `.claude/handoff/attachments/20260926-midturn-impl/plan.md` (26 chunks). P0 + P1a are WIP-committed; P1b (TS reducer) and P1c (turn table + kernel) were running at save time; M16 measurement running.

## Next actions

1. Collect P1b/P1c returns → WIP commit each (add only their files) → append their landed shapes to `.claude/handoff/attachments/20260926-midturn-impl/contacts.md`.
2. P1d (core wiring, critical) → P1e (input-path skeleton, critical) — sequential (shared files).
3. P1 phase gate: `/review code deep` on `5e9dcca..HEAD` (P0+P1) → fixes → `/qa full`-equivalent (P1 has no visible change: workspace tests + gates + GUI smoke) → squash `git reset --soft 5e9dcca` + one commit `S21: feat(midturn): P0·P1 …` → push → CI.
4. Then P4 (a→b→c) → P5 (a ∥ b→c) → P3 (a→b) → P2 (a…f) → P7 (M15 needs P2e2; M16 running now) → P8 (only if M15 green) → P6 (docs + final QA).
5. Final report to the user (items below).

## Decisions made this session (authoritative records)

- User answers N11 (b, widened into 「오류 뒤 멈춤」), N12 (a), N13 (b), tooltip 「목록에서 빼기」, simplicity direction, delegation — `.claude/handoff/attachments/20260926-midturn-answers.md` (verbatim quotes), PRD 6판 §3/§6, TRD §10-1.
- Orchestrator rulings (delegated) — review round 6: ONE mail gate = kernel `TurnFact.last_end_failed` for both backends and ALL vendor versions (old codex also stops mail after failure — user-visible, report it); interrupt neither sets nor releases; LLM field `stopped_after_error`; plan §6 decisions (SpawnParts, origin param, INPUT_AFFECTING const, M15 red → keep `Immediate`, WIP-commit-then-squash policy).
- Turn-end probe removed (M10 green + vendor source). Late echo after turn end → bubble, no automatic answering turn (concession).

## Repo state

- Branch `v0.3.2/feat/json-midturn-queue` in wt1; pushed up to `5e9dcca` (ADR-0231). Local WIP on top: `39d97fd` (P0 fixtures), `510011d` (P1a). Master merged at `6787c83` (conflicts: step-log resolved keeping both; `.claude/handoff/latest.md` kept ours).
- Phase base for the P0+P1 squash = `5e9dcca`.
- Uncommitted at save: P1b/P1c/M16 workers' files (M16: `.claude/handoff/attachments/20260925-midturn-phase0/{codex_harness.js,m16codex.js}` + report `docs/research/mid-turn-m16-measurements-2026-09-26.md`).

## Verification state

- Docs: `/review trd full` r6 (both FIX → applied) · r7 light PASS (7 LOW applied) · ADR `/review doc light` FIX → 12 applied · ADR lint: only pre-existing (0170 gap, 0016, ADR-0027 anchor).
- Measurements: M9/M10/M13 green (`docs/research/mid-turn-phase0b-measurements-2026-09-26.md`). M16 running. M15 not yet (needs P2e2).
- Code: P1a coder ran agent/protocol/daemon tests + `cargo build` + tsc + slot vitest (all `__EXIT=0`); NOT yet reviewed (`/review code`) or QA'd. P0 = data only, build checked.
- CI on the pushed branch: not checked yet.

## Traps

1. TRD lines are 3–5 KB; never let a worker read it whole. Use `changed_rows.py` for reviewer inputs.
2. Workers must run builds/tests via `scripts/run-detached.ps1` and judge by `__EXIT`; agent/daemon/base tests need `-- --test-threads=4`.
3. Phase 0 harness `.js` files fail in-repo (package.json is ESM) — copy as `.cjs` outside the repo.
4. When several workers share the worktree, WIP-commit only the finishing worker's files (explicit paths, never `git add -A`).
5. `git merge` conflict resolution: check for CRLF (step-log.md is CRLF) before regex-replacing markers; never `git add` a file before verifying markers are gone.
6. `OutputCore::new` / `AgentSession::new` signatures must not change (builders only); `transport/{pty,stdio,input_queue}.rs`, `TerminalSlot.tsx`, `src/lab/terminal/` diff 0 vs master.
7. `nmfc-origin-dev-harness` hooks misfire every turn — ignore.

## Stop conditions (autonomous mode — stop the phase and leave it for the user)

- `/review code` BLOCK, reviewer disagreement that is a direct contradiction, or the implement loop cap (2 consecutive unresolved / 3 total re-fixes).
- M15 red → no P8, keep `Immediate` (decided — report, don't stop).
- Never: master merge, `v*` tag push, touching wt2 or apps not started by us.

## For the final report (collected so far)

- Old codex (below the version floor) now also stops mail after a failed turn — a delegated ruling (R1), user-visible.
- Late echo after turn end: bubble appears but no automatic answer (answered with the user's next turn) — differs from what was said when N12 was explained.
- Pre-existing fixtures `claude/fixtures/claude_text.jsonl` / `claude_tool.jsonl` carry internal plugin names and `I:\Engram` paths (org privacy) — not touched; suggest a scrub.
- ADR-0231 number reserved by push; CLAUDE.md invariants update is P6.
