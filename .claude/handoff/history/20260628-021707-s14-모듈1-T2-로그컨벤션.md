# 핸드오프: S14 모듈① 전송 재배치 — T2 완료 + 로그 컨벤션 신설 (dashboard2)

## 한 줄 상태 + 다음 첫 액션
S14 모듈①(전송 재배치): D1 결정(A=가드 Rust 단독) → ADR-0037 → T1(deps) → T2(DaemonClient 연결+핸드셰이크, generation TOCTOU까지 수정) 커밋·푸쉬 완료. 로그 컨벤션 SSOT 신설 + review 바인딩 연결.
**다음 첫 액션:** T3(protocol_state: epoch/seq dedup·resubscribe·pending) 진입 — 단, 그 전/함께 **daemon_client tracing backfill**(이번에 만든 로그 컨벤션 따라). 둘 다 코더→`/review code`→`/qa`.

## 완료 + repo 상태
- 브랜치 **master**, origin 동기 (HEAD=`6e4d6bd`, pushed). github.com/kimsunzun/engram-dashboard.
- 커밋: `ea6181d`(ADR-0037) · `72492e5`(T1+T2 daemon_client) · `6e4d6bd`(로그 컨벤션+바인딩+CLAUDE.md). 사이 `2f430c5`=dashboard1 공유트리 커밋(continue 스킬·리서치).
- 완료: D1=A · ADR-0037(ADR-0020 결정3 부분폐기) · T1 deps 복원 · T2 `src-tauri/src/daemon_client/`(connect/ensure 분리 ADR-0021·단일 task가 split WS 단독소유·Auth/Hello 핸드셰이크·`lifecycle.rs` 단일 락 generation 가드로 TOCTOU 차단) · 로그 컨벤션(`docs/reference/logging-conventions.md`) + review 바인딩 관찰성 렌즈 + CLAUDE.md 예외 조임.
- **미커밋 = 전부 dashboard1 소관**(`src/lab/richslot/*`, `src/lab/main.tsx`, `src/components/slot/TerminalSlot.tsx`) — 건드리지 말 것.

## 검증 상태 (쌍)
- **돌린 것:** `cargo test`(워크스페이스 전체) green — src-tauri 70(daemon_client + 스트레스 포함), 0 failed · `cargo fmt --check` clean · `/review code deep`(opus doc-aware + Codex blind → TOCTOU까지 잡아 수정 후 PASS) · `/review doc full`(로그 컨벤션 → FIX 9건 반영). **재실행:** `cargo test`(루트).
- **검증 안 됨(오신뢰 금지):** daemon_client는 **mock 상대 격리 테스트만** — 실 데몬 end-to-end 미실시(UI/invoke 미배선이라 T7 + GUI 실측 때 가능) · **daemon_client tracing 0건**(backfill 안 됨) · T3~T8 미구현.

## 발견 버그 (미수정 — 다음 세션)
- `crates/engram-dashboard-daemon/src/lib.rs:207` 주석 "init_logging 재사용(키 마스킹 포함)" = **거짓**. `mask_secrets`(`core/src/logging/mod.rs:28`) 소스 호출처 0 = 마스킹 미배선. 주석 정정 필요.

## 미결 결정 / 주의 (do-not 포함)
- **View 붙이기 = B(순서대로) 선택** — 모듈① 마저 한 뒤 통일 경로로 붙임. (A=레이아웃셸+fixture 먼저 가시화는 보류. 레이아웃 백엔드 모듈②는 이미 완료, React 소비자=모듈③ 미구현.)
- 공통 파일(`SlotPane`·`SlotContextMenu`) 수정은 dashboard1과 **건드리기 전 핑**(합의됨, 모듈③ 충돌점).
- 리뷰 메커니즘 정리: 리뷰 단위=diff(커밋=리뷰됨, 파일 마커 안 씀 — rot 방지). 서브에이전트는 CLAUDE.md 자동 안 읽음 → 오케스트레이터가 바인딩 읽고 doc-aware에 규약 주입, blind(Codex)엔 안 줌(앵커링 차단).
- `latest.md`는 dashboard1과 공유 — 이 핸드오프가 덮어씀. dashboard1은 자기 핸드오프를 history/ 파일명으로 로드.

## 참조 (읽어야 할 것만)
- spike(T3~T8 분해·격리하네스·D2~D5): `docs/process/S14-multi-page-layout/module1-transport-spike.md`
- ADR-0037(전송 의미론=Rust 단독) · ADR-0036(전송 통일) · ADR-0011(agentClient 인터페이스)
- 로그 규약: `docs/reference/logging-conventions.md`
- TS 이식 명세서: `src/api/protocolClient.test.ts`(T3 = 21케이스) · `src/api/wsTransport.test.ts`
- step-log S14 섹션(`docs/process/step-log.md`)
