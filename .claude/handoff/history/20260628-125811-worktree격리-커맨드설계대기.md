# 핸드오프: worktree 격리 정착 + 커맨드/메시지 리서치 완료 → 커맨드 설계 결정 대기

**한 줄 상태:** dashboard1(wip/a1)·dashboard2(master)를 git **worktree로 격리** 완료. 커맨드/메시지 OSS 리서치 끝(wip/a1 커밋·push). 지금 **커맨드 시스템 설계 결정 4개**를 사용자와 정하던 중(A·C가 본질).

**다음 첫 액션:** 커맨드 설계 결정 **A(결과 반환)·C(protocol enum 분할)**부터 사용자와 확정 → (선택)ADR → 이후 view 고도화 or dashboard2 빌드 인풋.

## repo/worktree 상태 (★ 먼저 읽기)
- **worktree 2개**(같은 `.git` objects 공유, 각자 HEAD 격리):
  - `I:\Engram\apps\engram-dashboard` = master = **dashboard2**(S14).
  - `I:\Engram\apps\engram-dashboard-a1` = wip/a1 = **나(dashboard1)**.
- ★ **다음 dashboard1 세션은 `engram-dashboard-a1` 폴더(wip/a1)에서** 작업해야 격리 유지. (이 핸드오프는 `.claude/continue`가 gitignored라 main 트리 쪽에 저장됨 — cwd 기준. dashboard2와 `latest.md`를 공유하니, 덮였으면 `history/`에서 이 파일 찾을 것.)
- 내 작업 **전부 push됨**(origin/wip/a1 = `45a5d9e`), **미커밋 0**. 6커밋: lab richslot · TerminalSlot fix(ADR-0036) · branch-strategy(draft) · 리서치스코프(draft) · 리서치보고서 2 + study-note.
- 스트레이 `wip/a2`는 삭제됨. dashboard2는 master에서 S14 진행(자기 도메인 — **안 건드림**).

## 완료·검증한 것
- **리서치**(`/research` medium): cross-family(Claude Sonnet 3 + Codex 2, BLIND) + opus 교차/적대검증. **충돌 0 강수렴.** 보고서: `engram-dashboard-a1/docs/research/command-control-surface-survey-2026-06-28.md`, `agent-messaging-survey-2026-06-28.md`.
- **커맨드 결론:** 타입드 **intent 레지스트리** — VS Code 단일핸들러(사람=LLM 동일경로) + Zellij action enum + CQRS dispatch(트레이트 자체구현, cqrs-es류 거부) + 프론트=`invoke` 한 줄 + command ID≈LLM tool. **키=enum variant(serde tag), 인자=variant named 필드**(serde `tag/content`). 컨트롤러 트레이트로 교체성.
- **메시지 결론:** tokio 채널(seam 트레이트+전달클래스) now → NATS JetStream 원격 future. Ractor식 supervision(bounded 병용). 에이전틱 레이어(AgentId/Task/Handoff/Approval/…) transport 위. A2A=외부 boundary만. **control plane(커맨드)/data plane(메시지) 논리 분리**, 물리 전송로는 공유 가능.

## 검증 안 됨 (="된 것"으로 적지 말 것)
- 리서치는 **medium 서베이**(구현검증 X). 양 family 강수렴이지만 **만장일치≠정답**(tokio/NATS/plane분리에 공통 학습편향 가능 — 보고서에 명시).
- **S14 ViewManager 실제 API 미확인** → 레이아웃 커맨드(Split/Place)가 그걸 어떻게 부르는지(결정 D)는 **dashboard2 코드 나와야** 확정.
- `branch-strategy.md`·`리서치스코프` = **draft, `/review doc` 미통과**(canonical·master 머지 전 필요).
- Ractor 전달보장 at-most-once = 문서 미명시(불확실).

## 커맨드 설계 미결 (이어서 결정 — A·C 본질)
- **A. 결과 반환:** invoke 리턴 / 이벤트(correlation_id) / 둘 다. lean=즉시는 invoke리턴·진행중은 이벤트.
- **C. protocol Command enum 분할:** 한 덩어리 vs 도메인 sub-enum(`LayoutCommand`=dashboard2 / `Agent`·`ViewCommand`=각자) — **공유파일 충돌 방지**. lean=분할.
- B. 커맨드 vs 로컬상태 경계: lean="리로드 살아남거나 LLM 건드릴 것만 커맨드".
- E. LLM 노출: `exec` 한 tool(union) vs per-command. lean=하나로 시작.
- D.(S14 대기) ViewManager 호출=직접 call vs 이벤트.

## 분담 (잠정)
dashboard2=구조 빌드(command/message 백엔드 포함 가능성), 나=view 고도화(RichSlot 실통합 등). S14+리서치 후 누가 뭘 할지 확정. **dashboard2엔 리서치 인풋 아직 안 줌**(사용자 지시 — 저장 먼저).

## 커뮤니케이션 메모 (★ 중요)
사용자가 "내 설명이 어지럽다 / 독백 같다"고 피드백함. **결론 먼저·짧게·평이하게, 리스트 남발 금지**(org 규칙). 사용자는 **메시지 시스템 제작 경험 많음** → 기초 강의 금지, peer로 대화. 긴 구조덤프 X.

## do-not / 실패
- 공유 트리에서 `git checkout`(공유 HEAD 이동)·`git add -A`(상대 파일 staging) **금지** — worktree로 갈라놨으니 각자 폴더에서 자기 것만 커밋(`git add <경로>`).
- `/research` 서브에이전트가 main 트리에 study-note 흘린 전례 → 출력은 worktree(wip/a1)로 회수.

## 참조 (읽을 것만 — 전부 engram-dashboard-a1 / wip/a1)
- 리서치 보고서 2 + `docs/research/llm-control-surface-message-command-scope-2026-06-28.md`(스코프) · `docs/reference/branch-strategy.md`(draft).
- ADR: 0011(agentClient seam)·0002(capability.output)·0029(daemon)·0035/36/37(S14 View).
