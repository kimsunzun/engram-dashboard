# 핸드오프: 평평한 분할 렌더러 — 1~7단계 커밋 완료(7 = 렌더러 교체), 다음은 8단계(allotment 걷기)

## 한 줄 상태
TRD `docs/process/S21-flat-split-renderer/trd.md` §6 이행표 **1~7단계를 커밋·push 했다**. 7단계(`d014c0c` — 평평한 루트 렌더러, 유일한 사용자 체감 변경)는 `/review code deep` 3인 PASS · `/qa full` PARTIAL(터미널 항목만 미측정 — 실 에이전트 필요). CI: 1~6 초록(`6b82763`), **7(`d014c0c`)은 저장 시점 진행 중**. 남은 것: **8(allotment 걷기) → 9(문서) → 사용자 GUI 확인 → master 머지**.

## 다음 첫 액션
1. `gh run list --branch v0.3.2/refactor/flat-split-renderer --limit 3` — `d014c0c` CI 초록 확인(빨가면 그것부터).
2. **8단계** `/implement`(standard, qa full) — 아래 「8단계 목록」 전부. 한 커밋.
3. **9단계** 문서 — 아래 「9단계 목록」, `/review doc`. 한 커밋.
4. 사용자에게 **GUI 확인 요청**(아래 「사용자 GUI 확인 목록」) → 확인 뒤에만 master 머지(CLAUDE.md 「브랜치·커밋」: `merge-tree --write-tree` 종료코드 0 확인 → `commit-tree`(부모 master 먼저, `-F` 본문 항목) → `update-ref`(옛 SHA 넷째 인자) → 확인 넷 → push. fast-forward 금지).

## 자율 범위 (사용자 결정 2026-09-24~25)
「쭉 진행」·「핸드오프하고 막히기전까지 자율로」. 단계마다 코더 → `/review code` → `/qa` → 통과분 커밋·push. **master 머지 금지(사용자 최종 GUI 확인 전)**. QA PARTIAL(도구 한계) = 커밋 가(사용자 3단계 「커밋이야 그냥 하면 되는데」 — 4·7단계도 같은 기준, 커밋 메시지에 기록).

## repo 상태
- 워크트리 `I:\Engram\apps\engram-dashboard-wt2`, 브랜치 `v0.3.2/refactor/flat-split-renderer` = `d014c0c`(+ 이 핸드오프 커밋), origin 동기, 작업트리 깨끗.
- ★가짜 `M` 약 26개(`crates/*/bindings/*`·`src-tauri/Cargo.toml`) 스테이징 금지★.
- 앱·데몬 없음. **wt2 vite(1420)** 는 이전 세션 런처(cmd PID 11576 → `npm run dev`)가 붙들고 있다 — GUI QA 가 재사용, 죽이지 말 것.
- 줄끝: `src-tauri/src/**` 작업트리 CRLF · TS 파일마다 다름(`git ls-files --eol` 로 확인) · 인덱스 LF.

## 커밋 목록
`e401966` 1 geometry.rs · `3f8c4ef` 1b ratio f64·spatial 정확 인접 · `6c8eafe` 2 스냅샷 사각형 · `499eb3a` 3 캔버스·지표 보고 · `c9e1c2e` 4 `set_split_ratio`·버스 `split.setRatio`/`split.list`·`SplitTooDeep` · `c6baf21` 5 `LayoutLeaf` 이사 · `f363a51` 6 `splitPreview.ts`·`Splitter.tsx` · **`d014c0c` 7 평평한 렌더러**(`ViewLayoutRenderer` 평평한 루트 · `LayoutLeaf` 틀 + `SlotBody` · `index.css` 위치 규칙 · `useSplitDrag.ts` · `uiMetricsReport.ts` · `windowCanvasReport` 구독 · 테스트 재작성). 각 커밋 본문에 게이트 기록.

## 7단계 QA 결과 (125% 배율 PC)
- 코드: 워크스페이스 58 결과 줄 · **2818** 통과 · 0 실패 · 20 무시 · `lib_unit` 325 · 프론트 **1122** · 바인딩 sync·격리 게이트 통과.
- GUI 통과: 분할/닫기/승격 **한 프레임 전환**(칸 소실·재생성·빈 프레임 없음) · **이음매 0**(홀수 폭·DSF 1.0/1.25/1.5·네이티브 리사이즈) · `round()` 지원 · 테두리 인셋 0.8px@125%(TRD §7 미결 답) · 드래그 추종·놓을 때 1회 확정·튐 없음·한계 0.1 · `set_split_ratio(0.25)` 화면 반영 · 더블클릭 0.5 · 메뉴 닫힘 · 글자 선택 없음 · 팝아웃 평평 렌더·캔버스 보고·드래그 · 탭 전환 DOM 유지 · 오류 0.
- **미측정:** 터미널 글자 선명도·열 수 안정·WebGL 좌석·드래그 중 PTY 리사이즈(TRD R2) — 백엔드가 claude/codex 뿐이라 실 에이전트가 필요.
- 관찰(결함 아님): 루트 CSS 크기가 소수(125%)일 때 바깥 테두리가 0.2~0.4 CSS px 넘치거나 모자람(TRD §2e ①, 보이지 않음) · 구분선 선은 반올림 없이 `at×100%` 라 최대 ~0.4px 어긋남(같은 색이라 안 보임).

## 사용자 GUI 확인 목록 (master 머지 전)
1. **실 에이전트 터미널 칸**을 띄운 상태에서 구분선을 끌기 — 글자 흐림·열 수 흔들림·드래그 중 터미널 재배치 체감(TRD R1/R2).
2. 분할·닫기·팝아웃·탭 전환이 평소처럼 자연스러운지.
3. 창 테두리를 손으로 끌어 크기 변경(자동 QA 는 헬퍼로만 했다).
4. 채팅(rich) 칸이 있는 상태에서 드래그가 부드러운지.

## 8단계 목록 (allotment 걷기 + 흔적)
`src/main.tsx:5-8`(allotment CSS import + 주석 — 7단계 뒤 거짓) · `src/index.css:33-40`(allotment 변수) + `:19-20` 주석 근거 · `package.json:28` + lock(npm uninstall) · `src/components/slot/slotDaemonRestart.test.tsx:72-74` mock · `src/components/slot/TerminalSlot.tsx:301` 주석 · `src/components/layout/Splitter.tsx:158` 「(allotment 와 같다)」 · `LayoutLeaf.tsx:~111` 「allotment 는 DOM 스타일만 바꿨다」 · `ViewLayoutRenderer.flat.test.tsx:~523-524` 「allotment(DOM 스타일만 바꿈)보다」 · 남은 `rg -n allotment src/ scripts/ .claude/` 전수. → `npm test`·`tsc`·`/qa full`.

## 9단계 목록 (`/review doc`)
- TRD: §2h(:200-202) 1 ulp 과장 · §5(:255) geometry 교차 일치 = 1단계 한정 · :90 버스 `SplitRatioOutcome` → `RatioOutcome` · §4 캔버스 보고 상태 = `windowCanvasReport.ts`(웹뷰 싱글톤·최신 값·한 번에 하나) + 「잎 React.memo」 → 틀/`SlotBody` · §2d 보고 규칙 · §2e id 불일치 규칙 · §2f 범위 = 렌더 시점(캔버스 구독, 보고 전 루트 rect) + 밀려난 드래그 해제는 확정 안 함 · §7 R1 답(round() 지원·인셋 0.8px@125%·이음매 0 실측) · ADR-0227 앵커 목록에 `windowCanvasReport.ts`(ADR 은 누적).
- 내부 결정 기록: `slot_px` = 캔버스·지표 둘 다 알 때만 Some · 결과값 철자 두 표면 동일(버스 enum `RatioOutcome`) · 버스 view/split 없음 = CONFLICT.
- `src-tauri/src/layout/types.rs:70` ratio 문서 0.0~1.0 → 0.1~0.9(바인딩 재생성 같은 커밋).
- `CLAUDE.md`: 바인딩 내보내기 테스트 수 「그 8개」(세지 말고 문장 수정) · `lib_unit` 251 → 325 · 워크스페이스 2724 → 2818 · 기술 스택 allotment 제거.
- `prompts/engram-help.md` split.setRatio: 결과값 정렬된 줄 · 「(Applied 가 아니면 지금 값)」.
- `docs/reference/structure/session-path-ownership.md:1241,1269,1822,1862` → `LayoutLeaf.tsx`.
- `.claude/skill-bindings/qa.md`: §full 에 GUI 도구 조리법 승격 후보(qa 스킬 feedback.md 참조) — 사용자 승인 사안.
- 선택: `spatial.rs` 헤더 포인터 · `step-log.md` 이 라운드 항목(1~9 커밋·게이트) · 성능 후속(`SlotBody` memo 가 `node` 참조 비교 → 스냅샷마다 모든 칸 내용 재렌더; `node.id`+`content` 값 비교로 개선 가능).

## GUI QA 요령
부팅 오류 = 같은 CDP 세션 `addScriptToEvaluateOnNewDocument` → `reload` · view 캐시 = 페이지가 실제 로드한 모듈 URL dev `import()`(다른 URL = 빈 인스턴스) · 마우스/키 = `Input.dispatch*` 스크립트 · ★IPC 관찰은 `window.fetch` 감싸기(`ipc.localhost`) — `__TAURI_INTERNALS__.invoke` 는 대입 불가(조용히 무시)★ · 페이지 JS 는 Write 도구로(heredoc 이 역슬래시를 먹는다) · 네이티브 리사이즈 = 컴파일 C# `SetWindowPos` exe(`csc.exe`), ★PowerShell P/Invoke 금지★ · 버스 CLI = NO_TOKEN → `invoke` · rAF 스텁(`Splitter.test.tsx:32`). (7단계 QA 도구 모음은 세션 스크래치패드에 있었고 사라진다 — qa 스킬 feedback.md 에 기록.)

## 코더·리뷰어 요령
TRD 절·줄 인용(의역 금지) · 상태 기계는 모양 먼저 · 규칙별 변이 1종 · CRLF = Edit 도구 + python 바이트 검사 · 변이는 한 셸 호출(백업→변이→실행→복원→sha1) · 30만 토큰 넘은 에이전트엔 재지시 말고 새 에이전트 · codex blind = `~/.claude/skills/review/codex-call-spec.md`(`--fresh`/`--resume`). ADR 다음 번호 = **0230**.

## 정지 조건
게이트 비초록 · 재수정 3회 초과 · BLOCK · D1~D6·R11 밖 체감 동작 · master 머지(사용자 GUI 확인 전 금지).

## 참조
TRD `docs/process/S21-flat-split-renderer/trd.md` · ADR-0227 · 앞 핸드오프 `history/20260925-070455-…`(1~6 상세)·`history/20260925-083232-…`(7단계 파일·리뷰 경과).
