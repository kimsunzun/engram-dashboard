# 핸드오프: S14 출력 평면 2단계 커밋 완료(ffcd766) → 다음 = 모듈③ 슬롯 렌더(가시화). ⚠️ 전역 플러그인 4개 OFF

> `.claude/continue` gitignored(로컬). master, HEAD = **ffcd766**(이번 세션 커밋). 작업 트리 **클린**. 직전 핸드오프(20260630-210451)의 후속 — 그 "5차 재리뷰+cdp+커밋 대기"를 이번 세션이 전부 닫음.

## 한 줄 상태 + 다음 첫 액션
멀티뷰어 출력 평면 2단계(공유 버퍼 통합)가 **백엔드 완성·검증·커밋 완료**. 5차 cross-family 리뷰가 deliverable 게이트 누락 BLOCK 적출 → 수정 → 재검증(BLOCK 닫힘·신규결함0) → cdp 실측(라이브 배달 + 버퍼 전체 replay end-to-end) → ADR 0041~0043 + step-log + 커밋 ffcd766.

**다음 첫 액션:** **모듈③ 슬롯 렌더** — 메인창 ViewManager 슬롯이 지금 **터미널 stub**(`ViewLayoutRenderer.tsx`가 "Slot/agent"만 표시)이라 출력 평면이 화면에 안 보인다. ViewManager 슬롯 ↔ 실제 터미널(xterm) 렌더러를 연결하고 **멀티윈도우 fan-out을 cdp로 실측**하면 가시화 완성. PRD→TRD→DDD→구현 순서(굵으면 사용자 결정). 백엔드는 준비됨 — 프론트 wiring + 실측 위주.

## ⚠️ 환경 변경 (이번 세션 — 다음 세션 영향, 꼭 읽기)
**전역 플러그인 4개를 OFF 했다** (`~/.claude/settings.json` `enabledPlugins` 전부 `false`): `clangd-lsp@claude-plugins-official`, `nmfc-origin-wiki`, `nmfc-ue5-bridge`, `nmfc-origin-dev-harness`. 사용자 지시("전역으로 끄자 모두" — opt-in 모델로 전환, 매 세션 ~80k 토큰 절약).
- **적용 = 다음 세션부터**(재시작 시 로드 결정). 이 대시보드 워크스페이스엔 무영향(원래 안 씀).
- **★미완 후속:** **Origin 게임 프로젝트 + AVL C++ 워크스페이스는 그 프로젝트 `.claude/settings.json`에 `enabledPlugins:{...:true}`로 재활성해야 함 — 아직 안 했다.** 안 하면 거기서 dev-harness(bug-fix·ue5-dev·work-finish·p4-control)·언리얼 MCP·wiki·clangd LSP가 안 뜬다. Origin 게임 프로젝트 경로 모름(마켓플레이스 경로 F:\origin\…, I:\Engram_Workspace\agents\nmfc\…는 *플러그인 소스* 디렉토리지 게임 프로젝트 working dir 아님). 사용자에게 게임 프로젝트 경로 받아 거기서 재활성 세팅 필요.
- project-scope `enabledPlugins` override가 유저-scope를 덮는다(가능성 높음, 적용 후 /context로 확인 권장 — 미실측).

## 완료 + repo 상태
- **커밋 ffcd766** (master): 26 files, +4961/−500. 출력 평면 2단계 + T7 transport cutover 보류분 + CLAUDE.md 규약 2건 + 모듈① transport spike 노트. 작업 트리 클린.
- ADR **0041**(데몬 구독=layout 델타 단독·프론트 차단) · **0042**(구독 델타=slot 단위 diff) · **0043**(mount-replay=actor 경유+deliverable 게이트+배정/등록 fresh 분기). 인덱스 재생성·lint clean(error 0).
- step-log 최신 항목 추가됨.
- core 신규: `output_view_store.rs`(deliverable 게이트가 subscribe/resubscribe_slot/reconcile_slots에 균일 적용, reconcile 복구=cursor 값 기준) · `output_view_buffer.rs`. src-tauri: `output_channel.rs`(`build_deliverable` 순수 헬퍼) 등.

## 검증 상태 (쌍으로)
### 돌린 것 (green)
- `cargo test -p engram-dashboard-core`(130) · `-p protocol` · `cargo build`(루트) · `cargo fmt --check` · `rg "use tauri" crates/.../core/src`(0). 재실행 = 루트에서 그대로.
- **cdp 실측(TauriTransport 런타임 경로, 앱 띄워서):** ① 렌더러 등록 후 resize→redraw 출력이 새 평면으로 도착(라이브) ② 창 출력 Channel 재등록(`transport.registerOutputChannel()`)→버퍼 전체(seq 0~2, 183B) 재배달(replay). 둘 다 등록 렌더러에 도달 확인. 방법 = `node scripts/cdp.mjs eval "(async()=>{...ac.subscribeOutput...})()"`(포트 9223, 앱은 `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9223 npm run tauri dev`).
### 검증 안 됨 (오신뢰 금지)
- **모듈③ 자체 미착수.** 메인창 슬롯 터미널 렌더 = stub 그대로(의도). 멀티윈도우(여러 창 같은 agent) fan-out은 cdp로 *화면* 확인 안 함 — transport 레벨 바이트 흐름만 확인.
- **popup 창 터미널 실제 렌더** 시각 미확인(`PopupPage.tsx`가 `TerminalSlot` 렌더하나 이번에 안 띄워봄).
- **F2(2차 결함):** 빠른 drop→재배정(toggle/drag) 시 cursor 제거 후 재신설→전체 replay→같은 Channel 중복 send. 저심각(프론트 seq dedup이 화면 중복 차단·대역폭만 낭비), cursor lifecycle on drop 영역이라 이번 deliverable 게이트로 안 닫힘. 미수정·보고만.

## do-not / 실패한 접근
- **메인창 ViewManager 슬롯에서 터미널 출력을 보려 하지 말 것** — 거긴 stub다(모듈③ 전까지). 출력 평면 검증은 transport 레벨(`window.__ENGRAM_AGENT__.subscribeOutput` / `.transport.registerOutputChannel()`)로 한다(이번에 그렇게 함).
- 출력 평면 불변식(커밋됨, 깨지 말 것): 두 축(데몬 after_seq=버퍼 최신 ↔ per-view cursor) 합치기 금지 · buffer 락 보유 중 Channel send 금지(락 밖 flush) · buffer 락 ⊃ registry 락 중첩 0 · cursor advance ⟺ Channel delivery(membership은 deliverable 무관, advance만 게이트) · min_render_seq 모델 부활 금지 · 프론트 직접 데몬 구독 부활 금지(ADR-0041).
- 앱 실측 시: 기존 dev 인스턴스가 떠 있으면 포트 1420 충돌 → 종료 후 디버그 포트로 재실행(이번엔 사용자 인스턴스 종료+재실행함, 디버그 앱은 이후 종료됨). cdp는 `scripts/cdp.mjs`(MCP 아님, node 내장).

## 미결정·후속 (커밋 후 잔여)
- **코드 앵커 점진 부착:** load-bearing 코드에 `// ADR-0041`·`0042`·`0043` 한 줄(신규·수정분부터). 이번엔 시간상 안 붙임 — 다음에 boy-scout.
- Origin/AVL 워크스페이스 플러그인 재활성(위 ⚠️).
- 로드맵(step-log "다음 미진행"): LLM 제어 표면(§5 command 버스) · UI 레이아웃 영속화 · 원격/모바일 · 모드 시스템 · codex/gemini CLI spike · 자동재시작 · 메시지 시스템.

## 이번 세션 서브에이전트 이력
- **`/review code full`(5차):** opus doc-aware breaker(reviewer-deep) + Codex blind breaker → 둘 다 같은 BLOCK 수렴(deliverable 게이트 누락 → 영구 빈 화면). cross-family 4번째 실증.
- **코더 opus ×2:** ① 5차 BLOCK 수정(deliverable 전파 + reconcile 가드 cursor 값 기준 + 주석 rot 2건) ② minor FIX(FIX-A epoch reset None 주석 + FIX-B build_deliverable 헬퍼 분리).
- **재검증 = fresh opus doc-aware adversary(reviewer-deep):** BLOCK 닫힘·신규결함0 확인, minor FIX 2건만(주석 정합·통합 회귀 공백).

## 참조 (읽어야 할 것만)
- ADR `docs/decisions/`: **0040**(출력 단위=View 독립 토대)·**0041~0043**(이번 배선 결정).
- step-log 최신 항목(2026-07-01 모듈① 2단계).
- TRD `docs/process/S14-multi-page-layout/output-view-buffer-trd.md`(§1 락·§3 두 축·§4 생명주기).
- 모듈③ 진입점 코드: `src/components/layout/ViewLayoutRenderer.tsx`(현재 stub — 주석에 "전면 이주는 다음 슬라이스") · `src/components/slot/TerminalSlot.tsx`(실제 xterm — `agentClient.subscribeOutput`) · `src/pages/PopupPage.tsx`(popup 터미널) · `src/components/layout/AppLayout.tsx`(메인 렌더).
