<!-- 이 템플릿은 research/review/qa/adr/new-skill에서 공통 패턴을 추출한 것이다.
     기존 스킬이 변경되면 이 파일도 함께 갱신해야 한다 — 양쪽 중 하나만 갱신하면 rot한다.
     정본 비교 대상: .claude/skills/{research,review,qa,adr}/references/flow.md -->

# <스킬명> — 실행 절차

`$ARGUMENTS` = `[<인자 시그니처>]`. <인자 설명 한 줄>.

> 인자가 없는 단일 흐름이면: "`$ARGUMENTS` 없음. 단일 흐름."

## 0. 인자 파싱·기본값

- **<인자 1>** — <파싱 규칙. 없으면 기본값 X>
- **<인자 2>** — <파싱 규칙>
- 파싱 완료 후 "어떤 <인자>로 실행하는지" 사용자에게 한 줄 명시.

> 여러 인자 조합이 있으면 `## 0-1. 추정 규칙` 하위 섹션으로 확장 (review flow.md §0-1 패턴 참조).

## 1. <첫 번째 단계명>

<단계 설명 — 무엇을 하고, 어떤 조건에서 분기하는지.>

에이전트 스폰이 있는 경우 (예시):
```
- **<역할명> = opus/sonnet 서브에이전트** — <역할 설명>.
  스폰 시 컨텍스트: <무엇을 넘기는지>.
  출력 형식: <구조화 반환 형식 — verbose 덤프 금지>.
- 결과 취합: <어떻게 취합하는지>.
```

> 에이전트 스폰 없는 단계면 위 블록 생략.

## 2. <두 번째 단계명>

<단계 설명>

선택지 제시가 있는 경우:
- AskUserQuestion 또는 브리핑 형식으로 제시 — 임의 채택 금지.
- 사용자 확인 전 다음 단계 진행 금지.

## 3. <세 번째 단계명>

<단계 설명>

파일 생성이 있는 경우:
- 생성 전 §2 확인 완료 여부 체크.
- 생성 파일 목록을 사용자에게 명시한 뒤 생성.

> 하위 작업이 있으면 `### 하위 섹션명`으로 내린다 (헤더 레벨: `##` = 주 단계, `###` = 주 단계 내 하위 작업).
> 하위 번호 확장: `## 0-1.` 형식 (review flow.md 참조), `### 3.1.` 형식 사용하지 않는다.

## N. 완료 보고

완료 후 사용자에게 알릴 내용:
1. <산출 파일/결과>
2. <다음 권장 액션 — 있으면>
3. <열린 이슈/미결 — 있으면>

## 가드레일

앞 절에 없는 금지 규칙만. 중복 금지.

- **<금지 행동> 금지** — <이유>
- **<금지 행동> 금지** — <이유>

> 바인딩 의존 명령(cargo test, npm run 등)은 여기 하드코딩하지 않는다 — `bindings/<project>.md`에.
