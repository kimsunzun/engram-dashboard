//! tray — Tauri 통합 트레이 모듈(ADR-0026: 트레이를 앱에 통합, 2프로세스).
//!
//! 1단계(현재): 별도 tray-host crate 의 **순수 로직** 만 `core` 로 이관(동작 변경 없음).
//! 트레이 네이티브 배선(생성/메뉴/아이콘/X=hide)은 다음 단계 — 아직 아무도 core 를 호출하지 않는다.

pub mod core;
